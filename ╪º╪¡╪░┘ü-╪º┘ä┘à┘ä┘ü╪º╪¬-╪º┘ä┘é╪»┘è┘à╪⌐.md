# الملفات القديمة — هذه هي التي توقف البناء الآن

**٢٠٢٦-٠٨-٢١** · بعد أن نجح إصلاح السياق

---

## الخبر الجيد أولًا

سجل البناء الأخير يقول:

```
[builder  6/11] RUN npm ci                           ← نجح
[builder  7/11] RUN npx prisma generate              ← نجح
[builder  8/11] COPY tsconfig.json … ./              ← نجح
[builder  9/11] COPY src ./src                       ← نجح
[builder 10/11] RUN npm run build                    ← فشل هنا
```

**عطل السياق انتهى تمامًا.** البناء يصل الآن إلى مرحلة التصريف — وهذه أول مرة يُصرَّف فيها كود هذا المشروع على خادم حقيقي.

---

## سبب الفشل الجديد

```
src/modules/auth/application/services/pairing.service.ts
  TS2305: Module '"../../domain/auth.types"' has no exported member 'IConfirmPairingInput'
  TS2305: … has no exported member 'IPairingTicket'
  TS2305: … has no exported member 'DEVICE_REPOSITORY'
  TS2305: … has no exported member 'IDeviceRepository'

src/modules/auth/infrastructure/repositories/prisma-device.repository.ts
  TS2305: … has no exported member 'ICreateChildDeviceInput'
  TS2305: … has no exported member 'IDeviceRepository'

Found 6 error(s).
```

**الملفان المذكوران محذوفان عندي وموجودان عندك.** هما اثنان من الثلاثة عشر ملفًا في قائمة الحذف بملف `رفع-الكود.md`.

استُبدلت واجهة الاقتران القديمة بوحدة `pairing` كاملة، وحُذفت الأنواع التي كان الملفان يستوردانها. **الملفان يستوردان أسماءً لم تعد موجودة** — فيفشل التصريف.

**ولماذا بقيا؟** لأن **فكّ الضغط يضيف ويستبدل ولا يحذف.** هذه هي الحالة التي حذّرتُ منها بالضبط، وهذا سببها الحقيقي أمامك الآن: الكود يبدو سليمًا، وGitHub يبدو سليمًا، ولا شيء يشير إلى الخطأ حتى يصل الأمر إلى مُصرِّف.

---

## الحل — أمر واحد ثم دفعة

### ١ · احذف الثلاثة عشر

من PowerShell **داخل مجلد المشروع**:

```powershell
@(
 "apps\backend\prisma\migrations\20260731_life_intelligence_platform_sprint13\migration.sql",
 "apps\backend\src\common\utils\age.ts",
 "apps\backend\src\modules\auth\application\services\pairing.service.ts",
 "apps\backend\src\modules\auth\infrastructure\repositories\prisma-device.repository.ts",
 "apps\backend\src\modules\auth\presentation\controllers\device-pairing.controller.ts",
 "apps\backend\src\modules\auth\presentation\dto\confirm-pairing.dto.ts",
 "apps\backend\src\modules\auth\presentation\dto\initiate-pairing.dto.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\apple-iap.adapter.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\fawry.adapter.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\google-play-billing.adapter.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\paymob.adapter.ts",
 "apps\backend\test\auth\pairing.service.spec.ts",
 "apps\backend\test\common\age.spec.ts"
) | ForEach-Object { if (Test-Path $_) { Remove-Item $_ -Force; "حُذف: $_" } else { "غير موجود (لا بأس): $_" } }

Remove-Item "apps\backend\prisma\migrations\20260731_life_intelligence_platform_sprint13" -Recurse -Force -ErrorAction SilentlyContinue
```

**احذف الثلاثة عشر كلها، لا الاثنين اللذين ظهرا في الخطأ فقط.** المُصرِّف يبلّغ عن أول ما يعطبه؛ والأحد عشر الباقية إمّا ستعطب البناء التالي، أو ستبقى كودًا ميتًا يعمل بالطريقة القديمة بصمت — وهذا أسوأ.

> **`railway.json` و`.dockerignore` من جذر المستودع** يجب حذفهما أيضًا إن لم تكن قد فعلت — كانا في `_DELIVERY/DELETE.ps1` بالتسليم السابق. وجودهما في الجذر يُعيد عطل السياق كما كان.

### ٢ · ادفع

GitHub Desktop سيعرض الملفات المحذوفة بعلامة حمراء. **وجودها في القائمة هو دليل نجاح الخطوة الأولى.** اكتب وصفًا وادفع.

### ٣ · تحقّق قبل أن تبني

قبل أن تنتظر Railway، اختبر التصريف على جهازك — دقيقتان بدل دورة بناء كاملة:

```powershell
cd apps\backend
npm install
npx tsc --noEmit
```

**النجاح أن ينتهي بلا أي مخرجات.** إن ظهر خطأ يذكر `pairing.service` أو `age.ts` أو `IDeviceRepository`، فالحذف لم يكتمل.

---

## لماذا لم يظهر هذا قبل الآن

عندي في المستودع الاختبارات كلها خضراء و`tsc` نظيف — لأن الملفات محذوفة أصلًا. وعندك `nest build` يُصرِّف **كل ما تحت `src/`** بما فيه اليتيم. فالفرق ليس في الكود، بل في **ملفات زائدة عندك ليست عندي**.

وهذا بالضبط ما جعلني أضيف `_DELIVERY/DELETE.ps1` إلى كل تسليم بعد ذلك: **الأرشيف لا يستطيع الحذف، فوجب أن يقول ما يجب حذفه.**
