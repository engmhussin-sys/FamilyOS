# بيانات التجربة — أمر واحد يُنشئها لك

**٢٠٢٦-٠٨-٢١**

---

## أولًا: ما طلبتَه ثلاثة، واثنان منها لا وجود لهما كحسابات

وهذا تصميم مقصود، لا نقص — ولن أخترع لك حسابات ليست في المنتج.

### «سوبر أدمن» — ليس حسابًا، بل مفتاحًا

`Role.SUPER_ADMIN` في هذا الكود **ليس صفًّا في جدول المستخدمين**. هو من يملك `INTERNAL_ADMIN_API_KEY`، يتحقّق منه `InternalAdminGuard` من ترويسة `x-internal-admin-key`.

**لا بريد له ولا كلمة مرور ولا أسرة.** والحارس **يمتنع عمدًا** عن كتابة `request.user`، لأن مشغّل المنصّة لا أسرة له — وإسناد أسرة له سيضع مستأجرًا كاذبًا على الطلب.

فـ«بياناته» = المفتاح الذي تضعه أنت في متغيّرات الخدمة، وتكتبه في شاشة الفتح داخل الداشبورد وقت التشغيل.

### «أدمن» — هو حساب ولي الأمر نفسه

الداشبورد تسجّل الدخول بـ`POST /auth/login` **بحساب ولي أمر عادي**. لا يوجد دور أدمن منفصل للدخول. والشاشات العائلية تعرض **أسرتك أنت فقط** — وهذا ما تضمنه براهين عزل المستأجرين.

### الطفل — لا بيانات دخول له إطلاقًا

قرار منتج لا إغفال: الجهاز **يُقرَن** برمز، ولا يملك الطفل بريدًا ولا كلمة مرور. **طفل يستطيع أن يصادق على نفسه هو خطر لا ميزة.**

---

## ثانيًا: الأمر الذي يُنشئ كل شيء

```powershell
.\scripts\seed-test-accounts.ps1 https://familyos-staging.up.railway.app
```

يُنشئ عبر الـAPI الحقيقي — نفس النداءات التي تفعلها أسرة جديدة تمامًا:

1. حساب ولي أمر (والتسجيل يُنشئ الأسرة ومالكها)
2. طفلًا، ويحاول ثانيًا
3. رمز اقتران لجهاز الطفل الأول

ثم يطبع البيانات **ويكتبها في `TEST-ACCOUNTS.txt`** — وقد أضفتُ الملف إلى `.gitignore` في هذا التسليم، فلا يُرفع بالخطأ.

> **كلمة المرور تُولَّد على جهازك أنت**، ولا تمرّ بي ولا بأي طرف. ولو أردت واحدة بعينها: `$env:ABNY_TEST_PASSWORD = '...'` قبل التشغيل.
>
> **الأمر يُنشئ حسابًا حقيقيًّا** على المضيف الذي تسمّيه، ولا تراجع فيه. البريد مختوم بالوقت، فتكرار التشغيل لا يتعارض أبدًا.

### ما يبدو عليه الناتج

```
=== ABNY TEST ACCOUNTS ===
created  parent account
created  session
created  child Omar (58761e7e-…)
skipped  child Salma — the plan on a new family allows one child (403 PLAN_UPGRADE_REQUIRED)
created  pairing code for Omar

PARENT / ADMIN DASHBOARD LOGIN
  email    : abny.test.20260821-130121@example.com
  password : Abny…1
  use for  : the dashboard sign-in page, and the parent mobile app

CHILD APP PAIRING
  child    : Omar (id 58761e7e-…)
  code     : ……
  expires  : … seconds
TEST ACCOUNTS READY
```

---

## ثالثًا: أمر اكتشفه السكربت وأنت تحتاج معرفته قبل التجربة

**باقة الأسرة الجديدة تسمح بطفل واحد.** الطفل الثاني يردّ:

```
403  PLAN_UPGRADE_REQUIRED
"باقتك الحالية تكفي لطفل واحد. طوّر باقتك لتضيف بقية أطفالك…"
```

**هذا هو الجدار المالي يعمل كما ينبغي، لا عطل.** لكنه يعني شيئًا عمليًّا لخطة تجربتك: **سيناريوهات الإخوة لا تُختبر على حساب جديد** حتى تُرقّى الباقة.

اكتشفتُ هذا لأن المسوّدة الأولى من السكربت اعتبرته فشلًا وتوقّفت بعد الطفل الأول — أي أنها كانت ستُبلّغ عن جدار مالي سليم كأنه سكربت معطوب، وتترك أسرة نصف مزروعة. صار الآن: الأول إلزامي، والثاني يُحاوَل، والرفض **يُبلَّغ عنه بوصفه سلوك المنتج**.

---

## رابعًا: ترتيب التجربة

| # | ماذا | بماذا |
|---|---|---|
| ١ | **الداشبورد** — الشاشات العائلية | البريد وكلمة المرور من الملف |
| ٢ | **الداشبورد** — شاشات المنصّة (`/growth/*` وسجلّ القرارات) | نفس الحساب **+ مفتاح المشغّل** في شاشة الفتح |
| ٣ | **تطبيق ولي الأمر** | نفس البريد وكلمة المرور |
| ٤ | **تطبيق الطفل** | **رمز الاقتران** — لا بريد ولا كلمة مرور |

> ورمز الاقتران **يُستهلك مرة واحدة**؛ لتوليد غيره: `POST /api/v1/pairing/invite` بـ`{"childId":"…"}`.

---

## بأي درجة إثبات

| | |
|---|---|
| `seed-test-accounts.sh` | **RUNTIME VERIFIED** — خمسة اختبارات تشغّل السكربت نفسه على `AppModule` مُقلِع يستمع على منفذ حقيقي |
| البيانات التي يطبعها | **مُثبتة تشغيليًّا** — الاختبار يقرأ البريد وكلمة المرور **من مخرجات السكربت** ويسجّل بهما الدخول من جديد. سكربت يطبع كلمة مرور لم يُجرّبها هو العطل الذي يمنعه هذا |
| رمز الاقتران | **مُثبت تشغيليًّا** — يُستهلك فعلًا، والاستهلاك الثاني **يُرفض** |
| `seed-test-accounts.ps1` | **STATIC VERIFIED** — لا PowerShell في بيئتي. يحرسه فحص التكافؤ في CI، وقد وسّعتُه ليشمل هذا الزوج |

٢٤ اختبارًا في `test/deploy` · `tsc` نظيف · تكافؤ الأزواج الثلاثة ٠.
