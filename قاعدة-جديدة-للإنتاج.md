# قاعدة جديدة للإنتاج — بالنقرات

**٢٠٢٦-٠٨-٢١** · لا يُحذف شيء · القاعدة القديمة تبقى كما هي

---

## قبل أي نقرة

**تأكّد أن مُحدِّد البيئة أعلى الصفحة مكتوب عليه `production`.**

كل ما يلي يجب أن يحدث داخل هذه البيئة. لو كان مكتوبًا `staging` فبدّله أولًا — staging سليمة الآن ولا تلمسها.

---

## ١ · أضف قاعدة بيانات جديدة

في صفحة المشروع:

```
+ New   ←   Database   ←   Add PostgreSQL
```

ستظهر خدمة جديدة خلال ثوانٍ. **اقرأ اسمها بدقّة وسجّله** — سيكون شيئًا مثل `Postgres-abcd` أو `Postgres-2`، لأن الاسم `Postgres` محجوز للقديمة.

> **لا تحذف القاعدة القديمة ولا تلمسها.** بقاؤها هو ما يجعل هذه الخطوة قابلة للتراجع.

---

## ٢ · وجّه الخادم إلى الجديدة

اضغط خدمة **`FamilyOS`** ← تبويب **`Variables`** ← ابحث عن **`DATABASE_URL`** ← عدّلها.

### الطريقة الأنظف — مرجع

اكتب في الحقل:

```
${{Postgres-abcd.DATABASE_URL}}
```

**استبدل `Postgres-abcd` بالاسم الذي سجّلته في الخطوة ١.**

> عند كتابة `${{` تفتح Railway قائمة بالخدمات المتاحة — اختر منها بدل الكتابة، فهذا يمنع أي خطأ إملائي في الاسم.

### إن تعذّرت الكتابة على الهاتف — لصق مباشر

افتح خدمة **Postgres الجديدة** ← `Variables` ← انسخ قيمة `DATABASE_URL` ← الصقها في `DATABASE_URL` الخاصة بـ`FamilyOS`.

> اختر النسخة الداخلية (`…railway.internal…`) لا العامة (`…proxy.rlwy.net…`). الداخلية أسرع ولا تُحتسب استهلاك شبكة خارجية.

**الفرق الوحيد:** المرجع يظلّ صحيحًا لو تغيّرت بيانات الاتصال يومًا؛ اللصق لا. وكلاهما يعمل الآن.

---

## ٣ · وبينما أنت في `Variables`، أضف هذين

| المتغيّر | القيمة |
|---|---|
| `INTERNAL_ADMIN_API_KEY` | قيمة عشوائية طويلة تولّدها أنت — **لا ترسلها إليّ** |
| `CORS_ALLOWED_ORIGINS` | نطاق لوحة التحكم على الإنتاج، بلا مسار وبلا شرطة أخيرة |

لتوليد المفتاح على Windows:

```powershell
-join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Max 256) })
```

> بلا `INTERNAL_ADMIN_API_KEY` يفشل حارس المشغّل **مغلقًا** — فلا تقرأ شاشات التشخيص أنت أيضًا. وهو أيضًا ما يُغلق لوحة المشغّل المكشوفة حاليًّا على الإنترنت.

---

## ٤ · انشر

Railway غالبًا تبدأ نشرًا تلقائيًّا بمجرّد حفظ المتغيّرات. **إن لم تبدأ:**

```
Deployments   ←   النشر الأخير   ←   ⋮   ←   Redeploy
```

---

## ما تقرأه في السجلّ إن نجح

افتح **Deploy Logs** — لا Build Logs. المطلوب أن ترى:

```
[predeploy] === ABNY RELEASE STEP ===
Applying migration `0001_init`
Applying migration `0002_rewards_integrity_constraints`
…
Applying migration `0030_retire_duplicate_daily_goal_reward_rules`
[predeploy] migrate deploy succeeded. Nothing else to do.
```

**لن ترى `P3005` إطلاقًا.** القاعدة الفارغة لا تُثيره أصلًا — لا يوجد فيها ما «ليس فارغًا». تسع وعشرون هجرة تبني ١٠١ جدول و٥٦ سياسة عزل وسجلًّا تكتبه Prisma بنفسها، فلا يعود العطل إلى هذه القاعدة أبدًا.

ثم يقلع التطبيق، ويردّ `/health/ready`، وتتحوّل الحالة إلى **ACTIVE**.

---

## ثم تحقّق — ولا تصدّق الشارة الخضراء وحدها

```powershell
.\scripts\deploy-doctor.ps1 https://familyos-production-74ca.up.railway.app -Key $env:INTERNAL_ADMIN_API_KEY
```

المطلوب حرفيًّا:

```
PASS  build-identity   the operator surface is CLOSED
PASS  schema-version   29 migrations applied, latest 0030_… — matches this repository exactly
DEPLOY VERIFIED
```

- `PASS build-identity` ← الكود الحالي هو الذي يردّ، ولوحة المشغّل صارت مغلقة.
- `PASS schema-version` ← الهجرات التسع والعشرون **مطبَّقة فعلًا**، مقروءة من `_prisma_migrations` في القاعدة الحيّة نفسها.

---

## إن أردت حساب التجربة على القاعدة الجديدة

نداء واحد، بكلمة مرور تختارها أنت:

```powershell
$body = @{ email="you@yourdomain.com"; password="<كلمة-مرور-تختارها>";
           fullName="اسمك"; familyName="أسرتك"; acceptedTerms=$true } | ConvertTo-Json
Invoke-RestMethod -Uri "https://familyos-production-74ca.up.railway.app/api/v1/auth/register" `
  -Method Post -Body $body -ContentType 'application/json'
```

والحساب القديم باقٍ في القاعدة القديمة، لم يُمسّ.

---

## وإن ظهر خطأ

**أرسل لي `Deploy Logs` كما هي.** السكربت لا يفشل بصمت بعد اليوم: كل رفض يطبع ما قاسه بالضبط — عدد الجداول، وعدد السياسات، وما تحويه القاعدة. وذلك يكفيني لأقول لك السبب في رسالة واحدة.
