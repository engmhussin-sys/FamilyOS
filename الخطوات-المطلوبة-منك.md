# الخطوات المطلوبة منك — بالتفصيل

**٢٢ أغسطس ٢٠٢٦** · فرعك عند `dc704c1` · `main` عند `ccea0e8`

---

## أين أنت الآن بالضبط

| | |
|---|---|
| `main` على GitHub | `ccea0e8` — الدمج الأول تمّ ✅ |
| فرعك `feat/w2-operations-surface` | `dc704c1` — **متقدّم بـ commit واحد** (إصلاح CI) |
| PR مفتوح | **لا يوجد** — الأول أُغلق بالدمج |
| CI | **لم يعمل قط** — يعمل لأول مرة بعد الخطوة ٢ |

**خمس خطوات. الأولى أهمّها، وتأخذ دقيقتين.**

---

# الخطوة ١ · تحقّق من Railway — هل أقلعت الحاوية؟

الدمج الأول (`ccea0e8`) شغّل نشرًا تلقائيًا على Railway. **هذه لحظة الحقيقة**
لإصلاح `Cannot find module 'pg'` الذي كان يمنع الإقلاع.

### افعل

افتح لوحة Railway ← الخدمة ← تبويب **Deployments** ← آخر نشرة.

### ما تبحث عنه

| ما تراه | المعنى |
|---|---|
| **Active / Success** ✅ | الحاوية أقلعت. انتقل للخطوة ٢ |
| **Crashed** أو حلقة إعادة تشغيل | افتح **Logs** وأرسل لي آخر ٢٠ سطرًا |
| **Build failed** | افتح **Build Logs** وأرسل لي سطر الخطأ |
| لا توجد نشرة جديدة | Railway غير موصول بالدمج التلقائي — أخبرني |

### ثم اختبرها بنفسك

استبدل `<host>` بعنوان خدمتك من Railway (تبويب **Settings ← Domains**):

```
https://<host>/health/ready
```

**المطلوب أن ترى بالضبط:**

```json
{"status":"ok","database":true,"redis":true}
```

| ما تراه | التشخيص |
|---|---|
| `"database":false` | `DATABASE_URL` خطأ أو الـ migrations لم تُطبَّق |
| `"redis":false` | `REDIS_URL` مفقود — أضِفه، فجلسات المشغّلين تعيش فيه |
| صفحة خطأ 502 / لا شيء | الحاوية لا تعمل — ارجع إلى **Logs** أعلاه |

> **`/health/live` يبقى 200 حتى لو سقطت قاعدة البيانات** — هذا مقصود ومُختبَر،
> وإلا لقتلت المنصّة عمليةً سليمة عند أي تعثّر مؤقت.

---

# الخطوة ٢ · ادمج إصلاح CI

فرعك يحمل commit واحدًا يصلح السبب الذي جعل CI **لا يشغّل أي وظيفة إطلاقًا**.

### افعل

افتح هذا الرابط:

```
github.com/engmhussin-sys/FamilyOS/compare/main...feat/w2-operations-surface?expand=1
```

1. **Create pull request**
2. لا تنتظر الفحوص هنا — الملف الحالي على `main` ما زال معطوبًا فلن تعمل
3. **Merge pull request** ← **Confirm merge**

### ما يحدث بعدها

- `ci.yml` يعمل **لأول مرة في تاريخ المشروع** على `main`
- Railway ينشر مرة أخرى (تغيير في ملفات CI فقط — لا أثر على الكود)

---

# الخطوة ٣ · اقرأ نتائج CI الحقيقية

**Actions** ← أحدث تشغيل لـ `ci.yml` على `main`.

الآن ستظهر **خمس وظائف** بدل رسالة `Invalid workflow file`:

| الوظيفة | التوقّع | إن فشلت |
|---|---|---|
| **Backend (NestJS)** | ✅ | شغّلت كل بواباتها يدويًا اليوم — أرسل لي الخطأ |
| **Admin Dashboard (React)** | ✅ | نفس الشيء |
| **Docker Build** | 🟡 أول بناء حقيقي | فحصتُ الـ `COPY` الـ ١٣ استاتيكيًا — كلها موجودة |
| **Flutter** | ⚠️ **متوقّع أن يفشل** | **هذا مقصود** — اقرأ أسفل |
| **CI gate** | يفشل إن فشل أي شيء فوقه | — |

### 🔴 قرار: إن فشلت `Flutter` وحدها

**تجاهلها اليوم.** الواجهة الخلفية لا تعتمد على تطبيقَي الموبايل إطلاقًا.
ترقية AGP 9 لم تُبنَ قط، وفشلها هو بالضبط ما يكشف ما يحتاج تعديلًا — ولها
تسعة أيام حتى موعد Google Play، بينما الخادم يُنشر اليوم.

### كيف ترسل لي خطأ

افتح الوظيفة الحمراء ← الخطوة الحمراء ← انسخ **آخر ٢٠–٣٠ سطرًا**. سطر
`Error:` أو `error:` هو المهم. لا تحتاج إرسال السجل كله.

---

# الخطوة ٤ · أنشئ أول مشغّل

بعد أن يصير `/health/ready` أخضر. المسار **يُغلق نفسه للأبد** بعد أول مشغّل —
لا يمكن تشغيله مرتين.

### ما تحتاجه

قيمة `INTERNAL_ADMIN_API_KEY` من متغيّرات Railway.

### الأمر (من PowerShell عندك)

```powershell
$key  = "<القيمة من Railway>"
$host = "https://<host>"

$body = @{
  email    = "you@abny.app"
  fullName = "اسمك الكامل"
  password = "كلمة-سر-طويلة-جدا-١٢-حرفا-فأكثر"
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri "$host/api/v1/system/operators/bootstrap" `
  -Headers @{ "x-internal-admin-key" = $key } `
  -ContentType "application/json" -Body $body
```

**المتوقّع:** `{ id = ...; email = ... }`

| ما تراه | المعنى |
|---|---|
| `409 OPERATORS_ALREADY_EXIST` | يوجد مشغّل بالفعل — استخدم تسجيل الدخول |
| `401` | `INTERNAL_ADMIN_API_KEY` خطأ |
| خطأ في طول كلمة السر | **١٢ حرفًا على الأقل** — الشرط مقصود |

### ثم سجّل الدخول واحصل على الرمز

```powershell
$body = @{ email = "you@abny.app"; password = "نفس-كلمة-السر" } | ConvertTo-Json
$r = Invoke-RestMethod -Method Post -Uri "$host/api/v1/system/operators/session" `
  -Headers @{ "x-internal-admin-key" = $key } `
  -ContentType "application/json" -Body $body
$r.token
```

**الرمز صالح ثماني ساعات ولا يتجدّد** — تسجيل دخول كل صباح، وهذا مقصود:
حاسوب مفتوح ليلًا ليس كونسولًا مفتوحًا.

**لا تضع هذا الرمز ولا المفتاح في أي كود يصل للمتصفّح.**

---

# الخطوة ٥ · ابدأ بناء الموبايل — لا تؤجّله

**موعد Google Play: `targetSdk 36` مطلوب لكل تحديث من ٣١ أغسطس ٢٠٢٦ — بعد تسعة
أيام.** التطبيقان كانا على 34، ورقّيتُ السلسلة كاملة، **ولم تُبنَ**.

### افعل

من GitHub: **Actions** ← **build-apk.yml** ← **Run workflow** ← اختر `both`.

أو من سطر الأوامر إن كان `gh` مثبتًا:

```
gh workflow run build-apk.yml
```

### ما تتوقّعه

**فشل في التشغيل الأول على عمل حقيقي** — وهذا هو الغرض. AGP 9 أزال
`BaseExtension` وخصائص `android.*`، وAPI 35/36 شدّدا قواعد foreground-service
(تطبيق الطفل يعلن `specialUse`)، وأي إضافة في `pubspec` لم تنتقل بعد ستقول ذلك.

أرسل لي سطر الخطأ وسأصلحه.

---

## ملخّص في سطر واحد لكل خطوة

| # | الخطوة | كم تأخذ | الأهمية |
|---|---|---|---|
| ١ | تحقّق من نشرة Railway و`/health/ready` | دقيقتان | 🔴 لحظة الحقيقة |
| ٢ | افتح PR وادمج إصلاح CI | ٣ دقائق | 🔴 بدونه CI أعمى |
| ٣ | اقرأ نتائج CI الحقيقية | ١٥ دقيقة انتظار | 🟠 |
| ٤ | أنشئ أول مشغّل | ٣ دقائق | 🟠 |
| ٥ | شغّل بناء الـ APK | دقيقة + انتظار | 🔴 تسعة أيام |

---

## أشياء لا تحتاج فعلها اليوم

- **تفعيل RLS** — قرار نشر له كلفة أداء. التفاصيل في `FINAL_REVIEW_REPORT.md` §٧
- **نقل الـ ٤٤ مسار مشغّل المتبقية خلف البوابة الجديدة** — يُسجّل خروج كل مشغّل من
  كونسول حيّ، ويستحق نشرًا مستقلًا
- **هجرة DSL الخاصة بـ AGP 9** — موثّقة بمصدرها في `gradle.properties`
- **إضافة linter** — انظر `CONVENTIONS.md` §C-8. نتائج أول تشغيل عمل قائم بذاته،
  واليوم يوم نشر

---

## البند الوحيد الذي يزداد سوءًا بالانتظار

**Paymob / Fawry** — التعاقد يستغرق ٤–٨ أسابيع. كل يوم تأخير يوم إضافي قبل أن
تستطيع تحصيل اشتراك واحد. ابدأه اليوم بالتوازي مع كل ما سبق.
