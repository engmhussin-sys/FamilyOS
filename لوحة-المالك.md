# لوحة المالك — الرابط والحساب وما بداخلها

**٢٠٢٦-٠٨-٢١**

---

## أولًا: نصف ما طلبتَه موجود منذ زمن — ولم ترَه قط

| ما طلبتَه | الحالة |
|---|---|
| رابط أفتحه وأدخل بحسابي | **موجود** — الداشبورد نفسها |
| **إضافة شركات** | **موجود** — `POST /organizations` + الأعضاء والدعوات والسياسات والحملات والهوية، وصفحة `/organization` |
| أرقام المنصّة | **موجود** — `/growth/*`: النظرة التنفيذية، القمع، التوقّعات، البقاء، اقتصاد الوحدة، الاكتساب، الإحالة، المنتج |
| سجلّ قرارات الإشعارات | **موجود** — `/notifications/decisions` |
| **قائمة الحسابات وحالتها** | **بُني الآن** ⬅ |
| **ترقية حساب يدويًّا ثم سحبها** | **بُني الآن** ⬅ |

**ولماذا لم ترَ شيئًا منها؟** لأن `VITE_API_BASE_URL` لم تُضبط على خدمة الداشبورد قطّ، و`INTERNAL_ADMIN_API_KEY` غير موجود على الخادم. فكل شاشة منصّة تفشل **مغلقةً** — في وجهك أنت أيضًا. هذا مقصود: مفتاح غير مضبوط يجب ألّا يعني «مفتوح للجميع» أبدًا.

---

## ثانيًا: كيف تدخل

**بحسابك العادي** — لا يوجد «حساب سوبر أدمن» في هذا المنتج، وقد شرحتُ لماذا في `بيانات-التجربة.md`: `SUPER_ADMIN` هو حامل المفتاح لا صفٌّ في جدول المستخدمين.

1. افتح رابط الداشبورد وسجّل الدخول ببريدك وكلمة مرورك.
2. عند فتح أي شاشة منصّة تطلب منك **مفتاح المشغّل** — الصقه.
3. المفتاح يعيش في ذاكرة الصفحة فقط: يموت مع كل تحديث، فتعيد إدخاله. **مقصود** — مفتاح ينجو من إغلاق التبويب هو مفتاح مسرَّب.

---

## ثالثًا: الشاشة الجديدة — `‎/platform/accounts`

**أول شاشة في هذا المنتج تُرجع صفوفًا لا مجاميع.** كل ما سبق كان `totalFamilies` و«الأسر لكل سوق»؛ ومالك يقرأ «١٢٠٤ أسرة» لا يستطيع أن يجد الأسرة التي راسلته قبل قليل.

لكل أسرة صفّ فيه: **الاسم والسوق · بريد ولي الأمر · حالته (`ACTIVE` / `PENDING_VERIFICATION` / `SUSPENDED`) · عدد الأعضاء · عدد الأطفال · عدد الأجهزة · الباقة وحالة الاشتراك · آخر ظهور للجهاز · وهل توجد منحة سارية.**

وفيها بحث بالاسم أو ببريد ولي الأمر، وتصفّح بالصفحات.

> **ولا تحمل اسم طفل ولا تاريخ ميلاد ولا موقعًا.** بريد ولي الأمر هو الحقل الوحيد الذي يتصرّف عليه المشغّل فعلًا — وهو نفسه حقل تسجيل الدخول. وما عداه عدد أو حالة أو تاريخ. **الاختبار يفحص جسم الرد نصًّا** ويسقط إن تسلّل حقل شخصي لاحقًا.

**والتصفّح keyset لا offset:** المؤشّر `(created_at, id)`، فكل صفحة تكلّف مثل الأولى، ولا تُتخطّى أسرة ولا تتكرّر حين تُسجَّل أسرة جديدة وأنت تتصفّح.

---

## رابعًا: الترقية اليدوية — من داخل الصفّ

اضغط **«إدارة الباقة»** في صفّ الأسرة. تفتح لوحة تُظهر ما هو سارٍ الآن، ثم:

- **اختر المزايا** (ستّ مزايا مُسمّاة، منها `multiple_children` وهي التي تفتح الطفل الثاني)
- **عدد الأيام** — إلزامي، وبحدّ أقصى ٤٠٠
- **السبب** — إلزامي
- **امنح** · أو **اسحب كل المنح**

**ثلاثة قرارات تصميم تستحق أن تعرفها:**

**١ · تنتهي بنفسها.** `grantManual` في الكود يقبل منحة بلا نهاية؛ هذه الشاشة **لا تسمح بها**. منحة بلا تاريخ انتهاء هي أسرة تتوقّف بهدوء عن كونها عميلًا، ولا يلاحظ أحد لسنة.

**٢ · تُدوَّن لا تُسجَّل فقط.** كل منحة وكل سحب يكتبان صفًّا في `audit_logs` بسببك الذي كتبته. سطر السجلّ يذهب مع الحاوية؛ الصفّ يبقى. فسؤال «لماذا هذه الأسرة على PREMIUM بلا دفع؟» يبقى قابلًا للإجابة بعد شهور.

**٣ · السحب يستعمل مسار الاسترداد نفسه** (`revokeAll`) لا مسارًا خاصًّا. طريقان لإنهاء استحقاق يختلفان يومًا ما — وهذا المستودع دفع ثمن خدمتين اختلفتا على معنى «مستحقّ». **ولذلك يسحب كل استحقاقات الأسرة بما فيها أي استحقاق من دفع حقيقي**، والعدد المُعاد هو ما يخبرك بذلك، والتأكيد يقولها صراحة.

---

## خامسًا: عطلان وجدتُهما أثناء البناء

**١ · «ترقية» تنجح ولا تفعل شيئًا.** المسار الذي يمنح **باقة كاملة** يقرأ جدول `plan_definitions` — الموصوف في المخطّط بأنه «يُزرع مرة واحدة»، **ولا تزرعه أي هجرة**. فعلى أي قاعدة مبنيّة من تاريخ الهجرات هو فارغ، وكان النداء يردّ `200` بـ`features: []`: كنتَ ستقول لمختبِر إنه رُقّي، والجدار المالي يرفضه كما كان. صار الآن **يرفض** بـ`PLAN_CATALOGUE_EMPTY`، والمسار الذي تستعمله الشاشة يمنح **مزايا مُسمّاة** لا يحتاج كتالوجًا — لأن تحديد سعر باقة قرار عمل لك، ولستُ أخترعه لأفكّ عقدة اختبار.

**٢ · الشاشة كانت تكتب حالات التحميل بيدها.** حارس معماري في الداشبورد أسقطها بالاسم: الحالات الأربع (تحميل/خطأ/فراغ/محتوى) تعيش في `AsyncBoundary` وحدها. تسع رسائل تحميل مختلفة قليلًا هي كيف تتوقّف لوحة عن أن تبدو منتجًا واحدًا.

---

## سادسًا: ما تفعله أنت

على خدمة **الداشبورد**:

```
VITE_API_BASE_URL = https://<خادم-الإنتاج>/api/v1
```
> تُقرأ **وقت البناء** — لا تعمل حتى تُعيد نشر الداشبورد.

على خدمة **الخادم**:

```
INTERNAL_ADMIN_API_KEY = <قيمة عشوائية طويلة تولّدها أنت>
CORS_ALLOWED_ORIGINS   = https://<نطاق-الداشبورد>
```

ثم افتح `‎/platform/accounts`.

---

## درجات الإثبات

| | |
|---|---|
| `GET /system/accounts` | **RUNTIME VERIFIED** — ٧ اختبارات تُنشئ أسرتين حقيقيتين عبر HTTP وتقرؤهما عبر حدود المستأجرين، وتتحقّق أن العدّاد **يتحرّك** حين يُضاف طفل |
| الترقية والسحب | **RUNTIME VERIFIED** — ٩ اختبارات تُثبتها **بالجدار المالي نفسه**: الطفل الثاني مرفوض قبل المنحة، ينجح بعدها، ويُرفض ثانيةً بعد السحب |
| رفض غير المصرّح | **RUNTIME VERIFIED** — كل مسار جديد يُرفض بلا مفتاح وبمفتاح خاطئ |
| صفحة الداشبورد | **BUILD VERIFIED** — `tsc` + `vite build` ناجحان، و١٩٧ اختبارًا في الداشبورد خضراء بما فيها الحارس المعماري |
| على بيئتك | **NOT TESTED** — المتغيّران أعلاه ثم أول فتح للشاشة |

`tsc` نظيف · حارس النطاق ٠ · حارس الأحداث ٠ · ١٠٩٩ اختبارًا في نطاقات الفوترة والصلاحيات خضراء.
