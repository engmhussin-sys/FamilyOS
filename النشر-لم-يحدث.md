# البناء ينجح — والنشر لا يحدث

**٢٠٢٦-٠٨-٢١ · بعد بناءين ناجحين**

---

## ١ · أعجل ما في هذا الملف

**لوحة المشغّل على الإنتاج مقروءة من الإنترنت في هذه اللحظة، بلا أي مفتاح.**

طلبتُ الآن `GET /api/v1/system/diagnostics` على `familyos-production-74ca.up.railway.app` بلا رمز ولا مفتاح، فردّ **`200`** بهذا:

```json
{"version":"0.1.0","commit":null,"environment":"production","uptimeSeconds":260621,
 "memory":{"rssMb":94,…},"cpu":{…},"queue":null,
 "configValidation":{"isValid":true,"warningCount":4,
   "warningKeys":["LOCATION_ENCRYPTION_KEY","ANTHROPIC_API_KEY","SENTRY_DSN","FIREBASE_SERVICE_ACCOUNT_JSON"]},
 "featureFlags":[]}
```

`warningKeys` تخبر أي زائر **أي أسرار غير مضبوطة على إنتاجك** — أن مفتاح تشفير المواقع غائب، وأن لا تتبّع أخطاء، وأن اعتماد Firebase ناقص. هذه ليست تسريب قيم، لكنها **خريطة لِما لم يُحصَّن بعد**، مجانًا، لمن يسأل أولًا.

و`‏/api/v1/system/readiness` هناك ردّ `503` — أي أنه أجاب هو الآخر بلا مفتاح، وحقول `detail` فيه تحمل نصّ خطأ قاعدة البيانات نفسه وأي مزوّدي دفع مُهيّأون.

**الإصلاح موجود في الكود منذ أيام ومختبَر بأحد عشر اختبارًا. ما لم يحدث هو أن يصل إلى الخادم.**

---

## ٢ · القياس الذي يحسم الأمر

| الطلب | الإنتاج | staging |
|---|---|---|
| `GET /health/ready` | `200` · `{"status":"ok","database":true,"redis":true}` | `200` · نفسه |
| `GET /api/v1/system/diagnostics` | **`200` بمحتوى كامل** | **`500`** |
| `GET /api/v1/system/readiness` | **`503`** | **`503`** |

في الكود الحالي هذان المساران خلف `InternalAdminGuard`، والحارس يعمل **قبل** دالة المعالجة. فالجواب الوحيد الممكن لنداء بلا مفتاح هو **`401`**.

**لا `200` ولا `500` ولا `503` يمكن أن يخرج من مسار محروس.** كلها من داخل الدالة نفسها. إذن:

> **البيئتان — الإنتاج و staging — تُشغّلان بناءً سابقًا للإصلاح.**

---

## ٣ · والرقم الذي لا يقبل التأويل

```
"uptimeSeconds": 260621
```

**٧٢ ساعة و٢٣ دقيقة — ثلاثة أيام كاملة.** حاوية الإنتاج تعمل بلا انقطاع منذ نحو **١٨ أغسطس صباحًا**.

عملية Node تبدأ من الصفر مع كل نشر. فرقم كهذا يعني شيئًا واحدًا: **لم يُنشر شيء على الإنتاج منذ ثلاثة أيام.**

وقد بُنيت صورتان ناجحتان في هذه المدة:

| الوقت | بصمة الصورة |
|---|---|
| ٠٠:٤٥ | `sha256:ccc3d7b7…` |
| ٠٨:٢٢ | `sha256:ade8ed3e…` |

**صورتان مختلفتان، بُنيتا ودُفعتا، ولا واحدة منهما تعمل في أي مكان.**

فالمشكلة **لم تعد في البناء**. البناء انتهى وانتهى معه عطل السياق. المشكلة الآن في الخطوة التالية له: **الترقية إلى نشر حيّ.**

---

## ٤ · ما أطلبه منك — سجلّ واحد، لا سجلّ البناء

كل ما أرسلتَه حتى الآن هو **Build Logs**. وهي خضراء، وصدقها لا يعني شيئًا عن النشر.

> **Railway ← الخدمة ← آخر Deployment ← تبويب `Deploy Logs`** (لا `Build Logs`)

وهذا تحديدًا ما أبحث عنه فيه:

1. **مخرجات `preDeployCommand`** — أي `npx prisma migrate deploy`. إن فشلت، **تُلغى الترقية بالكامل ويبقى النشر القديم يخدم**، والشارة تبقى مطمئنة. وهذا هو المرشّح الأول عندي.
2. **نتيجة فحص الصحة** على `/health/ready` خلال ٣٠٠ ثانية. الفشل هنا يُنتج الأثر نفسه بالضبط.
3. **دورة إقلاع/انهيار** — متغيّر بيئة إلزامي ناقص يُسقط العملية قبل أن تستمع لأي طلب. الأربعة الإلزامية: `DATABASE_URL` و`REDIS_URL` و`JWT_ACCESS_SECRET` و`JWT_REFRESH_SECRET`.

وتحقّق أيضًا من **الفرع** الذي تتابعه الخدمة: `Settings ← Source ← Branch` يجب أن يكون `abny/sprint-f1-unblock`. خدمة تتابع `main` تبني وتنشر كودًا لم يرَ شيئًا مما رفعتَه — والصورة تُبنى بنجاح تام.

---

## ٥ · وفرق staging عن الإنتاج يستحق سطرين

الاثنان بناء قديم، لكن **الإنتاج يردّ `200` و staging يردّ `500` على المسار نفسه من الكود نفسه.**

الدالة التي تخدم هذا المسار تلمس قاعدة البيانات **مرة واحدة**: قراءة قائمة رايات الميزات. والإنتاج يردّ `featureFlags: []` — أي أن الجدول موجود وفارغ.

**فالمرشّح الأقوى لـ`500` على staging هو قاعدة بياناتها**: جدول ناقص، أو هجرات لم تكتمل. أقولها كترجيح لا كحقيقة — لأن جسم الرد لا يصلني.

**والمهم أن `‏/health/ready` يقول `ok` على الاثنين.** هو يسأل «هل تردّ Postgres على `SELECT 1`؟» — وقاعدة ينقصها نصف جداولها تجيب عن هذا السؤال بنفس البشاشة. وهذا بالضبط ما بُني حقل `schema` من أجله في تسليم الأمس: يقرأ `_prisma_migrations` فيقول كم هجرة اكتملت وأيّها الأخيرة. **لكنه في الكود الذي لم يُنشر بعد.**

---

## ٦ · ترتيب ما ينبغي فعله

| # | الفعل | لماذا الآن |
|---|---|---|
| ١ | افتح `Deploy Logs` وأرسل آخر ٥٠ سطرًا | هو الشيء الوحيد الذي لا أستطيع قياسه من الخارج |
| ٢ | اضبط `INTERNAL_ADMIN_API_KEY` على **الإنتاج** | بدونه يفشل الحارس مغلقًا بعد النشر، فتنغلق الشاشات في وجهك أنت أيضًا |
| ٣ | انشر الكود الحالي على **staging** أولًا | الإنتاج مكشوف الآن، لكن أول نشر يحمل ٢٩ هجرة على بيانات حيّة لا يُجرَّب على الإنتاج |
| ٤ | `deploy-doctor` على الاثنين | لا تصدّق «Deployed». صدّق `PASS build-identity` |
| ٥ | اضبط `GIT_COMMIT_SHA` كـbuild arg | `commit: null` يعني أن أي نشر غير منسوب إلى كوميت — وهذا ما جعل سؤال «أي بناء يعمل؟» يستغرق يومين |

---

## درجات الإثبات

| | |
|---|---|
| حالة الإنتاج و staging أعلاه | **RUNTIME VERIFIED** — ستّة طلبات حقيقية إلى المضيفين الحيّين، اليوم |
| «لم يُنشر شيء منذ ٣ أيام» | **RUNTIME VERIFIED** — `uptimeSeconds` من العملية نفسها |
| سبب `500` على staging | **ترجيح** — جسم الرد لا يصلني، والاستدلال مشروح في §٥ |
| سبب توقّف الترقية | **NOT TESTED** — يحتاج `Deploy Logs`، الخطوة ١ |
