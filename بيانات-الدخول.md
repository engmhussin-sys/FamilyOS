# بيانات الدخول

**لا يوجد حساب إنتاج، ولم أخترع واحدًا.** ما يلي حسابات **محلية** ينشئها السكربت على جهازك، ومفتاح مشغّل **تولّده أنت**. السكربت يرفض العمل على أي قاعدة بيانات ليست `localhost`، فهذه الحسابات لا يمكن أن تصل إلى بيئة حقيقية.

---

## ١. شغّل المنظومة وازرع البيانات

```bash
docker compose up -d                 # PostgreSQL + Redis
cd apps/backend
npx prisma migrate deploy            # ٢٩ هجرة → ١٠١ جدول
npm run seed:demo                    # ٣٠ أسرة · ٤٥ طفلًا · ٩٨ يومًا من التاريخ
npm run start:dev                    # المنفذ 3000
```

نافذة أخرى:

```bash
cd apps/admin-dashboard
npm run dev                          # المنفذ 5173
```

---

## ٢. الدخول إلى الداشبورد — حساب ولي أمر

الداشبورد يسجّل الدخول عبر `POST /auth/login` بحساب ولي أمر عادي.

| | |
|---|---|
| **البريد** | `demo.eg-01.parent1@demo-seed.invalid` |
| **كلمة المرور** | `DemoSeed!2026` |

الصيغة العامة: `demo.<slug>.parent1@demo-seed.invalid` — و`slug` يأخذ `eg-01` … `eg-15` (مصر) و`sa-01` … `sa-12` (السعودية). **كلمة المرور نفسها لكل الحسابات**، لأنها بلا قيمة خارج جهازك.

للاطلاع على كل الحسابات المزروعة:

```sql
SELECT email FROM users WHERE email LIKE '%@demo-seed.invalid';
```

---

## ٣. مفتاح المشغّل — تولّده أنت، ولا يأتي منّي

شاشات المنصّة (النمو، الاشتراكات، سجل قرارات الإشعارات) تقف خلف `InternalAdminGuard`. المفتاح **ليس** حساب دخول — هو سر مشترك بين خادمك وداشبوردك، ويجب أن تولّده على جهازك:

```bash
openssl rand -hex 32
```

ضع الناتج في `apps/backend/.env`:

```
INTERNAL_ADMIN_API_KEY=<الناتج>
```

ثم الصق **القيمة نفسها** في الداشبورد عندما يطلبها.

**يُحفَظ في الذاكرة فقط** — لا في `localStorage`، ولا في كوكي، ولا في الرابط، ويُمسح فور أي رفض من الخادم. لذلك ستعيد إدخاله بعد كل تحديث للصفحة، وهذا مقصود لا خلل: مفتاح المشغّل الذي ينجو من إغلاق التبويب هو مفتاح مسرَّب.

الخادم يقارن الطرفين بـ HMAC ثم `timingSafeEqual`، فلا يكشف طول المفتاح ولا موضع أول اختلاف.

---

## ٤. تطبيق الطفل — لا يوجد له حساب

الطفل لا يملك بريدًا ولا كلمة مرور، وهذا قرار تصميمي. الجهاز يُقرن:

1. من تطبيق ولي الأمر (أو `POST /pairing/invite`) اطلب دعوة للطفل.
2. أدخل الرمز في تطبيق الطفل → `POST /pairing/accept` ثم `device/register` ثم `verify`.
3. يوافق ولي الأمر → `POST /pairing/activate`.

الجهاز يحمل بعدها رمزًا خاصًا به. **لا يستطيع الطفل تعديل النقاط ولا المكافآت ولا حالة التحقق ولا موافقة ولي الأمر ولا هوية الأسرة** — كلها خادمية، ويوجد حارس اختبارات يرفض أي مسار يخالف ذلك.

---

## ٥. ما لا أستطيع إعطاءك إياه، ولن أختلقه

| البند | لماذا |
|---|---|
| Firebase | يحتاج مشروعًا باسمك. ضع `google-services.json` في `apps/parent-app/android/app/` — تطبيق الطفل لا يحتاجه |
| keystore | تولّده أنت؛ سطر `keytool` بالضبط في `signing.properties.example` |
| Paymob / Fawry | تعاقد تجاري ٤–٨ أسابيع |
| Play Console / App Store | حسابات باسمك |

---

## ٦. حساب قديم يجب تدويره

`test1@example.com` / `SecurePass123!` كانا مكتوبين داخل `scripts/verification-script*.ps1`. أزلتُهما وصارت السكربتات تأخذ `-BaseUrl` و`-Email` و`-Password` بلا قيم افتراضية — **لكنهما ما زالا في تاريخ Git.** دوّر ذلك الحساب قبل مشاركة المستودع.
