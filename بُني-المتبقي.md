# بُني المتبقّي

**٢٠٢٦-٠٨-٢١** · سبع شاشات مشغّل، وكلها خلف مفتاحك

---

## ما صار موجودًا الآن

| الشاشة | ما تفعله |
|---|---|
| `‎/platform/accounts` | سجلّ الحسابات + **تفاصيل الأسرة** + الترقية + **تعليق/استعادة** |
| `‎/platform/health` | **صحّة المنصّة** — البناء، والمخطّط، والاعتماديات |
| `‎/platform/plans` | **كتالوج الباقات** — تعريف وتحرير |
| `‎/platform/support` | **طابور الدعم** |
| `‎/growth/*` · `‎/notifications/decisions` | كما كانت |

كلها في القائمة الجانبية تحت «التشغيل»، وكلها خلف بوّابتين: جلستك ثم مفتاح المشغّل.

---

## ١ · كتالوج الباقات — أخطر ما كان ناقصًا

`plan_definitions` موصوف في المخطّط بأنه «يُزرع مرة، وقابل للتحرير إداريًّا لاحقًا». **ولا نصف من النصفين كان صحيحًا:** لا هجرة تزرعه، ولا شاشة تحرّره.

وأثره لم يكن تجميليًّا:

- `hasFeature` يسقط إلى باقة الأسرة، يبحث عنها هنا، لا يجدها، **فيجيب بلا** — أي أن **كل ميزة مدفوعة كانت مقفلة لكل أسرة**، بما فيها ما كان يُفترض أن تشمله الباقة **المجانية**، لأن قائمة مزاياها في هذا الجدول أيضًا
- و`grantForPlan` يمرّ على مزايا الباقة ليكتب الاستحقاقات؛ فبلا باقة **لا يمنح شراء حقيقي مُتحقَّق منه شيئًا**

**ولا زرعتُه بهجرة، عمدًا.** الزرع يحتاج أسعارًا، **وسعرٌ لم يخترْه أحد هو سعرٌ يدفعه عميل في النهاية.** الشاشة تبدأ فارغة وتقول لك ما يكلّفه فراغها. الأسماء والأسعار والمزايا لك.

**ولا يوجد حذف:** الاشتراكات القائمة تشير إلى الباقة، وحذف صفّها يتركها تشير إلى لا شيء. `isActive: false` يتقاعد الباقة من قائمة العملاء وكل إشارة إليها تبقى صالحة.

**وما تثبته الاختبارات ليس «كُتب صفّ»** بل: **عرّف باقة → تكتسب كل أسرة عليها ما تقوله الباقة بالضبط** — ثم تفقد ميزةً حين تُحرَّر الباقة لتُسقطها. مقيسًا عبر `hasFeature`، السلطة الوحيدة.

---

## ٢ · صحّة المنصّة — الشاشة التي كلّفنا غيابُها يومًا

`‎/system/readiness` و`‎/system/diagnostics` كانا يحملان الجواب طوال الوقت. لم يعرضهما شيء، فقضينا اليوم في سجلّات Railway.

**وأهمّ سطر فيها هو المخطّط** — ولا يستطيع أي فحص صحّة آخر أن يعطيه: `‎/health/ready` يسأل «هل تردّ Postgres على `SELECT 1`؟»، وقاعدة متأخّرة بثلاثين هجرة تجيب بنفس البشاشة. **وهكذا بالضبط عملت حاوية ثلاثة أيام على مخطّط لا يتوقّعه كودها.**

---

## ٣ · تفاصيل الأسرة + سجلّ النشاط

الصفّ يعطيك العدّادات؛ والشكوى تحتاج ما بعدها. الآن: الأعضاء وحالاتهم · الأطفال **بالاسم الأول والفئة العمرية** · الأجهزة وآخر نبضة لكل جهاز · الاشتراك · الاستحقاقات السارية · وآخر خمسين حركة في سجلّ التدقيق — **بما فيها ترقيات فريقك أنت.** لوحة تستطيع أن تتصرّف ولا تستطيع أن تُظهر ما فعلت، لا يُحاسَب عليها أحد.

**وتاريخ الميلاد غير موجود** — الاستعلام لا يقرأ العمود أصلًا. أسلم طريقة لعدم كشف حقل هي ألّا تقرأه.

---

## ٤ · تعليق واستعادة — والعطل الذي أمسكه الاختبار

الحالة كانت معروضة وغير قابلة للتغيير. الآن تُعلَّق وتُستعاد، مع سبب إلزامي وصفّ تدقيق.

**والاختبار أمسك عطلًا في تصميمي:** المستخدم المسجَّل حديثًا حالته `PENDING_VERIFICATION` لا `ACTIVE`. فـ«إعادة تفعيل» بمعنى «اضبطها على ACTIVE» كانت **ستُعلِّم بريدًا غير مُوثَّق بأنه مُوثَّق بضغطة دعم** — ولا شيء يبلّغ، ومسار التوثيق الذي يملك هذا الانتقال لا يعمل أصلًا.

فصارت الاستعادة **تُرجِع ما استبدله التعليق**، مقروءًا من صفّ التدقيق الذي كتبه التعليق نفسه؛ وإن لم يوجد صفّ **ترفض** بدل أن تخمّن. و`DELETED` نهائية وخارج المتناول: نقض قرار احتفاظ بالبيانات ليس لفتة دعم.

---

## ٥ · طابور الدعم — كان يعمل من طرفيه وغير مرئي

`POST /support` يستقبل منذ Sprint 8، و`GET /support` يُرجع خلف المفتاح — **ولا شيء يعرضهما.** فمن كتب إليكم كان يكتب إلى جدول.

الأولوية أولًا، و`isPriority` **محسوبة من الاستحقاقات وقت الإرسال** لا من العميل: تقول ما اشتراه لا ما يشعر به.

**ولا يوجد زرّ ردّ** — وهذا صدق لا نقص: لا نظام تذاكر ولا محادثات ولا مسار بريد صادر في هذا المنتج، وصندوق ردّ لا يؤلّف شيئًا أسوأ من غيابه. البريد معروض لتردّ من حيث تردّ فعلًا.

---

## ٦ · حالة التقارير — لم أبنِها، وهذا هو السبب

`ReportsService` يولّد تقرير الطفل **عند الطلب ولا يخزّن شيئًا.** لا يوجد جدول تقارير ولا تاريخ توليد.

فشاشة «حالة التقارير» ستعرض أرقامًا لا يحملها أي جدول. **حفظ سجلّ لكل توليد قرار منتج، لا شيء يُفبرك خلف رسم بياني.** قل لي إن أردته وأبنيه — لكنه إضافة إلى المخطّط، لا شاشة.

---

## ٧ · ماسورتان ضاغطتان أوقفتا البناء، وكلتاهما كانت محقّة

**حارس المستأجرين رفض `GET /system/accounts/:familyId`.** القاعدة كانت تسمح بالمعامل فقط حين يُقارَن بالتوكن — ومسار المشغّل لا توكن له يقارَن به، لأن **قراءة أسرة لا ينتمي إليها المتصل هي السؤال نفسه**. فصارت القاعدة تسمّي الحالتين المشروعتين، **وصار الاستثناء حقيقة مفحوصة**: يبقى صالحًا ما دام الملف يحوي `InternalAdminGuard` فعلًا. اختبرتُه بالطفرة: حذفتُ الحارس فسقط البناء.

**وسجلّ الجداول الخاملة رفض `PlanDefinition`.** كان مُعلَنًا «يُكتب بالهجرات فقط» بحجّة أن «باقة يسكّها التطبيق وقت التشغيل تغيير تسعير لم يراجعه أحد» — **ولا الهجرات كتبته**، وهذا بالضبط هو العطل الذي أقفل كل ميزة مدفوعة. صار له كاتب وقت تشغيل مُراجَع، فحُذف الإعلان كما يوجب ذلك السجلّ.

---

## درجات الإثبات

| | |
|---|---|
| كتالوج الباقات | **RUNTIME VERIFIED** — ٦ اختبارات، تُثبت اكتساب الأسرة للمزايا وفقدانها |
| سجلّ الحسابات + التفاصيل + التعليق | **RUNTIME VERIFIED** — ١٠ اختبارات، منها فرع الخصوصية وفرع الاستعادة |
| الترقية اليدوية | **RUNTIME VERIFIED** — ٩ اختبارات بالجدار المالي نفسه |
| الشاشات الأربع | **BUILD VERIFIED** — `tsc` + `vite build`، و١٩٧ اختبارًا في الداشبورد خضراء |
| حارس المستأجرين | **مُختبَر بالطفرة** — حذف الحارس يُسقط البناء |

**١٢٧٢ اختبارًا خضراء** في `system-diagnostics` و`billing` و`architecture` وتغطية حرّاس المتحكّمات · `tsc` نظيف · حارس النطاق ٠ · حارس الأحداث ٠.

---

**وما زال كل هذا معطّلًا حتى تضبط `VITE_API_BASE_URL` على الداشبورد و`INTERNAL_ADMIN_API_KEY` على الخادم.**
