# خطوات الرفع — ماذا تفعل الآن بالضبط

**٢٢ أغسطس ٢٠٢٦**

---

## ⚠️ أولًا: لا تنسخ ملفات «التغييرات فقط» فوق مشروعك

هذا مهم، ويوفّر عليك مشكلة كبيرة.

قِستُ الفجوة بين ما على GitHub وما بين يديك الآن:

| | فرع `main` على GitHub | العمل الجاهز |
|---|---|---|
| الـ commit | `bb1abfe` | `2596f17` |
| عدد الـ commits | **٩٩** | **٤٢٣** |
| عدد الملفات | ١٤٦٥ | ١٥٢٩ |

**مستودعك على GitHub متأخّر ٣٢٤ commit.** فلو نسخت ملف «التغييرات فقط»
(`ABNY-changed-files.zip` — ١٠١ ملفًا) فوق نسختك، ستحصل على مشروع مكسور: مئة ملف
جديد فوق قاعدة تنقصها سبرنتات كاملة.

**الملف الصحيح للنسخ هو `ABNY-FULL-PROJECT.zip`** — أو الأفضل منه، الطريقة أ أدناه.

---

## الطريقة أ — الموصى بها: ملف `.bundle` (٣ أوامر)

**لماذا هي الأفضل:** تنقل الـ ٤٢٣ commit بتاريخها كاملًا، **وتحذف الملفات القديمة
تلقائيًا**، ولا تترك أي بقايا. جرّبتُها فعليًا على نسخة محاكية لمستودعك — التفاصيل
في آخر هذا القسم.

### ١. نزّل `ABNY-commits.bundle` وضعه في مكان تعرفه

مثلًا `C:\Users\<اسمك>\Downloads\ABNY-commits.bundle`

### ٢. افتح سطر الأوامر من GitHub Desktop

من داخل GitHub Desktop:

> **Repository** ← **Open in Command Prompt** (أو **Open in Git Bash**)

إن لم يكن الخيار ظاهرًا: **File ← Options ← Integrations ← Shell** واختر واحدًا.

### ٣. الصق هذه الأوامر الثلاثة

```bash
git fetch "C:/Users/<اسمك>/Downloads/ABNY-commits.bundle" feat/w2-operations-surface:feat/w2-operations-surface

git checkout feat/w2-operations-surface

git push origin feat/w2-operations-surface
```

> **انتبه للمسار:** استخدم `/` وليس `\` داخل علامتَي التنصيص، حتى في Windows.

**هذا كل شيء.** لا نسخ ملفات، ولا حذف يدوي، ولا commit جديد — التاريخ كامل كما هو.

### ماذا سيحدث

بعد الأمر الأول والثاني مباشرة، سيُظهر GitHub Desktop أنك على فرع
`feat/w2-operations-surface`، والملفات على قرصك تحوّلت إلى ٤٢٣ commit. لن يظهر أي
تغيير غير محفوظ (`0 changed files`) — وهذه علامة النجاح.

### ما تحقّقتُ منه بنفسي

بنيتُ نسخة محاكية لمستودعك بالضبط (`bb1abfe` · ٩٩ commit · ١٤٦٥ ملفًا) وشغّلتُ
الأمرين عليها:

```
قبل :  bb1abfe   ٩٩ commit   ١٤٦٥ ملفًا
بعد :  2596f17  ٤٢٣ commit   ١٥٢٩ ملفًا
شجرة العمل نظيفة: 0 تغييرات
بقايا ملفات قديمة: 0
```

---

## الطريقة ب — بدون سطر أوامر (زر واحد أكثر عملًا)

استخدمها فقط إن تعذّر فتح سطر الأوامر. عيبها أنها **تُنتج commit واحدًا ضخمًا**
يبتلع ٣٢٤ commit، فيضيع تاريخ التغييرات — وهو ما يجعل تتبّع أي عطب لاحقًا أصعب.

### ١. أنشئ فرعًا في GitHub Desktop

**Current Branch ← New Branch** ← الاسم: `feat/w2-operations-surface`

### ٢. فُكّ `ABNY-FULL-PROJECT.zip` فوق مجلد المشروع

اختر **Replace / استبدال الكل** عندما يسأل Windows.

> **لا تفكّه في مجلد جديد.** المسارات داخل الملف نسبية لجذر المشروع، فيجب أن يقع
> `apps/` فوق `apps/` الموجود لديك.

### ٣. احذف ١١ ملفًا قديمًا — هذه الخطوة إلزامية

الـ zip **يضيف ويستبدل ولا يحذف**. هذه الملفات حُذفت من المشروع ولن تختفي وحدها:

```
_DELIVERY/DELETE.ps1
_DELIVERY/DELETE.sh
_DELIVERY/MANIFEST.txt
apps/admin-dashboard/tailwind.config.js
prisma-client-generated.zip
scripts/verify_doctor_parity.py
قاعدةجديدةللإنتاج.md
╪¡╪º┘ä╪⌐-╪º┘ä┘å╪┤╪▒.md
╪º╪¡╪░┘ü-╪º┘ä┘à┘ä┘ü╪º╪¬-╪º┘ä┘é╪»┘è┘à╪⌐.md
╪╣╪╖┘ä-╪º┘ä╪¿┘å╪º╪í.md
╪▒┘ü╪╣-╪º┘ä┘â┘ê╪».md
```

الخمسة الأخيرة أسماؤها عربية مشوّهة الترميز — تحذفها كما هي.

**`apps/admin-dashboard/tailwind.config.js` هو الأهم في القائمة**: Tailwind 4 يقرأ
إعداده من ملف CSS، ووجود ملف إعداد قديم بجانبه مصدر التباس.

### ٤. Commit ثم Push

اكتب في الوصف: `sprint F2 — operations surface, safety review, deploy fixes`
ثم **Commit to feat/w2-operations-surface** ثم **Publish branch**.

---

## بعد الرفع — بالترتيب

### ١. راقب CI

GitHub ← **Actions**. الوظائف الحاجبة الآن **تسعة** فحوص:

| الفحص | ما يمنعه |
|---|---|
| `Runtime dependency completeness` ← **جديد** | حزمة إنتاج في `devDependencies` — العطب الذي كان يمنع الحاوية من الإقلاع |
| `Tenant-scoping static guard` | SQL خام يلمس جدول مستأجر بلا نطاق |
| `Event-emission static guard` | كتابة حالة إنجاز بلا Outbox |
| `Dependency currency guard` | حزمة متأخّرة إصدارًا رئيسيًا |
| `Mobile toolchain currency` | Flutter/AGP/Gradle/Kotlin متأخّرة |
| `Ops script halves agree` | تباعد نصفَي سكربتات التشغيل |
| الاختبارات | ٢٤٦ حزمة · ٥٦٨٦ اختبارًا |
| `Docker Build` | **هذا أول بناء حقيقي للصورة** — عندي كان Docker Hub محجوبًا |
| لوحة الإدارة | ٢٣٥ اختبارًا + البناء |

### ٢. ادمج إلى `main`

من GitHub: **Compare & pull request** ← راجع ← **Merge**.

### ٣. انشر على Railway

كل التفاصيل في **`انشر-الآن.md`**: المتغيّرات المطلوبة، و`Root Directory` الذي
يجب أن يكون `apps/backend`، وأمر إنشاء أول مشغّل.

**تذكير واحد:** `GIT_COMMIT_SHA` اضبطه كمتغيّر خدمة — `railway up` لا يمرّر
build-args، وبدونه يُبلّغ التشخيص `null`.

### ٤. ابدأ بناء الموبايل — لا تؤجّله

```bash
gh workflow run build-apk.yml
```

موعد Google Play (`targetSdk 36`) بعد **تسعة أيام**، وترقية AGP 9 **لم تُبنَ**.
توقّع فشل التشغيل الأول على عمل حقيقي — هذا مقصود، وهو ما يكشف ما يحتاج تعديلًا.

---

## إن واجهت مشكلة

| ما تراه | ما تفعله |
|---|---|
| `fatal: '...' does not appear to be a git repository` | مسار الـ bundle خطأ. استخدم `/` لا `\`، وضعه بين علامتَي تنصيص |
| `error: pathspec ... did not match` | الأمر الأول لم ينجح. أعِد تشغيله وانظر رسالته |
| GitHub Desktop يُظهر ٢٠٠٠ ملف متغيّر بعد الطريقة أ | لم تُنفَّذ `git checkout`. نفّذ الأمر الثاني |
| `Updates were rejected` عند الـ push | شخص آخر دفع إلى نفس الفرع — أرسل لي الرسالة كما هي |
| `Cannot find module 'pg'` عند التشغيل | حزمة إنتاج عادت إلى `devDependencies` — شغّل `npm run ci:runtime-deps` |

---

## الملفات الثلاثة التي أرسلتها ولماذا

| الملف | متى تستخدمه |
|---|---|
| **`ABNY-commits.bundle`** | **الطريقة أ — هذا هو المطلوب** |
| `ABNY-FULL-PROJECT.zip` | الطريقة ب، أو لقراءة الكود خارج git |
| `ABNY-changed-files.zip` | **للمراجعة فقط** — لا تنسخه فوق مشروعك (اقرأ أعلى الصفحة) |
