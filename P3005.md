# P3005 — السبب مُسمّى الآن

**٢٠٢٦-٠٨-٢١ · سجلّ النشر**

---

## ما قاله السجلّ

```
2026-08-21T08:22:33  Starting Container
                     29 migrations found in prisma/migrations
                     Error: P3005
                     The database schema is not empty.
2026-08-21T08:22:35  Stopping Container
```

**ثانيتان.** الحاوية أقلعت، ونفّذت `preDeployCommand`، وفشلت، فتوقّفت.

وحين يفشل `preDeployCommand` **تُلغي Railway الترقية بأكملها ويبقى النشر السابق يخدم** — وهذا بالضبط ما قِسْتُه من الخارج قبل أن أرى هذا السجلّ: `uptimeSeconds` ثلاثة أيام، وصورتان مبنيتان لا تعمل واحدة منهما. **البناء لم يعد المشكلة منذ الأمس؛ المشكلة كانت هنا، على بُعد ثانيتين من الإقلاع.**

---

## ماذا يعني P3005 بالضبط

يصدر عن شرط واحد لا غير:

> القاعدة **فيها جداول**، و**جدول `_prisma_migrations` غائب أو فارغ**.

وهذا يقول شيئًا محدّدًا عن تاريخ قاعدتك: **لم تُطبَّق عليها ولا هجرة واحدة عبر Prisma Migrate قطّ.** جداولها جاءت من `prisma db push` أو من SQL نُفِّذ يدويًّا — كلاهما ينشئ الجداول بلا سجلّ.

فـPrisma تقف أمام قاعدة فيها جداول ولا تعرف **أي** الهجرات التسع والعشرين تمثّلها تلك الجداول. **فترفض بدل أن تخمّن. والرفض هو التصرّف الصحيح.**

> وهذا يفسّر أيضًا ما لاحظتُه أمس ورجّحتُه: `‏/system/diagnostics` يردّ `500` على staging و`200` على الإنتاج من الكود نفسه. الترجيح كان «قاعدة staging ناقصة»، وP3005 يؤكّد أن **لا واحدة من القاعدتين مرّت بهجرة واحدة عبر Prisma**.

---

## ⚠️ والحل الشائع فخّ

الإجابة التي ستجدها في كل مكان هي **baseline**: كتابة صفوف في `_prisma_migrations` تقول «هذه الهجرة مطبَّقة» **بلا تنفيذ SQL الخاص بها**.

**وهذه ادّعاء عن قاعدة حيّة، لا إجراء تقني محايد.**

فإن كانت القاعدة تنقصها أعمدة الهجرة `0030`، وبَصَّلْتَها على أنها مطبَّقة، فسيتخطّى `migrate deploy` تلك الهجرة **إلى الأبد**. يعمل التطبيق حينها بكود جديد على مخطّط قديم: **`200` على كل مسار لا يلمس العمود الناقص، و`500` على أول مسار يلمسه** — في الإنتاج، بعد أسابيع، بلا أي خيط يعود إلى اليوم الذي شغّل فيه أحدهم أمرًا.

فالسؤال ليس «كيف أُسكت P3005»، بل: **هل هذه القاعدة تطابق فعلًا مخطّط هذا الفرع؟**

---

## الأداة — تقيس قبل أن تدّعي

`scripts/db-baseline.ps1` يشغّل `prisma migrate diff` بين **القاعدة الحيّة** و`schema.prisma`:

| النتيجة | ما يفعله |
|---|---|
| **لا فارق** | البَصْل صِدْق. ينفّذه: `migrate resolve --applied` لكل هجرة |
| **أي فارق** | **يرفض.** يطبع SQL الناقص كاملًا ولا يكتب حرفًا |
| **الإشارتان تختلفان** | يرفض. رمز الخروج والنصّ المنبعث يُقرآن **مستقلَّين**، والاختلاف بينهما يعني قياسًا غامضًا |
| **`diff` نفسه فشل** | يرفض بمخرجاته. وهذا الفرع تحديدًا هو الذي كان سيُقرأ خطأً كـ«لا فارق» |

**ولا يكتب شيئًا بلا `-Apply`.** بدونه تشخيص محض يطبع الأوامر التي كان سينفّذها.

> **ثمانية اختبارات تقود هذه القرارات كلها** — بما فيها فروع الرفض الأربعة، التي لا يمكن إنتاجها عند الطلب أمام قاعدة حقيقية.
>
> **وقد أمسك الاختبار عطلًا حقيقيًّا في مسوّدتي الأولى:** `grep -v` يخرج بالرمز `1` حين **لا** يجد شيئًا — أي في حالة «لا فارق» بالذات — ومع `pipefail` كان السكربت يخرج فاشلًا على المدخل الوحيد الذي يعني «آمن»، ولا يكمل إلا حين **يوجد** انحراف. صامت، ومقلوب تمامًا. لولا الاختبار لسلّمتُه لك هكذا.

---

## الخطوات — والفرق بين البيئتين جوهري

### أولًا: احصل على رابط عام للقاعدة

`postgres.railway.internal` لا يُحلّ خارج Railway. من خدمة Postgres ← **Connect** ← انسخ **العنوان العام** (`…proxy.rlwy.net:PORT`).

```powershell
$env:DATABASE_URL = "<الرابط العام>"
```

> **افعل هذا لكل بيئة على حدة، وتأكّد أي قاعدة أنت متصل بها قبل كل أمر.** الرابط لا يُطبع في مخرجات السكربت — يخفي بيانات الاعتماد ويُظهر المضيف فقط. **ولا ترسله إليّ.**

### ثانيًا: شخّص — بلا كتابة

```powershell
.\scripts\db-baseline.ps1
```

### ثالثًا: تصرّف حسب النتيجة

| النتيجة | staging | production |
|---|---|---|
| **`SAFE TO BASELINE`** | `.\scripts\db-baseline.ps1 -Apply` ثم أعد النشر | نفسه — بلا خطر، فالمخطّط مطابق |
| **`BLOCKED … DOES NOT MATCH`** | **صفّر القاعدة** (أدناه) | **قف.** أرسل لي الانحراف المطبوع |

#### تصفير staging — إن ولا بيانات فيها تهمّك

```sql
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
```

> **يمحو كل شيء في تلك القاعدة.** نفّذه على **staging فقط**، وبعد أن تتأكّد مرتين من القاعدة التي أنت متصل بها.

ثم أعد النشر. `migrate deploy` يبني من الصفر: **٢٩ هجرة ← ١٠١ جدول**، وسجلًّا تكتبه Prisma بنفسها — فلا يتكرّر P3005 هنا أبدًا. وهذا المسار **مُثبت**: هو نفسه الذي تُبنى عليه ٥٤٩٠ اختبارًا عندي.

#### الإنتاج — لا تصفّر ولا تُبَصِّل

إن ظهر انحراف على الإنتاج فهو يحمل أسرًا حقيقية، ولا يُصفَّر. تحديد الهجرات الممثَّلة فعلًا في مخطّطه يحتاج مراجعة **هجرة هجرة**؛ عندها يُبَصَّل ما هو مطبَّق فقط، ويُنفَّذ الباقي حقيقةً. **أرسل لي مخرجات `BLOCKED` وأنا أتولّى التقسيم.**

---

## ورتّب النشر هكذا

**staging أولًا، ثم الإنتاج.** الإنتاج مكشوف الآن — لوحة المشغّل عليه مقروءة من الإنترنت — وهذا يدعو للاستعجال، لكن **أول نشر يحمل ٢٩ هجرة على بيانات حيّة لا يُجرَّب على الإنتاج.** جرّب السلسلة كاملة على staging، فإن أعطى الطبيب:

```
PASS  build-identity   the operator surface is CLOSED
PASS  schema-version   29 migrations applied, latest 0030_… — matches this repository exactly
DEPLOY VERIFIED
```

عندها كرّرها على الإنتاج بثقة مقيسة لا مأمولة.

---

## درجات الإثبات

| | |
|---|---|
| سبب توقّف الترقية | **مُثبت من سجلّ النشر** — P3005 بالاسم، وتوقّف الحاوية بعد ثانيتين |
| منطق `db-baseline.sh` | **RUNTIME VERIFIED** — ٨ اختبارات تشغّل السكربت نفسه، وأمسكت عطلًا في فرع «لا فارق» |
| `db-baseline.ps1` | **STATIC VERIFIED** — لا PowerShell في بيئتي. يحرسه فحص تكافؤ في CI يسقط إن فقد أحد النصفين رمز رفض |
| حالة قاعدتَيك فعلًا | **NOT TESTED** — الخطوة الثانية أعلاه هي التي تقيسها |
