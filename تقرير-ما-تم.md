# تقرير مفصّل — ما تم في هذا السبرنت

**«ابني» / ABNY — FamilyOS**
الفرع `feat/w2-operations-surface` · من `7445f2e` إلى `2596f17` · ١٦ commit
٢٢ أغسطس ٢٠٢٦

> هذا تقرير **ما بُني وما أُصلح**. تقرير المراجعة النقدية للعيوب السبعة عشر في
> `FINAL_REVIEW_REPORT.md`، وخطوات الرفع في `خطوات-الرفع.md`، وخطوات النشر في
> `انشر-الآن.md`.

---

## ١. الأرقام

| | |
|---|---|
| commits | ١٦ |
| ملفات معدّلة | ١٦٨ |
| أسطر مضافة / محذوفة | +١٦٬٦٨٦ / −٩٬٠٢٤ |
| ملفات جديدة | ٤٢ |
| migrations جديدة | ٣ (`0031`, `0032`, `0033`) |
| حزم اختبار | ٢٤٦ (كانت ٢٢٣) |
| اختبارات | **٥٦٨٦ ناجحًا · صفر متخطّى** |
| حرّاس CI حاجبة | ٩ (كانت ٦) |

---

## ٢. ما بُني — خمس قدرات لم تكن موجودة

### ٢-١ · لوحة العمليات (W2): اثنا عشر مسارًا كانت مبنيّة وغير مرئية

اثنا عشر مسار مشغّل كانت موجودة في الواجهة الخلفية **ولا يستدعيها شيء**. بُنيت لها
واجهة:

| الصفحة | ما تُظهره |
|---|---|
| `JobsPage` | المهام المجدولة وحالة كل تشغيل |
| `OutboxPage` | صندوق الأحداث الصادرة وإعادة المحاولة |
| `DeliveriesPage` | تسليم الإشعارات وأسباب الفشل |
| `AiUsagePage` | استهلاك الذكاء الاصطناعي وميزانيته |

ومعها مكوّنان مشتركان — `DataTable` و `ConfirmDialog` — و**كل إجراء مؤثّر يمرّ عبر
`ConfirmDialog`**: لا تشغيل مهمة ولا إعادة إرسال بضغطة واحدة.

**قيد أمني محفوظ:** المتصفّح لا يرى `INTERNAL_ADMIN_API_KEY` إطلاقًا، ولا يُخزَّن
في `localStorage` — يُدخَل في الذاكرة ويُعاد إدخاله بعد كل تحديث للصفحة، وهذا مقصود.

**تصحيح ذاتي:** نشرتُ في تقرير الاستكشاف ادّعاءً بأن `AiUsageLog.familyId` لا
يُكتب أبدًا وأن ميزانية الذكاء الاصطناعي تجمع المنصّة كلها. **كان خاطئًا.** الامتداد
(`Prisma Client Extension`) يختم `familyId` عند الكتابة، و`aggregate` ضمن عمليات
القراءة المُعترَضة، فالميزانية مفلترة فعلًا. صحّحتُه في commit مستقل (`9765142`)،
والسبب كان مخالفتي لقاعدتي: قرأتُ استدعاء `create` ولم أقرأ الامتداد الذي يعيد كتابته.

### ٢-٢ · حياة الجهاز: `HEARTBEAT_MISSED` لم يكن له منتج

الحالة موجودة في المخطّط منذ البداية، **ولا شيء في المشروع يكتبها**. أي أن جهاز طفل
يمكن أن ينقطع للأبد ولا يعرف أحد.

- `device-liveness.ts` — منطق خالص: `DEVICE_STALE_AFTER_HOURS = 24`
- `DeviceLivenessService` — يقرأ ويصنّف
- `device-liveness-sweep.job` — مهمة مجدولة تكتب الحالة فعلًا
- migration `0031`

### ٢-٣ · هوية المشغّلين: من يدير هذه المنصّة

**قبل:** إجابة واحدة بمقدار bit واحد — من يملك `INTERNAL_ADMIN_API_KEY` يفعل
**كل شيء**. موظف دعم يقرأ تذكرة يحمل نفس السر الذي يعدّل الأسعار ويوقف الأسر.
و**سحب صلاحية شخص واحد يعني تدوير المفتاح على الجميع.**

**بعد:**

| المكوّن | الدور |
|---|---|
| `permissions.ts` | مفردات مغلقة `<domain>.<verb>` — والقراءة صلاحية مستقلة دائمًا، لا تُشتقّ من الكتابة |
| `OperatorSessionService` | جلسات مبهمة (opaque) في Redis، مفتاحها SHA-256 للرمز، صلاحية ٨ ساعات، بلا تجديد |
| `OperatorAuthGuard` | بوابة ثانية **داخل** `InternalAdminGuard` — تفوّض إليه ثم تسأل «من أنت» و«هل يحقّ لك» |
| `OperatorAuthController` | دخول · خروج · مَن أنا · bootstrap يُغلق نفسه |
| `OperatorAdminController` | الدليل · الإنشاء · تغيير الدور والحالة |
| migration `0032` | جدول `operators` |

**لماذا جلسة في Redis وليست JWT؟** السبب هو عين المشكلة: **JWT لا يمكن سحبه.**
هو ادّعاء موقّع يظل صحيحًا حتى ينتهي. والعيب الذي بُني هذا العمل لإصلاحه هو أن
صلاحية شخص واحد لا تُسحب دون سحب الجميع. رمز عديم الحالة كان سيصلح التسمية ويترك
السحب مكسورًا كما هو. هنا: السحب = حذف مفتاح واحد، والطلب التالي مرفوض.

**وسبب ثانٍ:** الـ JWT الموجود **مصمَّم للأسر** — حمولته تحمل `familyId` و
`familyRole`، و`principalRoleFromToken` **يُرجع الدور المجهول إلى `PARENT`**. رمز
مشغّل يمرّ عبر تلك الآلية يبعد تزويرًا واحدًا عن أن يصير ولي أمر.

**الأدوار الأربعة:**

| الدور | يقرأ | يكتب | محتوى بلاغ السلامة |
|---|---|---|---|
| `SUPER_ADMIN` | كل شيء | كل شيء | ✔ |
| `SUPPORT` | الأسر · الأجهزة · **وجود** البلاغ | لا شيء | ✘ |
| `SAFETY` | الأسر · الأجهزة · البلاغات | مراجعة وتصعيد فقط | ✔ |
| `READ_ONLY` | كل قراءة | لا شيء | ✘ |

`SAFETY` **لا يحمل أي صلاحية مالية إطلاقًا**: من يقرأ ضيق طفل لا شأن له بأموال
الأسرة، وهذا الفصل هو ما يجعل صلاحية قراءة المحتوى قابلة للدفاع عنها أصلًا.

**الـ bootstrap:** أول مشغّل على نشر جديد مشكلة حقيقية — `create` تتطلب مشغّلًا
فاعلًا، ولا يوجد. رُفض حلّان شائعان: **كلمة سر مزروعة في migration** (كلمة سر تعيش
في المستودع وفي الإنتاج للأبد)، و**مسار «أنشئ مديرًا» دائم** (نقطة تصعيد صلاحيات
مفتوحة مدى حياة النشر). الحل: مسار **يرفض لحظة وجود أي صف مشغّل** — نافذته بين أول
migration وأول مشغّل، ويُغلق وحده وللأبد.

### ٢-٤ · مكتب السلامة: كل بلاغ ضيق أُنتج كان غير مُراجَع

`AlertStatus` له أربع قيم منذ إنشاء الجدول، و**قيمة واحدة فقط كانت قابلة للوصول**.
`reviewed_at` لم يكن له كاتب في المشروع كله. أي أن **كل إشارة ضيق أنتجها هذا المنتج
غير مُراجَعة**، وإنذار النمو الذي يعدّ الحرِجة غير المراجَعة **لا يستطيع إلا أن
يصعد**.

| المكوّن | الدور |
|---|---|
| `safety-review.ts` | جدول الانتقالات — ٩ قواعد، قيمة يمكن لاختبار تعدادها |
| `SafetyReviewService` | الطابور · قراءة بلاغ · النقل |
| `SafetyOperationsController` | ٤ مسارات · ٣ صلاحيات |
| migration `0033` | `ai_alert_notes` + سحب `UPDATE`/`DELETE` من `abny_app` |

**الخط الفاصل الذي لا يُخترق:** الطابور **لا يجلب** `title` ولا `description` —
قرار على مستوى الاستعلام لا على مستوى التحويل، لأن **أأمن طريقة لعدم كشف حقل هي
ألّا تقرأه**. والكلمات خلف صلاحية منفصلة (`safety.read_content`)، **وكل قراءة لها
تكتب صف تدقيق قبل أن تعود بالمحتوى** — إن فشلت الكتابة تفشل القراءة.

**ولا شيء هنا يحذف شيئًا.** لا مسار حذف، ولا أرشفة، ولا إزالة — وقاعدة البيانات
تفرضها كذلك: `0033` تسحب `UPDATE` و `DELETE` من دور التطبيق، فخدمة مستقبلية نسيت
القاعدة يرفضها Postgres لا مراجعة الكود.

### ٢-٥ · قاعدة الحداثة (C-6) — بحرّاس لا بوثيقة

قاعدتك: **أحدث الإصدارات، ولا إصدارات قديمة.** جُعلت قابلة للفحص:

- `assert-dependencies-current.ts` — ٦٠ حزمة npm؛ الاستثناء يجب أن يسمّي العائق
- `assert-mobile-toolchain-current.py` — الأربعة التي لا يراها npm
- `test_mobile_toolchain_guard.py` — ١٦ فحصًا يثبت أن الحارس **ما زال يقرأ** الملفات

وثلاث أدوات كانت تمنع TypeScript 7 — **استُبدلت الأدوات ولم تُؤخَّر السلسلة**:

| المحذوف | السبب | البديل |
|---|---|---|
| `ts-jest` | `peerDependencies.typescript: ">=4.3 <7"` | `@swc/jest` |
| `ts-node` | يستخدم واجهة مترجم لا يكشفها TS 7 | `tsx` |
| `@nestjs/cli` | TS 7 يشحن `tsc` فقط | `tsc -p` + `tsx watch` |

**الاستثناء الوحيد المعلَن:** Node **24 (Active LTS)** لا 26 (Current). الكتابة
مقابل بيئة تشغيل غير منشورة تجعل استدعاء API غير موجود يمرّ من الفحص — وهو الفشل
الذي تُوجد الأنواع لمنعه.

---

## ٣. ما أُصلح — سبعة عشر عيبًا

تفاصيلها في `FINAL_REVIEW_REPORT.md`. هنا الخلاصة:

### 🔴 سلامة الطفل (١)

**سباق يمحو تصعيدًا.** موظفان يفتحان نفس البلاغ الحرج، كلاهما يقرأ `NEW`، الأول
يصعّد والثاني يرفض — **آخر كتابة تفوز، والاثنان يُبلَّغان بالنجاح**.
الإصلاح: `AND status = ${from}` + transaction واحدة.
**مثبَت وقت التشغيل** مقابل PostgreSQL 16، **مع ضابط تحكّم سلبي**: بحذف الشرط تسقط
٣ من ٤ اختبارات.

### 🔴 الأمن والصلاحيات (٧)

| # | العيب |
|---|---|
| ٢ | `operators.manage` **بلا أي مسار** — سحب صلاحية شخص واحد لم يكن ممكنًا لأحد |
| ٣ | جلسة قد تنجو من السحب (فهرس JSON يفقد hash عند تسجيلَي دخول متزامنين) |
| ٤ | الإيقاف يُثبَّت والجلسة تبقى حيّة ٨ ساعات إن فشل قتلها |
| ٥ | `SAFETY` لا يستطيع فتح الكونسول أصلًا (401 من `/me`) |
| ٦ | مشغّل يستطيع إقفال الكونسول نهائيًا (إيقاف نفسه / آخر `SUPER_ADMIN`) |
| ٧ | سباق في bootstrap: بريدان مختلفان ⟵ `SUPER_ADMIN` اثنان |
| ٨ | التعديل وسجل التدقيق transaction منفصلتان — انهيار بينهما = إجراء بلا سجل |

### 🔴 النشر (٧)

| # | العيب |
|---|---|
| ٩ | **الحاوية لا تُقلع**: `@prisma/adapter-pg` و`prisma` في `devDependencies` |
| ١٠ | `dotenv` في `devDependencies` والـ CLI يستوردها |
| ١١ | الـ Dockerfile لا ينسخ `prisma.config.ts` — ومنه فقط تُقرأ سلسلة الاتصال |
| ١٢ | `predeploy.sh` يمرّر رايات `migrate diff` لم تعد Prisma 7 تقبلها |
| ١٣–١٥ | `predeploy-schema-probe.js` و`seed.ts` و`seed-demo.ts` و`grant-default-consents.ts` تبني العميل بطريقة محذوفة |

### 🟠 البنية التحتية للاختبار (٢) — وهي التي كانت تُخفي ما سبق

| # | العيب |
|---|---|
| ١٦ | **٥٢ حزمة اختبار تسرّب اتصال Postgres** — `$disconnect()` لا يغلق pool سلّمه المستدعي |
| ١٧ | **حارس العزل قابل للإرضاء بالتعليقات** — يبحث عن `family_id` في ٢٥ سطرًا حرفيًا |

---

## ٤. التغيير الأكبر: كيف صار التحقّق

هذا أهم ما في التقرير.

**قبل اليوم:** كل اختبارات التكامل كانت **تُتخطّى بصمت** لغياب
`INTEGRATION_DATABASE_URL` — ٣٤٧٠ اختبارًا «أخضر» و**٢٢٠٤ لم تُنفَّذ**، و٨٩ حزمة
لم تعمل قط.

**اليوم:** PostgreSQL 16 حقيقي · ٣٢ migration · Redis 7 · **٢٤٦/٢٤٦ حزمة ·
٥٦٨٦ اختبارًا · صفر متخطّى**.

**وأكثر:** لأن Docker Hub محجوب في بيئتي، أعدتُ إنتاج **مرحلة تشغيل الحاوية** على
القرص — نفس ملفات `COPY` بالضبط، `npm ci --omit=dev`، ثم أمر `CMD` — وشغّلتها.
**هكذا وُجد عطب الإقلاع.** ولا اختبار كان ليجده: الاختبارات تعمل على التثبيت الكامل،
والعطب يوجد فقط في صورة مبنيّة بـ `--omit=dev`.

### ثلاث «سقّاطات» اشتغلت فور توفّر قاعدة بيانات

- `cross-tenant-probe` وجد **أربعة مسارات `system/*` لا يغطيها شيء** (الفلتر كان يسمّي
  `InternalAdminGuard` وحده، والمسارات الجديدة تحمل `OperatorAuthGuard`)
- `tenant-rls.integration` — عدّاد جداول RLS: ٥٤ ← ٥٥، والسياسات ٥٦ ← ٥٧
- `ai-safety-alert.e2e` كان يتحقّق من عمود **حذفته** migration `0033`

---

## ٥. ترقية سلسلة أدوات Android

**السبب موعد نهائي، لا تفضيل:** Google Play تشترط `targetSdk 36` لكل تحديث من
**٣١ أغسطس ٢٠٢٦**.

| | قبل | بعد | المصدر (قُرئ ٢٢ أغسطس) |
|---|---|---|---|
| compileSdk / targetSdk | 34 | **36** | Play Console Help |
| Android Gradle Plugin | 8.1.1 | **9.3.0** | developer.android.com |
| Gradle | 8.3 | **9.7.1** | docs.gradle.org |
| Kotlin | 1.9.10 | **2.4.10** | kotlinlang.org |
| Flutter | 3.24.5 | **3.47.0** | docs.flutter.dev |

**لا رقم واحد كُتب من الذاكرة** (قاعدة C-5). واخترتُ **أصغر تغيير يحقّق الهدف**:
AGP 9 يفعّل «Kotlin المدمج» و DSL جديدًا، لكن Flutter توثّق حالة انتقالية
(`android.newDsl=false` و `android.builtInKotlin=false`) وهي ما اعتمدته — هجرة الـ
DSL تغيير مصدري **يجب بناؤه** ليعني شيئًا، وليس هو موضوع الموعد.

**`NOT TESTED`** — لا Flutter SDK ولا Android SDK هنا. `build-apk.yml` هو الفيصل،
و**توقّع فشل التشغيل الأول على عمل حقيقي**.

---

## ٦. مفردات الإثبات — بلا مبالغة

التزمتُ بمفرداتك: `CODE REVIEWED` · `STATIC VERIFIED` · `RUNTIME VERIFIED` ·
`BLOCKED` · `NOT TESTED`. **لم أكتب «جاهز للإنتاج» ولا «آمن بالكامل» ولا «١٠٠٪»
مرة واحدة.**

| ما هو | الحالة |
|---|---|
| ٢٤٦ حزمة / ٥٦٨٦ اختبارًا | `RUNTIME VERIFIED` — PostgreSQL 16 + Redis 7 |
| سباق مراجعة السلامة | `RUNTIME VERIFIED` + ضابط تحكّم سلبي |
| إقلاع صورة الإنتاج | `RUNTIME VERIFIED` |
| `/health/live` يبقى 200 مع سقوط Postgres | `RUNTIME VERIFIED` |
| سحب صلاحية مشغّل حيّ ⟵ 401 فورًا | `RUNTIME VERIFIED` |
| تدقيق قراءة محتوى بلاغ طفل | `RUNTIME VERIFIED` |
| `tsc` · البناء · ٩ حرّاس | `STATIC VERIFIED` |
| لوحة الإدارة (٢٣٥ اختبارًا + بناء) | `STATIC VERIFIED` |
| بناء صورة Docker | `BLOCKED` — Docker Hub يُجيب 403 |
| حداثة أدوات الموبايل | `BLOCKED` — المستودعات الأربعة تُجيب 403 |
| **ترقية Flutter / AGP** | **`NOT TESTED`** |
| `flutter pub get` | `BLOCKED` — pub.dev يُجيب 403 |

---

## ٧. ما لم أفعله — وهو قرارك

**١. RLS خامد فعليًا.** قِسته لا استنتجته: كل جدول مستأجر يحمل `tenant_isolation`
**و** `tenant_bypass_owner` الممنوحة للمالك، وPostgres يجمع السياسات المتساهلة
بـ OR، **والتطبيق يتصل بصفة المالك**. والدالة `withRls` **بلا موضع استدعاء واحد**.
العزل الحقيقي اليوم يقوم على Prisma Extension وحده.

ليس عيبًا أدخلتُه، وتفعيله قرار نشر (دور Postgres مقيّد + منح + كلمة سر في Railway)
وله كلفة أداء. أضفت ضبط `app.current_family_id` على مسار السلامة وحده.

**٢. أربعة وأربعون مسار مشغّل ما زالت خلف المفتاح المشترك وحده.** نقلها يُسجّل خروج
كل مشغّل من كونسول حيّ ويستحق نشرًا مستقلًا.

**٣. هجرة DSL الخاصة بـ AGP 9.** موثّقة بمصدرها في `gradle.properties` لكلا
التطبيقين.

**٤. قاعدة «آخر SUPER_ADMIN» غير قابلة للوصول عمليًا اليوم** — لأن قاعدة «لا تعدّل
نفسك» ترفض أولًا. كُتبت وسُجّل ذلك صراحةً في الكود: هي الحارس الذي يهمّ بعد تعديل
شخص آخر، ولذلك يُكتب **قبله**.

---

## ٨. الخطوة التالية

بالترتيب، وأولها الأعجل:

1. **`خطوات-الرفع.md`** ← ارفع الفرع (٣ أوامر)
2. راقب CI — **أول بناء حقيقي لصورة Docker** يحدث هناك
3. ادمج إلى `main` ثم انشر (`انشر-الآن.md`)
4. **`gh workflow run build-apk.yml`** — تسعة أيام على موعد Play
5. **Paymob / Fawry** — تعاقد ٤–٨ أسابيع، والبند الوحيد الذي يزداد سوءًا بالانتظار
6. قرار: تفعيل RLS أم إبقاؤه دفاعًا في العمق
