# مبروك 🎉

**«ابني» / ABNY — الخادم والداشبورد والتطبيقان جاهزون للتجربة.**

٢٨٤ كوميت · **٢٣١ مجموعة اختبار · ٥٤٨٤ اختبارًا · صفر إخفاق** على قاعدة بيانات بُنيت من الصفر.

---

## أولًا — الصراحة قبل الاحتفال

هناك **رابطان** في هذا الملف: واحد يعمل عندك خلال عشر دقائق، وواحد ينتظر خطوة واحدة منك أنت.

| | الحالة |
|---|---|
| **التجربة المحلية** | ✅ جاهزة الآن — الأوامر أدناه، وبيانات الدخول جاهزة |
| **رابط staging** | ⏳ ينتظر **اسم خدمة staging** فقط. بنيتك السحابية موجودة وحيّة — راجع القسم الثالث |
| **APK على هاتف حقيقي** | ⏳ ينتظر جهاز Windows. لا يحتاج Firebase ولا keystore ولا متجر |

**لم أخترع رابطًا ولا حسابًا ولا مفتاحًا.** الرابط الذي أعطيك إياه يعمل، والذي لا أملكه أقول إنه ينتظرك.

---

## ثانيًا — جرّب الآن، محليًا (١٠ دقائق)

```bash
docker compose up -d                 # PostgreSQL + Redis
cd apps/backend
npx prisma migrate deploy            # ٢٩ هجرة → ١٠١ جدول
npm run seed:demo                    # ٣٠ أسرة · ٤٥ طفلًا · ٩٨ يومًا من التاريخ
npm run start:dev                    # ← الخادم على 3000
```

نافذة أخرى:

```bash
cd apps/admin-dashboard
npm run dev                          # ← الداشبورد على 5173
```

### 🔗 رابط التجربة

**`http://localhost:5173`**

### 🔑 بيانات الدخول

| | |
|---|---|
| **البريد** | `demo.eg-01.parent1@demo-seed.invalid` |
| **كلمة المرور** | `DemoSeed!2026` |

الصيغة العامة `demo.<slug>.parent1@demo-seed.invalid`، و`slug` من `eg-01` … `eg-15` (مصر) و`sa-01` … `sa-12` (السعودية). كلمة المرور نفسها للجميع لأنها بلا قيمة خارج جهازك، والسكربت **يرفض** العمل على أي قاعدة بيانات ليست محلية.

### 🗝️ مفتاح المشغّل — تولّده أنت

```bash
openssl rand -hex 32
```

ضعه في `apps/backend/.env` باسم `INTERNAL_ADMIN_API_KEY`، والصق القيمة نفسها في الداشبورد عندما يطلبها. يُحفظ في الذاكرة فقط — لا في `localStorage` ولا كوكي ولا في الرابط — فستعيد إدخاله بعد كل تحديث للصفحة، وهذا مقصود: مفتاح مشغّل ينجو من إغلاق التبويب هو مفتاح مسرَّب.

الأرقام التي ستراها **مرّت عبر الخدمات الحقيقية والـ outbox** — ليست صفوفًا مزروعة بالقوة، فهي تحترم نفس القيود التي يحترمها الإنتاج. وما لا يمكن قياسه يظهر «غير مُقاس»، ولا يظهر أبدًا كصفر.

---

## ثالثًا — رابط staging: ينقصه اسم واحد

**تصحيح:** كنتُ قد كتبتُ أن النشر ينتظر حسابًا سحابيًا تنشئه. **كان ذلك خطأً.** بحثتُ في تاريخ Git فوجدتُ بيئتك، وتحققتُ منها حيّةً عبر HTTPS يوم ٢٠٢٦-٠٨-٢٠:

```
https://familyos-production-74ca.up.railway.app
  /health/live   →  {"status":"ok"}
  /health/ready  →  {"status":"ok","database":true,"redis":true}
```

بنيتك موجودة وتعمل: Railway، وPostgreSQL وRedis موصولان. **وقد أكّدتَ أن هذا المضيف هو production، فلن أقترب منه.**

ما ينقص هو **اسم خدمة staging ورابطها** لا أكثر. ضعهما في GitHub — `RAILWAY_SERVICE_NAME` و`STAGING_HEALTH_URL` كـvariables و`RAILWAY_TOKEN` كـsecret — والتفاصيل في `DEPLOY_STAGING.md`.

```
رابط staging: ______________________________
```

**وضعتُ قفلًا صلبًا:** workflow النشر يرفض العمل — قبل أي بناء وقبل أي هجرة — إن كان الهدف هو production. يُطبّع الرابط (حالة الأحرف، الشرطة المائلة الأخيرة، `:443`، النقطة الأخيرة)، ويقارن المضيف والتسمية الأولى واسم الخدمة، ويشترط أن يتفق الاثنان على هدف واحد. أربع محاولات لخداعه — متغيّر فارغ، شرطة أخيرة، أحرف كبيرة، إعادة تسمية الخدمة — كلها تُرفَض. وللنشر على production يومًا ما يوجد مخرج **متعمَّد الإزعاج**: عبارة تُكتب بالكامل يدويًا، ولا يمكن أن تُنفَّذ صامتة.

**Railway هو الاختيار الصحيح أصلًا** لسبب محدد: هجرة `0004` تنفّذ `CREATE ROLE abny_app`، ومستخدم قاعدة مُقيَّد لدى مزوّدين آخرين لا يستطيع ذلك فيتوقف النشر عندها. **ولا يوجد لدى أي مزوّد منطقة في مصر أو الخليج** — أقرب المتاح أمستردام أو فرانكفورت، وزمن الاستجابة الحقيقي **لم يُقَس**.

### ⚠️ وأمر يخصّ production الآن

`GET /api/v1/system/diagnostics` على مضيفك الحيّ **يرد بلا مصادقة** ويكشف الإصدار والبيئة ووقت التشغيل وأسماء أربعة مفاتيح اعتماد ناقصة. أُغلق هذا في الكوميت `06f0a5a` هنا، **لكن البناء المنشور متأخر بـ٢٨٥ كوميت فالثغرة مفتوحة عندك الآن.** واخترتَ إغلاقها مع أول نشر — وهو الصحيح، إذ يغلق معها بقية ما أُصلح.

---

## رابعًا — أول APK: لا ينتظر شيئًا خارجيًا

على جهاز Windows، ثمانِ خطوات في `WINDOWS_RUN.md`. أول APK تجريبي **لا يحتاج Firebase ولا keystore ولا Play Console**. الأمر الحاسم:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\release-doctor.ps1 -Profile debug
```

وتسعة بنود فقط قد تحتاج تثبيتًا، لكل واحد إصداره ومصدره وأمر تثبيته وأمر التحقق منه — الجدول في `WINDOWS_RUN.md` الخطوة ٤.

---

## خامسًا — ما الذي بُني فعلًا

| | |
|---|---|
| الخادم | ٢٣١ مجموعة · ٥٤٨٤ اختبارًا · صفر إخفاق، على قاعدة من الصفر (٢٩ هجرة → ١٠١ جدول)، Redis حقيقي، وتطبيق HTTP مُشغَّل فعليًا |
| قاعدة البيانات | عزل بين الأسر بحارس يفحص كل مسار · صفر مخالفات |
| سلامة الطفل | البايتات المفحوصة هي البايتات المُسلَّمة · الفشل مغلق عند عمر أو فئة مجهولة · **الرفض لا يكتب شيئًا** — ٩٠ جدولًا مفحوصة وRedis كاملة |
| الإشعارات الذكية | كل مفتاح له منتج واحد وجمهور ونسخة عربية ومقصد يجيبه التطبيق فعلًا |
| المكافآت | حدث واحد = منحة واحدة = دفعة واحدة، مُثبت بإعادة التشغيل وبحذف العلامة حتى ترفض قاعدة البيانات نفسها |
| الداشبورد | ١٩٧ اختبارًا · البناء نظيف |
| التطبيقان | ٩ فاحصات ثابتة · صفر مشاكل |

---

## سادسًا — ما لا أدّعيه

**لم يُصرَّف أي كود Dart أو Kotlin أو Gradle في هذه البيئة، ولم يُنفَّذ أي اختبار Dart.** كل نتائج الموبايل ثابتة. لم تُبنَ صورة Docker ولا نُشرت staging. صناديق Apple وGoogle وPaymob وFawry غير مُتحقَّقة. الإشعارات على جهاز حقيقي غير مُختبرة. **ولا توجد نسبة جاهزية في هذا الملف ولا في أي ملف آخر** — الأرقام مقيسة أو غائبة.

---

## سابعًا — أول خطوة بعد هذا الملف

شغّل الأوامر في القسم الثاني، وافتح `http://localhost:5173`، وادخل بالحساب أعلاه. عشر دقائق، ولا تحتاج فيها شيئًا من أحد.

وابدأ تعاقد **Paymob / Fawry اليوم** — أربعة إلى ثمانية أسابيع، وهو البند الوحيد في المشروع كله الذي يزداد سوءًا بمجرد الانتظار.

**مبروك — المشروع جاهز للتجربة.**
