# ABNY — تقرير مدير المشروع

٢٠٢٦-٠٨-٢٠ · فرع `abny/sprint-f1-unblock` · **٢٨٤ كوميت** · الشجرة نظيفة

---

## ١. الحالة المقيسة

| | |
|---|---|
| الخادم | **٢٣١ مجموعة · ٥٤٨٤ اختبارًا · صفر إخفاق** — قاعدة بيانات بُنيت من الصفر بـ٢٩ هجرة (0001–0030، بلا 0012) → ١٠١ جدول، Redis حقيقي، تطبيق HTTP مُشغَّل. `RUNTIME VERIFIED` |
| `tsc --noEmit` | ناجح |
| حارس العزل · حارس الأحداث | صفر · صفر |
| الداشبورد | ١٩٧ اختبارًا · `vite build` نظيف. `BUILD VERIFIED` |
| التطبيقان | ٩ فاحصات ثابتة · صفر مشاكل. **لم يُصرَّف Dart قط** — `STATIC VERIFIED` فقط |

الخادم انتقل من ٢٢٣/٥٣٨٣ إلى ٢٣١/٥٤٨٤ في هذه الجولة: **+٨ مجموعات و+١٠١ اختبار**، كلها إثباتات لعيوب أُغلقت.

---

## ٢. مراجعة الازدواج — ما وجدناه

طرحتُ على الفريق سؤالًا واحدًا: **هل كُتب شيء مرتين؟** لا «هل يوجد تكرار» — كل كود يكرّر قليلًا — بل: هل هناك **تنفيذان لمفهوم واحد**، يمكن أن ينحرفا فيعطيا إجابتين مختلفتين للسؤال نفسه.

فريقان عملا بالتوازي على الخادم والواجهات. النتيجة: **١٨ ازدواجًا حقيقيًا**، منها **١٢ أُغلقت في هذه الجولة**.

### الخادم

| # | ما كان | الأثر على أسرة حقيقية |
|---|---|---|
| **B1** | خدمتان تجيبان «هل هذه الأسرة مستحقّة؟» بمجموعتَي حالات مختلفتين | أسرة **دفعت** وفشل تجديد بطاقتها تُرفَض من إضافة طفل ومن جهاز ثانٍ ومن الدعم ومن الرؤى — في نافذة السماح المبنية أصلًا لمنع ذلك. وبالعكس: أسرة **استُرجع مالها** تحتفظ بكل شيء |
| **B2** | مفتاحا idempotency مختلفان لسلسلة واحدة | **الطفل يُدفع له مرتين** عن سلسلة سبعة أيام واحدة — نفس عيب الهجرة 0030 عائدًا من باب لا يراه الحارس |
| **B3** | **ثلاث** إجابات لسؤال «كم عمر هذا الطفل؟» | يوم ميلاد الطفل التاسع: هدف الماء ١٧٠٠ مل أو ٢١٠٠ حسب من أجاب. شاشة التقدّم تقول «تم» بينما التنبيه ما زال ينبّه، والمكافأة تُدفع عند الخط الخطأ |
| **B4** | جدول `habit_completions` يُكتب بطريقتين، انحرفتا بسطر واحد | جهاز غير متصل يزامن إنجاز الأمس: **المكافأة تُدفع والسلسلة تنكسر** |
| **B5** | سلّم السلاسل `[3,7,14,30,60,100]` مقابل `[3,7,14,30]` | سلسلة ماء ١٠٠ يوم لا تعطي شيئًا، ونفس الطول في العادات يدفع عملات |
| **B6** | `GET /system/diagnostics` و`/system/readiness` **بلا مصادقة** | يُسمّيان البناء لأي زائر: الإصدار، الـcommit، `NODE_ENV`، الرايات، ونصوص أخطاء التبعيات، وأي مزوّدي دفع مُعدّون |
| **B7** | فترة السماح تكتب `grace_period_ends_at` ولا تمدّ صفوف الاستحقاق | وعد الأيام السبعة **لا يُسلَّم** لأسرة لَحِقَ صفوفُها انتهاءُ الدورة |

### الواجهات

| # | ما كان | الأثر |
|---|---|---|
| **F1** | تبويب المكافآت يجمع الدقائق على **ساعة الجهاز**، والخادم يحسبها | نفس الأسرة، نفس الثانية: تبويب يقول «٤٥ دقيقة» وتبويب آخر وشاشة الطفل تقول «١٥» |
| **F2** | شاشة الطفل تمنع الطلب عند نقص العملات | الخادم **يسمح** به عمدًا ليصل إلى ولي الأمر — والطفل يرى زرًا ميتًا |
| **F3** | نسخة ثالثة من «أي طفل؟» | نُقلت إلى `ChildPicker` الموجود |
| **F4** | تطبيق الطفل **بلا أي مهلة HTTP** | رفع الدليل على اتصال نصف مفتوح **يعلّق للأبد** بلا خطأ ولا مخرج — والإصلاح كان موجودًا في تطبيق ولي الأمر |
| **F5** | نموذجان لصف قاعدة بيانات واحد داخل تطبيق واحد | |
| **F6** | نفس الرصيد باسمين: «نقطة» و«نقاط خبرة» | يُقرأ كاقتصادين |
| **F8** | `AsyncState` لم يغادر مجلده؛ **ثلاث من أربع بطاقات بلا فرع خطأ** | فشل الجلب يبدو كـ«لا توجد بيانات» — مفاتيح الموافقة تظهر «غير ممنوحة» عند فشل القراءة |
| **F9** | عرضان للمال في لوحة واحدة | صفحات النمو بأرقام عربية وعملة بموضعها، والفوترة `179.00 EGP` |

**بقيت مفتوحة عمدًا:** ازدواج مقصود بين التطبيقين (لا حزمة مشتركة بينهما) وجدول تدهور الروابط في تطبيق الطفل — كلاهما مُعلَّم بكاشف انحراف يسقط إن اختلف الطرفان، لا بتعليق يَعِد.

---

## ٣. الأدوات التي اخترتها، ولماذا

| الطبقة | الاختيار | السبب |
|---|---|---|
| **الاستضافة** | **Railway** (`railway.json`) | الهجرة `0004` تنفّذ `CREATE ROLE abny_app`؛ مستخدم قاعدة مُقيَّد لا يستطيع ذلك فيتوقف النشر عندها. Railway يعطي الصلاحية، ومعه Redis في نفس المشروع، وكل النشر أربعة ملفات فينتقل لفريق ops لاحقًا بلا احتجاز |
| **البديل** | **Render** (`render.yaml`، كامل) | حتى لا تكون محتجزًا عند مزوّد واحد |
| **مرفوض** | Fly.io | PostgreSQL غير مُدار، و«التصفير عند الخمول» يوقف مُرحِّل الـOutbox |
| **CI/CD** | GitHub Actions (`deploy-staging.yml`) | يستدعي بوابات `ci.yml` بدل نسخها — لا تُصان مرتين — ويرفض النشر إن سقطت |
| **الصورة** | Docker متعدد المراحل | أُصلح فيها عيبان حقيقيان: مرحلة التشغيل كانت تستدعي `prisma generate` بعد حذف الـCLI؛ ونقطة الدخول بلا `exec` فكان `sh` هو PID 1 ولا يصل SIGTERM إلى node، فلا يعمل `enableShutdownHooks()` |
| **الهجرات** | خطوة ما قبل النشر من نفس الصورة | لا داخل نقطة الدخول: نسخة خاسرة قد تخدم على مخطط نصف مطبَّق، وهجرة سيئة تدخل حلقة انهيار بعد اختفاء النسخة السليمة |
| **الصحة** | `/health/live` + `/health/ready` | بلا مصادقة، ولا يكشفان الإصدار ولا البيئة — وهذا مؤكَّد على الأجسام الحقيقية مفتاحًا مفتاحًا |
| **الأسرار** | `.env.staging.example` بـ٤٥ متغيّرًا | كلٌّ معلَّم «يوفّره المشغّل» أو «قيمة آمنة»، مقروءة من مُتحقِّقات الإقلاع لا مخمَّنة. **لا سرّ واحد في المستودع** |
| **الأخطاء** | Sentry | تبعية موجودة سلفًا؛ الـDSN اختياري ومعلَّم |

---

## ٤. كيف أدرتُ الفريق

سبعة فرق بالتوازي، **بملكية ملفات غير متقاطعة** — وهذا شرط لا تحسين: عندما تعمل عدة فرق على فرع واحد، `git add -A` يبتلع عمل غيرك و`git stash` يمحوه. حدث الاثنان في هذا المشروع، فصار المنع بندًا في كل تكليف بعدها.

القواعد التي فرضتُها على كل مكلَّف: **لا تُصلح اختبارًا ساقطًا بحذفه أو إضعافه** · **أثبت العيب بالتشغيل قبل إصلاحه، فإن لم يظهر فقل ذلك وتوقف** · **لا تدّعِ تحققًا زمنيًا إن كان تحليلًا ثابتًا** · **لا تخترع بيانات اعتماد** · **لا نسب جاهزية** · **ما لا تملكه تُبلّغ عنه بالملف والسطر والتغيير المطلوب، ولا تمدّ يدك إليه**.

آخر بند هو ما جعل هذه الجولة تعمل: خمسة تسليمات دقيقة انتقلت بين الفرق بلا تصادم واحد، وأغلقتُ الثلاثة الأخيرة بنفسي.

---

## ٥. ما بقي — وكلّه خارج الكود

| البند | المالك | الحالة |
|---|---|---|
| `git push` | أنت | البروكسي يرفض هذا المستودع تحديدًا. الحزمة تحمل التاريخ كاملًا |
| حساب Railway + رابط staging | أنت | كل الملفات جاهزة؛ ينقص الحساب |
| جهاز Windows + أول APK | أنت | لا يحتاج Firebase ولا keystore |
| Firebase · keystore · Play Console | أنت | لم يُختلق منها شيء |
| Paymob / Fawry | أنت | ٤–٨ أسابيع. **البند الوحيد الذي يزداد سوءًا بالانتظار وحده** |
| FCM | Claude #1 | مُفوَّض؛ العقد من جهتنا مكتمل |
| حساب `test1@example.com` | أنت | أُزيل من الملفات، وما زال في تاريخ Git. **دوّره** |

وفي `POST_PILOT_BACKLOG.md` عشرة بنود مكتوبة ومؤجَّلة عمدًا، ولا واحد منها من الفئات الستّ التي يُسمح لها بمقاطعة البناء.

---

## ٦. ما لا يُدَّعى

**لم يُصرَّف Dart ولا Kotlin ولا Gradle في هذه البيئة، ولم يُنفَّذ اختبار Dart واحد.** لم تُبنَ صورة Docker. لم تُنشر staging ولم يقرأ أي مزوّد ملف إعداده. زمن الاستجابة من مصر والسعودية غير مقيس. صناديق Apple وGoogle وPaymob وFawry غير مُتحقَّقة. الإشعارات على جهاز حقيقي غير مُختبرة.

**ولا توجد نسبة جاهزية في هذا التقرير** — رقم مقيس، أو غياب معلن.
