# التقرير النهائي — مراجعة كاملة لعمل هذا السبرنت

**«ابني» / ABNY — FamilyOS** · الفرع `feat/w2-operations-surface` · النطاق `7445f2e..18c332e`
التاريخ: 22 أغسطس 2026

---

## 1. الإجابة المباشرة على سؤالك

سألتَ: **«هل تم مراجعة الكل ام تحب ان تراجع الكل وتعطى تقرير نهائى»**.

جوابي وقتها كان: **لا، لم يُراجَع الكل.** الآن راجعتُ الكل، وهذا التقرير هو النتيجة.

والأهم: **المراجعة لم تكن شكلية.** وجدت **أربعة عشر عيبًا**، منها **ستة تمنع الإطلاق**،
وواحد منها كان يمكن أن يمحو تصعيد بلاغ سلامة طفل. كلها أُصلحت، وكلها مُختبَرة الآن
مقابل PostgreSQL 16 و Redis 7 حقيقيين — لا محاكاة.

---

## 2. كيف راجعت — وما الذي تغيّر في طريقة التحقق

الفرق الجوهري عن كل تقرير سابق في هذا السبرنت:

> **شغّلتُ قاعدة بيانات حقيقية.**

قبل اليوم، كل اختبارات التكامل (integration / e2e) كانت **تُتخطّى بصمت** لأن
`INTEGRATION_DATABASE_URL` غير موجود. النتيجة: 3462 اختبارًا «أخضر» بينما **89 حزمة
اختبار لم تُنفَّذ إطلاقًا**.

اليوم: شغّلت PostgreSQL 16 محليًا، طبّقت الـ 32 migration، شغّلت Redis، وأعدت تشغيل كل شيء.

| | قبل | بعد |
|---|---|---|
| حزم الاختبار المُنفَّذة | 156 من 245 | **246 من 246** |
| الاختبارات | 3470 ناجح · 2204 متخطّى | **5686 ناجح · 0 متخطّى** |
| قاعدة البيانات | لا شيء | PostgreSQL 16 حقيقي |
| Redis | لا شيء | Redis 7 حقيقي |

**الـ 2204 اختبار المتخطّى هي المكان الذي كانت تختبئ فيه ثلاثة عيوب.** لم يكن ممكنًا
العثور عليها بالقراءة.

> **ملاحظة عملية لك:** إن كان CI عندك لا يضبط `INTEGRATION_DATABASE_URL`، فهو يقيس
> ثلثي المشروع فقط. الملف `.github/workflows/ci.yml` يضبطها بالفعل — لكن أي بيئة أخرى
> (جهاز مطوّر، سبرنت سابق) كانت تعطي طمأنينة كاذبة.

---

## 3. ما وجدته — مرتّبًا بالخطورة

### 🔴 حرج — سلامة الطفل

**١. سباق في مراجعة البلاغات: رفضٌ يمحو تصعيدًا، والاثنان يُبلَّغان بالنجاح.**

`SafetyReviewService.transition` كانت تقرأ حالة البلاغ، تتحقق من شرعية الانتقال، ثم تكتب:

```sql
UPDATE ai_alerts SET status = ... WHERE id = $1
```

بدون `AND status = <الحالة التي قرأناها للتو>`، وبدون قفل، وبدون transaction.

**السيناريو الواقعي:** موظفان في مكتب السلامة يفتحان نفس البلاغ الحرج. كلاهما يقرأ `NEW`.
الأول يصعّد (`ESCALATED`)، والثاني — بعد جزء من الثانية — يرفض (`DISMISSED`).
**آخر كتابة تفوز.** التصعيد اختفى. **وكلا الموظفين رأى رسالة نجاح.**

على منتج للأطفال، هذا ليس سباقًا يُؤجَّل. هذا بالضبط الفشل الذي بُني نظام المراجعة كله لمنعه.

**الإصلاح:** compare-and-set (`AND status = ${from}`) + transaction واحدة تضم النقل
والملاحظة وسجل التدقيق. الخاسر يحصل على `409 ALERT_MOVED_CONCURRENTLY` يسمّي الحالة
التي اختفت — لا نجاح صامت، ولا 500.

**الدليل — `RUNTIME VERIFIED`، وليس استنتاجًا:**
ملف اختبار جديد يشغّل الخدمة الحقيقية مقابل PostgreSQL 16 ويثبت:

- فائز واحد من اثنين متزامنين ✓
- فائز واحد من عشرة متزامنين ✓
- الخاسر لا يكتب **شيئًا** — لا ملاحظة، لا سجل تدقيق ✓
- النقل المتتابع العادي ما زال يعمل (الإصلاح ليس تجميدًا) ✓

**وشغّلت ضابط تحكّم سلبي (negative control):** حذفت شرط `AND status` مؤقتًا — سقطت
ثلاثة من الاختبارات الأربعة فورًا. أي أن الاختبار يقيس الإصلاح فعلًا، ولا ينجح لسبب آخر.

---

### 🔴 حرج — الأمن والصلاحيات

**٢. القدرة الوحيدة التي بُني السبرنت من أجلها لم تكن قابلة للتنفيذ إطلاقًا.**

الصلاحية `operators.manage` كانت موجودة في المصفوفة، ممنوحة لـ `SUPER_ADMIN`،
و**لا يوجد أي مسار HTTP في التطبيق يطلبها**. الدالتان `OperatorService.create`
و `.update` مكتوبتان ومُختبرتان و**لا يمكن الوصول إليهما من أي مكان**.

بعبارة أوضح: **سحب صلاحية شخص واحد دون سحب صلاحية الجميع** — وهو الهدف الكامل من بناء
نظام هوية للمشغّلين — **لم يكن ممكنًا لأي أحد عبر الـ API**.

لماذا لم يكتشفه أي اختبار؟ لأن صلاحية بلا مسار هي بالضبط الشيء الذي لا سبب لدى أي حزمة
اختبار كي تلاحظه.

**الإصلاح:** `OperatorAdminController` — دليل الموظفين خلف `operators.read`،
والإنشاء وتغيير الدور/الإيقاف/السحب خلف `operators.manage`.

**٣. جلسة قد تنجو من السحب.**

الفهرس العكسي للجلسات كان مصفوفة JSON تُقرأ وتُعدَّل وتُكتب في كل تسجيل دخول.
تسجيلا دخول متزامنان ⟵ يضيع hash ⟵ **جلسة حيّة لا يستطيع `revokeAll` رؤيتها**.
أي: عملية سحب صلاحية **تُبلّغ بالنجاح والشخص ما زال داخل الكونسول**.

**الإصلاح:** Redis SET (`SADD` لا يفقد كتابة متزامنة) + سحب ذرّي في خطوة واحدة على الخادم.
اختبار جديد: عشرة تسجيلات دخول متزامنة ⟵ `revokeAll` يقتل العشرة.

**٤. الإيقاف قد يُثبَّت في قاعدة البيانات بينما يفشل قتل الجلسة.**

Redis لا تنضم إلى transaction في Postgres، فأحد الطرفين قد يفشل بعد نجاح الآخر.
كان الترتيب: حدّث الصف، ثم اقتل الجلسات — أي أن فشل القتل يترك مشغّلًا **مسحوبة صلاحيته
ومعه كونسول يعمل لثماني ساعات**.

**الإصلاح:** الجلسات تموت **أولًا**، ثم يُثبَّت الصف، ثم كنسة ثانية تلتقط من سجّل دخوله
بينهما. الفشل الآن في الاتجاه الآمن: شخص نشط سُجّل خروجه بلا داعٍ فيعيد الدخول.

**٥. مكتب السلامة لم يكن يستطيع فتح الكونسول أصلًا.**

`GET /me` و `DELETE /session` كانتا خلف `operators.read`. دور `SAFETY` — **المستخدم
الأساسي لهذا الكونسول** — يحصل على 401 من الاثنتين: الواجهة لا تُرسم له، ورمزه ذو
الثماني ساعات لا يستطيع حامله إنهاءه.

**الإصلاح:** `operators.self` يحملها كل دور.

**٦. مشغّل يستطيع إقفال الكونسول نهائيًا.**

لا شيء يمنع مشغّلًا من إيقاف نفسه، ولا من سحب صلاحية **آخر `SUPER_ADMIN` نشط** —
وبما أن `operators.manage` لا يحملها غيره، والـ bootstrap يُغلق نفسه للأبد، فالنتيجة
نشرٌ لا يمكن استعادة أي مشغّل فيه إلا بتفريغ الجدول يدويًا من قاعدة البيانات.

**الإصلاح:** رفضان صريحان، الثاني مُنفَّذ داخل الـ transaction.

**٧. سباق في bootstrap أول مشغّل.**

`count()` ثم `create()`: طلبان متزامنان ببريدين **مختلفين** يقرآن صفرًا وينشئان
`SUPER_ADMIN` **اثنين**. الفهرس الفريد على البريد لا يمسك هذا لأن البريدين مختلفان.
على المسار الوحيد الذي يمنح صلاحية غير مقيّدة على كونسول يرى بيانات أطفال،
«على الأرجح واحد فقط» ليست خاصية مقبولة.

**الإصلاح:** `SERIALIZABLE` — على هذه الدالة وحدها.

**٨. التعديل وسجل التدقيق كانا transaction منفصلتين.**

توجيهك واضح: *كل إجراء ذي صلاحية يجب أن يُنشئ حدث تدقيق غير قابل للتعديل*.
لكن كل مسار كان: عدّل ⟵ انتظر ⟵ سجّل. انهيار أو خطأ بينهما = عائلة مُوقَفة أو بلاغ
مُحرَّك أو مشغّل مسحوب **بلا أي سجل**.

**الإصلاح:** `AuditService.record` تقبل transaction client اختيارية.

---

### 🔴 حرج — النشر (Deploy)

**٩–١٢. ترقية Prisma 7 تركت مسار النشر غير قابل للتشغيل.**

ستة عيوب، كلها بين «commit مدموج» و«حاوية تعمل»، **ولا واحد منها يمكن لأي اختبار
الوصول إليه** — ولهذا كانت الحزمة خضراء بينما `predeploy.sh` لم يكن ليكتمل:

- `dotenv` في `devDependencies` بينما `prisma.config.ts` يستوردها — و `npm ci --omit=dev`
  في مرحلة التشغيل تعني أن الـ CLI لا يستطيع تحميل إعداداته
- الـ Dockerfile لا ينسخ `prisma.config.ts` أصلًا — ومنه فقط تُقرأ سلسلة الاتصال في Prisma 7
- `predeploy.sh` يمرّر رايات `migrate diff` لم تعد Prisma 7 تقبلها
- أربعة ملفات (`predeploy-schema-probe.js`, `seed.ts`, `seed-demo.ts`,
  `grant-default-consents.ts`) ما زالت تبني العميل عبر `datasources` أو مسار wasm — وكلاهما محذوف

**وتعليقان كانا يبالغان — صحّحتهما بدل تركهما:**
`schema.prisma` و `prisma.config.ts` كانا يقولان إن Prisma 7 أزالت الـ engine binary.
أزالت **query engine** فقط. **schema engine ما زال binary أصليًا**، وكل أمر `migrate`
ينزّله — وقد أعدتُ إنتاج الخطأ اليوم في حاوية هذا المشروع: `403 Forbidden` من
`binaries.prisma.sh`، مع `PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1` وبدونها.

> عمليًا: تطبيقك لا يحتاج binary، لكن **الـ migrations تحتاجه**. في بيئة محجوبة، طبّق
> ملفات `prisma/migrations/*/migration.sql` مباشرة بـ psql — وهي نفس الجُمل بنفس الترتيب.
> هكذا فعلتُ اليوم لتشغيل 32 migration.

---

### 🟠 مهم — البنية التحتية للاختبار (كانت تُخفي العيوب أعلاه)

**١٣. اثنتان وخمسون حزمة اختبار تسرّب اتصال Postgres لكل واحدة.**

`$disconnect()` يغلق ما فتحه Prisma، **ولا يغلق `pg.Pool` سلّمه المستدعي**. وكل نسخة
خاصة من `offlinePrismaService()` كانت تبني الـ pool inline، بلا اسم، بلا مرجع لإغلاقه.
Jest كان يطبع «did not exit one second after the test run» وكل حزمة تترك اتصالات خاملة.

**١٤. حارس العزل بين المستأجرين (tenant guard) كان قابلًا للإرضاء بـ«النثر».**

القاعدة 2 كانت تبحث عن `family_id` في الـ 25 سطرًا التالية **حرفيًا** — فجملة SQL بلا
أي نطاق مستأجر تمرّ لأن **تعليقًا** أو جملة مجاورة على بُعد 20 سطرًا تصادف أن تحتوي النص.

اكتشفتها بالصدفة: أضفت تعليقًا طويلًا بين جملتين فسقطت قاعدة كانت خضراء **دون أن يتغير
أي SQL**.

**الإصلاح:** الحارس الآن يحذف التعليقات، **يحلّ ثوابت `SQL_*`** (فجملة محفوظة في ملف
`.sql.ts` تُحاكم بـ SQL الخاص بها لا باسم متغيّرها)، و**يحترم فعلًا** مخرج
`runAsSystem` الذي كانت رسالة الخطأ الخاصة به تَعِد به ولا تفحصه. والمخرجان
**يُعدّان ويُطبَعان** (27 و 4) — لأن مخرجًا لا يعدّه أحد هو مخرج ينمو.

**وثلاث «سقّاطات» (ratchets) اشتغلت فور توفّر قاعدة بيانات حقيقية:**

- `cross-tenant-probe` وجد **أربعة مسارات `system/*` لا يغطيها شيء** — لأن الفلتر كان
  يسمّي `InternalAdminGuard` وحده، والمسارات الجديدة تحمل `OperatorAuthGuard` (الذي
  يفوّض إليه ثم يفحص جلسة وصلاحية — أي نفس البوابة **زائد اثنتين**)
- `tenant-rls.integration` — عدّاد جداول RLS: 54 ← 55، والسياسات 56 ← 57، لـ `ai_alert_notes`
- `ai-safety-alert.e2e` كان يتحقق من عمود **حذفته** migration 0033 (`reviewedByUserId`
  الذي كان يشير إلى `users` — أي فرد من الأسرة — لمراجعة لا يقوم بها إلا موظفو المنصة)

---

## 4. 🚨 عاجل — موعد Google Play بعد تسعة أيام

هذا **ليس** بندًا تقنيًا، بل قرار عمل بتاريخ محدد:

> **Google Play تشترط `targetSdk 36` لكل تطبيق جديد ولكل تحديث ابتداءً من
> ٣١ أغسطس ٢٠٢٦** (مع إمكانية طلب تمديد حتى ١ نوفمبر ٢٠٢٦).

التطبيقان عندك على `targetSdk 34`. **بعد ٣١ أغسطس لن تستطيع نشر أي تحديث لأيٍّ منهما.**

نفّذت الترقية الكاملة. الأرقام الأربعة مترابطة فتتحرك معًا، **وكل رقم استُخرج اليوم من
مصدره الرسمي — لا رقم واحد كُتب من الذاكرة** (قاعدة C-5):

| | قبل | بعد | المصدر |
|---|---|---|---|
| compileSdk / targetSdk | 34 | **36** | شرط Google Play أعلاه |
| Android Gradle Plugin | 8.1.1 | **9.3.0** | developer.android.com — يوليو 2026 |
| Gradle | 8.3 | **9.7.1** | docs.gradle.org — AGP 9.3 يشترط ≥ 9.5.0 |
| Kotlin | 1.9.10 | **2.4.10** | kotlinlang.org — 14 يوليو 2026 |
| Flutter | 3.24.5 | **3.47.0** | docs.flutter.dev — قوالبه انتقلت إلى AGP 9+ |

**اخترتُ أصغر تغيير يحقق الهدف، عمدًا.** AGP 9 يفعّل «Kotlin المدمج» و DSL جديدًا، وهو
ما يستلزم حذف `kotlin-android` وإعادة كتابة `kotlinOptions`. لكن Flutter توثّق حالة
انتقالية لهذه الهجرة بالذات (`android.newDsl=false` و `android.builtInKotlin=false`)،
وهي ما اعتمدته. هجرة الـ DSL تغيير مصدري في ملفَي Gradle **يجب بناؤه ليعني شيئًا**،
وليس هو موضوع الموعد النهائي.

### ⚠️ هذا الجزء تحديدًا: `NOT TESTED`

لا يوجد Flutter SDK ولا Android SDK ولا اتصال بمستودعاتهما في البيئة التي نفّذت التغيير.
مكتوب بوضوح في كل ملف مسّه التغيير.

**التشغيل الأول لـ `build-apk.yml` يجب أن يُتوقَّع منه أن يفشل على عمل حقيقي:**
AGP 9 أزال `BaseExtension` وعدة خصائص `android.*`؛ و API 35/36 شدّدا قواعد
foreground-service (وتطبيق الطفل يعلن `specialUse`)؛ وأي إضافة في `pubspec` لم تنتقل
إلى AGP 9 ستقول ذلك هناك. **هذه الإخفاقات هي المقصودة — النجاح غير مفترض.**

`minSdk` تُركت على 21 بدل تخمينها: أرضية Flutter 3.47 لم تكن قابلة للتحقق هنا، و AGP
يفشل في أول بناء ذاكرًا الرقم المطلوب بالضبط. **minSdk مخمَّنة أسوأ من قديمة.**

**الخطوة المطلوبة منك:**
```bash
gh workflow run build-apk.yml
```

---

## 5. جدول الأدلة — بمفردات الإثبات المتفق عليها

| ما هو | الحالة | الدليل |
|---|---|---|
| اختبارات الواجهة الخلفية (246 حزمة / 5686 اختبار) | `RUNTIME VERIFIED` | PostgreSQL 16 + Redis 7 حقيقيان، 0 متخطّى |
| إصلاح سباق مراجعة السلامة | `RUNTIME VERIFIED` | اختبار تزامن + ضابط تحكّم سلبي |
| سحب جلسات المشغّل (10 متزامنة) | `RUNTIME VERIFIED` | اختبار جديد على الفهرس القائم على SET |
| `tsc --noEmit` — الواجهة الخلفية | `STATIC VERIFIED` | خروج 0 |
| بناء الواجهة الخلفية | `STATIC VERIFIED` | `dist/main.js`، 7.7 MB |
| لوحة الإدارة — tsc + 235 اختبار + بناء | `STATIC VERIFIED` | 20 حزمة، بناء 2.82 ث |
| حارس العزل بين المستأجرين | `STATIC VERIFIED` | 0 مخالفة، مع عدّ المخارج |
| حارس إصدار الأحداث | `STATIC VERIFIED` | 0 مخالفة |
| حارس حداثة الاعتماديات (C-6) | `STATIC VERIFIED` | 60 حزمة، 0 مخالفة |
| اختبار حارس أدوات الموبايل الذاتي | `STATIC VERIFIED` | 16 فحصًا |
| حارس حداثة أدوات الموبايل | `BLOCKED` | المستودعات الأربعة تُجيب 403 هنا |
| **ترقية Flutter / AGP / Gradle / Kotlin** | **`NOT TESTED`** | **لا SDK ولا شبكة — CI هو الفيصل** |
| ترقية `file_picker` وأخواتها | `BLOCKED` | pub.dev يُجيب 403 — لم أخترع `pubspec.lock` |

---

## 6. ما لم أُصلحه — وهو قرارك لا قراري

**١. RLS خامد فعليًا على مستوى المنصة كلها.**

قِسته اليوم ولم أستنتجه: كل جدول مستأجر يحمل سياستين — `tenant_isolation` **و**
`tenant_bypass_owner` الممنوحة لمالك الجدول. و PostgreSQL يجمع السياسات المتساهلة
بـ OR. **والتطبيق يتصل بصفة المالك.** كما أن الدالة المساعدة `withRls` في
`common/tenancy/rls.ts` **ليس لها ولا موضع استدعاء واحد** في المشروع كله.

النتيجة: العزل الحقيقي اليوم يقوم على Prisma Client Extension وحده. RLS طبقة دفاع
معدّة لكنها غير مفعّلة.

**هذا ليس عيبًا أدخلتُه، ولا شيئًا أصلحه من طرفي**: تفعيله قرار نشر (دور Postgres مقيّد
جديد + منح صلاحيات + كلمة سر في Railway) وله كلفة أداء حقيقية. أضفت ضبط
`app.current_family_id` على مسار السلامة وحده — لا يغيّر شيئًا اليوم، ويمنع ذلك المسار
من الإبلاغ عن تعارضات وهمية يوم تنتقلون إلى دور مقيّد.

**٢. أربعة وأربعون مسار مشغّل ما زالت خلف المفتاح المشترك وحده.**

فقط مكتب السلامة ومسارات إدارة المشغّلين خلف `OperatorAuthGuard`. نقل الباقي يُسجّل خروج
كل مشغّل من كونسول حيّ، ويستحق نشرًا مستقلًا — كما هو مكتوب في رأس الوحدة منذ البداية.

**٣. هجرة DSL الخاصة بـ AGP 9** (حذف `kotlin-android`، ونقل `jvmTarget` إلى
`kotlin { compilerOptions { } }`، ثم `android.builtInKotlin=true`). موثّقة بالمصدر في
`gradle.properties` لكلا التطبيقين. ليست عاجلة — الموعد النهائي هو العاجل.

---

## 7. التسليم

| | |
|---|---|
| الملف | `ABNY-final-review-fixes.zip` |
| النطاق | `a4ca8e4..18c332e` — ستة commits |
| الملفات | 94 معدّلًا · 1 محذوف |
| التنسيق | مسارات بالنسبة لجذر المشروع، بلا مجلد غلاف (قاعدة C-1) |
| الحذف | `_DELIVERY/DELETE.ps1` و `DELETE.sh` — الملف الوحيد المحذوف هو `__pycache__` |

الـ commits الستة:

```
18c332e  chore: stop committing scripts/ci/__pycache__
365c831  feat(mobile): raise the whole Android chain — Play requires targetSdk 36
1735d22  fix(test): the suites leaked connections, and three ratchets were passing wrongly
b0d8156  fix(safety): two operators, one alert, and a dismissal that erased an escalation
b81ddc0  fix(operators): the one thing this sprint was for could not be done
c5050a1  fix(deploy): the Prisma 7 upgrade left the deploy path unrunnable
```

---

## 8. الخلاصة بثلاث جمل

**١.** لا، لم يكن الكل مراجَعًا — والآن صار كذلك، والمراجعة وجدت أربعة عشر عيبًا حقيقيًا
منها ستة تمنع الإطلاق وواحد يمسّ سلامة الأطفال مباشرة.

**٢.** السبب الجذري في كل الحالات الثلاث الأخيرة واحد: **ثلثا حزم الاختبار كانت
تُتخطّى بصمت**، وحارس واحد كان يُرضى بالتعليقات بدل الكود — أي أن «الأخضر» لم يكن يعني
ما ظننّاه.

**٣.** الأمر الأكثر إلحاحًا الآن ليس شيئًا مما أصلحته، بل **شغّل `build-apk.yml`**:
ترقية الموبايل غير مبنيّة، وموعد Google Play بعد تسعة أيام.
