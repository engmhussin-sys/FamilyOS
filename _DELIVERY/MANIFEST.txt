ABNY — DELIVERY MANIFEST
range   : HEAD~1..HEAD
head    : 73a29ac7b4af00fa8c5efc367e42f6623618b24e
branch  : abny/sprint-f1-unblock
files   : 6 changed, 1 to delete

HOW TO APPLY
  1. Run _DELIVERY/DELETE.ps1 (Windows) or DELETE.sh — ONLY if it lists files.
  2. Extract this zip over the project root. Choose Replace when asked.
     The paths inside are repo-relative: apps/... lands on apps/...
  3. Delete the _DELIVERY folder. It is notes, not source.

CHANGED (extract these)
M	.github/workflows/ci.yml
A	P3005.md
A	apps/backend/test/deploy/db-baseline.spec.ts
A	scripts/db-baseline.ps1
A	scripts/db-baseline.sh
R076	scripts/verify_doctor_parity.py	scripts/verify_ops_script_parity.py

DELETED (a zip cannot remove these — run the DELETE script)
scripts/verify_doctor_parity.py

COMMITS
73a29ac deploy: P3005 — baseline the database, and refuse when that would be a lie
