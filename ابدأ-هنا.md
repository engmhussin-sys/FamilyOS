# ابدأ من هنا — ABNY / «ابني»

ثلاث خطوات. كل أمر منسوخ كما هو.

---

## الخطوة ١ — احصل على الكود (٢ دقيقة)

الـ sandbox ممنوع من الـ `git push` (البروكسي يرفض حقن بيانات الاعتماد لهذا المستودع). لذلك أرسلتُ لك **كل الكوميتات في ملف واحد**: `ABNY-commits.bundle`.

على جهازك، داخل مجلد المشروع:

```bash
git fetch /path/to/ABNY-commits.bundle abny/sprint-f1-unblock:abny/sprint-f1-unblock
git checkout abny/sprint-f1-unblock
git push origin abny/sprint-f1-unblock
```

استبدل `/path/to/` بمكان تنزيل الملف.

**بمجرد الـ push، يبدأ GitHub Actions ببناء الـ APK تلقائيًا.**

---

## الخطوة ٢ — نزّل أول APK (١٠–١٥ دقيقة، بدون أي إعداد منك)

الـ workflow `.github/workflows/build-apk.yml` موجود ويرفع الـ APK كـ artifact.

1. افتح المستودع على GitHub → تبويب **Actions**
2. اختر آخر تشغيل على فرع `abny/sprint-f1-unblock`
3. انزل إلى **Artifacts** ونزّل:
   - `parent-app-debug-<sha>` → تطبيق ولي الأمر
   - `child-app-debug-<sha>` → تطبيق الطفل
4. انقل الملف إلى هاتف Android وثبّته (فعّل «تثبيت من مصادر غير معروفة»)

**نسخة الـ debug لا تحتاج Firebase ولا keystore.** تلك مطلوبة فقط لنسخة الإصدار (AAB) على المتجر — التفاصيل في `MOBILE_BUILD_HANDOFF.md`.

إذا فشل الـ build: افتح الـ artifact المسمّى `analyze-<app>-<sha>` أو `buildlog-<app>-<sha>` — فيه الخطأ الحقيقي سطرًا بسطر. هذه أول مرة يُصرَّف فيها الكود فعليًا، فتوقّع ملاحظة أو اثنتين من المحلّل؛ هذا طبيعي وليس دليلًا على خلل.

---

## الخطوة ٣ — افتح الداشبورد

الخطوات الكاملة والدقيقة في **`apps/admin-dashboard/RUN.md`**. باختصار:

```bash
docker compose up -d            # PostgreSQL + Redis
cd apps/backend
npx prisma migrate deploy
npm run start:dev               # المنفذ 3000

# في نافذة أخرى
cd apps/admin-dashboard
npm run dev                     # المنفذ 5173
```

ثم افتح `http://localhost:5173`.

**مفتاح المشغّل:** الداشبورد سيطلب منه عند فتح شاشات النمو. ضع في `.env` الخاص بالـ backend:

```
INTERNAL_ADMIN_API_KEY=<سر عشوائي طويل>
```

والصق القيمة نفسها في الداشبورد. المفتاح **يُحفظ في الذاكرة فقط** — لا في `localStorage` ولا في كوكي ولا في الرابط، ويُمسح فور أي رفض من الخادم. لهذا ستعيد إدخاله بعد كل تحديث للصفحة، وهذا مقصود.

**قاعدة بيانات جديدة ستظهر فارغة — وهذا هو الجواب الصحيح.** الداشبورد لا يخترع أرقامًا: ما لا يمكن قياسه يظهر «غير مُقاس» ولا يظهر أبدًا كصفر.

---

## ما الذي لا يعمل بعد، بصراحة

| البند | الحالة |
|---|---|
| **الإشعارات على جهاز حقيقي** | تحتاج مشروع Firebase. العقد من جهتنا جاهز؛ التقاط الـ token يملكه فريق آخر |
| **رفع دليل الإنجاز (تسميع/صورة)** | الكود مكتوب بالكامل، لكن ٥ حزم لم تُحمَّل هنا (المستودعات محجوبة). ستُحلّ عند أول `flutter pub get` |
| **الدفع الحقيقي** | Paymob / Fawry — التعاقد التجاري ٤–٨ أسابيع. **هذا البند الوحيد الذي يزداد سوءًا بالانتظار — ابدأه اليوم** |
| **staging** | لا يوجد نشر بعد |
| **iOS** | قرار مسجَّل: الرقابة الأبوية الكاملة تتطلب جهاز Android |

---

## الحالة المقيسة الآن

- الخادم: **٢٢١ مجموعة · ٥٣٣٩ اختبارًا · صفر إخفاق** على قاعدة بيانات مُهاجَرة من الصفر (٣٠ هجرة → ١٠١ جدول)، مع Redis حقيقي وتطبيق HTTP مُشغَّل فعليًا
- الداشبورد: **١٨٤ اختبارًا** ناجحة، والبناء ينجح
- التطبيقان: **٩ فاحصات ثابتة**، صفر مشاكل

**لم يُصرَّف أي كود Flutter هنا ولم يُنفَّذ أي اختبار Dart** — لا توجد أدوات Flutter في هذه البيئة. أول تصريف حقيقي سيحدث في GitHub Actions بعد الـ push.
