# عطل بناء Railway — التشخيص والحلّ

سجلّان من ٢٠٢٦-٠٨-٢٠، الساعة ٢٣:٤٥.

---

## ما تقوله السجلات بالضبط

```
23:45:12  "/apps/backend/prisma": not found
23:45:25  "/apps/backend/src": not found
```

**مسار مختلف في كل مرة.** ومعهما في السجلّين:

```
[err]  [builder 4/11] COPY apps/backend/package.json apps/backend/package-lock.json ./
[err]  [builder 5/11] COPY apps/backend/prisma ./prisma
[err]  [builder 8/11] COPY apps/backend/tsconfig.json …
[err]  [builder 9/11] COPY apps/backend/src ./src
```

**كل أوامر النسخ من سياق البناء فشلت.** buildkit ينفّذها بالتوازي ويبلّغ عن أوّل من سقط — ولهذا اختلف الاسم بين التشغيلين. فالمشكلة ليست مجلدًا ناقصًا واحدًا: **السياق المرفوع لا يحتوي `apps/` أصلًا.**

## وما تقوله السجلات إنه يعمل

```
[inf]  [internal] load build definition from apps/backend/Dockerfile
[inf]  [internal] load .dockerignore
```

الاثنان نجحا. أي أن Railway **يقرأ `railway.json`** ويجد الـDockerfile في مكانه ويقرأ `.dockerignore`. **فالـDockerfile ليس المشكلة** — المشكلة في محتوى الأرشيف الذي رُفع.

> هذا **تقدّم** عن المحاولة الأولى: قبلها لم يكن `railway.json` يُقرأ إطلاقًا.

---

## ثلاثة أسباب محتملة — ولا أستطيع رؤية إعداداتك

| # | السبب | كيف تتحقق |
|---|---|---|
| ١ | **Root Directory ما زال `apps/backend`** | Railway ← الخدمة ← Settings ← Source ← يجب أن يكون `/` أو فارغًا |
| ٢ | **Railway يبني فرعًا آخر** (`main` مثلًا وأنت دفعتَ `abny/sprint-f1-unblock`) | Settings ← Source ← Branch. وقارن الـcommit sha في ترويسة النشر بما دفعتَه |
| ٣ | **الملفات ليست على GitHub فعلًا** — فُكّ الضغط في مجلد متداخل، أو لم يكتمل الرفع | على جهازك: `git ls-files apps/backend/src \| head` — إن لم يُخرج شيئًا فالملفات غير مرفوعة |

**تخمين أيّها هو يكلّف بناءً لكل تخمين.**

---

## الحلّ: اجعل البناء نفسه يخبرك — بناء واحد

أضفتُ `apps/backend/Dockerfile.context-probe`. لا ينسخ مسارات محددة، بل ينسخ **السياق كلّه** ثم يسرده. و`COPY . /ctx` لا يمكن أن يفشل كما تفشل الأوامر الأخرى — السياق موجود دائمًا ولو كان فارغًا. هذه هي الحيلة كلّها: تحويل خطأ يذكر مسارًا واحدًا إلى **قائمة بكل ما هو موجود**.

### الاستخدام

1. Railway ← الخدمة ← **Settings ← Build** ← اجعل مسار الـDockerfile:
   ```
   apps/backend/Dockerfile.context-probe
   ```
2. انشر. **سيفشل عمدًا في سطره الأخير** — وهذا مقصود: البناء الناجح تطوي المنصّة سجلّه، والفاشل يُبقي كل شيء ظاهرًا.
3. اقرأ الكتل الستّ في السجل.
4. أعد المسار إلى `apps/backend/Dockerfile`.

### قراءة النتيجة

| ما تراه | المعنى | الحل |
|---|---|---|
| `apps/` موجود و`apps/backend/src` موجود | السياق سليم | أعد تشغيل البناء الحقيقي |
| **`apps/` مفقود**، لكن `src/` و`prisma/` في جذر السياق | Root Directory = `apps/backend` — السياق يبدأ مستوًى أعمق | Settings ← Source ← Root Directory = `/` |
| `apps/` موجود و`apps/backend/src` مفقود | الرفع مُرشَّح | راجع `.dockerignore` وتأكد أن `src` مرفوع فعلًا |
| الجذر شبه **فارغ** | فرع آخر أو لقطة قديمة | Settings ← Source ← Branch، وقارن الـsha |

والكتلة السادسة تحسم سؤال «أي كوميت يبني Railway؟»: إن غاب `CONVENTIONS.md` فالسياق **أقدم من الكوميت ٢٩٢** — أي أن Railway يبني دفعة أقدم مما تظن.

---

## قبل الاختبار — تحقّق من واحد

راجع البند رقم ١ أعلاه أولًا؛ **هو الأرجح**، وإصلاحه إعداد واحد بلا أي بناء. إن كان `/` بالفعل، فشغّل المسبار.

---

## وتذكير واحد

السجلّ لا يذكر اسم الخدمة. **تأكد أنك على staging لا production** — المضيف الإنتاجي هو `familyos-production-74ca.up.railway.app`. البناء الفاشل غير ضارّ إطلاقًا: لم يُنشر شيء، ولم تُنفَّذ أي هجرة، والمضيف الحيّ ظلّ يخدم كما هو. لكن المحاولة الناجحة القادمة **ستنشر** — ويجب أن تنشر على staging.
