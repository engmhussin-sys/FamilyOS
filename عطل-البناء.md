# عطل بناء Railway — أُغلق من جذره

**آخر تحديث: ٢٠٢٦-٠٨-٢١** — بعد ستّ محاولات فاشلة.

---

## ما حدث، باختصار

أضفتُ `railway.json` في **جذر** المستودع وأعدتُ كتابة `apps/backend/Dockerfile` ليبني من جذر المستودع. **وهذا كسر إعدادًا كان يعمل.**

مشروعك على Railway فيه أكثر من خدمة من مستودع واحد — `familyos-dashboard` جذرها `apps/admin-dashboard`، والخادم خدمة أخرى. وRailway يقرأ ملف الإعداد **بالمسار لا بالخدمة**. فخدمة اللوحة قرأت ملفي المكتوب للخادم، وحمّلت Dockerfile الخادم بنجاح (لأن مساره يُحلّ من جذر المستودع)، ثم فشل كل نسخ لأن **سياق البناء هو مجلد الخدمة نفسها** — و`apps/backend/src` غير موجود داخل `apps/admin-dashboard/`.

### البصمة التي تستحق أن تُحفظ

```
[inf]  load build definition from apps/backend/Dockerfile   ← نجح
[err]  COPY apps/backend/package.json … prisma … src        ← فشل الكل
```

**Dockerfile يُقرأ بنجاح بينما كل أوامر نسخه تفشل = الـDockerfile والسياق جاءا من مجلدين مختلفين.** هذا خطأ في إعداد الخدمة، لا في الملف — أيًّا كان المشروع.

---

## الإصلاح: كل قابل للنشر مستقلّ بذاته

بدل أن أطلب منك تعديل إعدادات مرة بعد مرة، **أزلتُ السبب**:

| كان | صار |
|---|---|
| `railway.json` في جذر المستودع | `apps/backend/railway.json` |
| `dockerfilePath: apps/backend/Dockerfile` | `dockerfilePath: Dockerfile` |
| `COPY apps/backend/src ./src` | `COPY src ./src` |
| `.dockerignore` في الجذر | `apps/backend/.dockerignore` |
| سياق البناء = جذر المستودع | **سياق البناء = `apps/backend`** |

وتبعها `docker-compose.yml` و`render.yaml` و`ci.yml` وworkflow النشر — **ستّة ملفات تسمّي السياق، وكلها متفقة الآن**، ويوجد فحص في الـworkflow يسقط إن عاد `railway.json` إلى الجذر.

**النتيجة:** خدمة الخادم تقرأ إعدادها من مجلدها، ولا تستطيع أن تمسّ أي خدمة شقيقة. وخدمة اللوحة تعود إلى ما كانت تفعله.

---

## ما تفعله أنت — إعدادان

### على خدمة **الخادم**

```
Settings ← Source ← Root Directory  =  apps/backend
Settings ← Config-as-code           =  railway.json
```

`railway.json` هنا يُحلّ **داخل** `apps/backend`، فيجد الملف الجديد.

### على خدمة **`familyos-dashboard`**

```
Settings ← Config-as-code  ←  امسح المسار
```

كانت `Online` طوال الوقت ولم تتأثر — **البناء الفاشل لا يستبدل نشرًا يعمل**. مسح الحقل يعيدها إلى ما كانت عليه.

### ⚠️ وقبل الاثنين

مُحدِّد البيئة في أعلى الصفحة كان مكتوبًا عليه **`production`**. كل إعداد تغيّره وهو مختار **يغيّر الإنتاج**. بدّله إلى staging أولًا.

---

## إن فشل البناء مرة أخرى

لا تخمّن. `apps/backend/Dockerfile.context-probe` ينسخ السياق كلّه ويسرده — و`COPY .` لا يمكن أن يفشل كما تفشل الأوامر المسمّاة، لأن السياق موجود دائمًا ولو كان فارغًا.

**الاستخدام:** Settings ← Build ← مسار الـDockerfile = `Dockerfile.context-probe` ← انشر ← اقرأ الكتل الخمس ← أعده إلى `Dockerfile`. يفشل عمدًا في آخره حتى لا تطوي المنصّة سجلّه.

**القراءة — وانتبه، فهي معكوسة عمّا كانت قبل الترحيل:**

| ما تراه في جذر السياق | المعنى |
|---|---|
| `package.json` و`prisma/` و`src/` | **صحيح** — السياق هو `apps/backend`. أي فشل بعدها داخل البناء نفسه (npm أو prisma أو tsc)، اقرأ خطأ الخطوة |
| مجلد `apps/` | السياق هو جذر المستودع، مستوًى أعلى مما يجب → Root Directory = `apps/backend` |
| لا `apps/` ولا `package.json` | مجلد خدمة أخرى تمامًا — تأكد أنك على خدمة الخادم |
| `package.json` موجود و`src/` مفقود | الرفع مُرشَّح → راجع `apps/backend/.dockerignore` |
| شبه فارغ | فرع آخر أو لقطة قديمة → راجع Branch وقارن الـsha |

والكتلة الرابعة تطبع `railway.json` من داخل السياق — فتعرف أي إعداد تقرأه الخدمة فعلًا.

---

## ولا ضرر وقع

ستّ محاولات فاشلة **لم تنشر شيئًا ولم تُنفّذ هجرة واحدة**. خادمك ولوحتك وRedis كلها ظلّت `Online` تخدم طوال الوقت. البناء الفاشل يموت قبل أن يلمس أي شيء حيّ.

**ولم يُبنَ هذا الإصلاح هنا** — لا يوجد Docker في بيئتي. كل ما سبق `STATIC VERIFIED`: المسارات مفحوصة، والملفات الستّة متفقة، وpython الخاص بالـworkflow نُفِّذ على الشجرة وخرج بصفر. أول بناء حقيقي عندك هو الإثبات.
