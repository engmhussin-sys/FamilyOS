# انشر الآن — ٣ أوامر

**لا أستطيع الـ push من بيئتي.** البروكسي يرفض هذا المستودع تحديدًا:

```
access denied by the git proxy: engmhussin-sys/FamilyOS is not in this
session's authorized repository set
```

هذا إعداد في الجلسة، لا خلل في الكود. لذلك النشر يتم منك — وهو ثلاثة أوامر.

---

## الأوامر

نزّل `ABNY-commits.bundle`، ثم من داخل مجلد المشروع على جهازك:

```bash
git fetch /path/to/ABNY-commits.bundle abny/sprint-f1-unblock:abny/sprint-f1-unblock
git checkout abny/sprint-f1-unblock
git push origin abny/sprint-f1-unblock
```

استبدل `/path/to/` بمسار التنزيل. الملف يحمل **٢٥٩ كوميت** كاملة.

---

## بعد الـ push مباشرة

**١. الـ APK يُبنى تلقائيًا.** الـ workflow يعمل من تلقاء نفسه:

GitHub → **Actions** → آخر تشغيل على `abny/sprint-f1-unblock` → **Artifacts**:

- `parent-app-debug-<sha>` — تطبيق ولي الأمر
- `child-app-debug-<sha>` — تطبيق الطفل

انقل الـ APK إلى هاتف Android وثبّته. **نسخة الـ debug لا تحتاج Firebase ولا keystore.**

إن فشل البناء، الجواب في artifact اسمه `analyze-<app>-<sha>` — سطر الخطأ بالضبط. هذه أول مرة يُصرَّف فيها كود Flutter في تاريخ المشروع، فتوقّع ملاحظة أو اثنتين؛ هذا ثمن غياب الأدوات هنا، لا خلل في العمل.

**٢. الداشبورد — بأرقام حقيقية:**

```bash
docker compose up -d
cd apps/backend
npx prisma migrate deploy
npm run seed:demo          # ← بيانات تجريبية واقعية
npm run start:dev

# نافذة أخرى
cd apps/admin-dashboard
npm run dev
```

افتح `http://localhost:5173`.

`seed:demo` ينشئ ٣٠ أسرة (١٥ مصر · ١٢ السعودية · ٣ بلا بلد)، ٤٥ طفلًا عبر كل الفئات العمرية، ٢١ اشتراكًا في كل حالات دورة الحياة بعملة كل بلد، ٣٣٠ محاولة إنجاز، ٢٧٢ سطر مكافأة، و٩٨ يومًا من التاريخ حتى تظهر الاتجاهات والأرباع.

الأرقام تمرّ عبر **الخدمات الحقيقية والـ outbox** — ليست صفوفًا مزروعة بالقوة، فهي تحترم نفس القيود التي يحترمها الإنتاج. والسكربت يرفض العمل على أي قاعدة بيانات ليست محلية.

**مفتاح المشغّل:** ضع `INTERNAL_ADMIN_API_KEY=<سر طويل>` في `.env` والصق القيمة نفسها في الداشبورد. يُحفظ في الذاكرة فقط — ستعيد إدخاله بعد كل تحديث للصفحة، وهذا مقصود.

---

## الحالة المقيسة قبل النشر مباشرة

| | |
|---|---|
| الخادم | **٢٢١ مجموعة · ٥٣٣٩ اختبارًا · صفر إخفاق** — قاعدة بيانات من الصفر (٣٠ هجرة → ١٠١ جدول)، Redis حقيقي، HTTP مُشغَّل |
| `tsc` | ناجح |
| حارس العزل بين الأسر · حارس الأحداث | صفر مخالفات |
| الداشبورد | ١٨٤ اختبارًا · البناء ينجح |
| التطبيقان | ٩ فاحصات ثابتة · صفر مشاكل |

**غير مُدَّعى:** لم يُصرَّف كود Flutter ولم يُنفَّذ اختبار Dart في هذه البيئة. أول تصريف حقيقي في GitHub Actions.

---

## ما يحتاجك بعد التجربة الأولى

| البند | لماذا |
|---|---|
| **Paymob / Fawry** | تعاقد ٤–٨ أسابيع. **البند الوحيد الذي يزداد سوءًا بالانتظار — ابدأه اليوم** |
| Firebase | للإشعارات على جهاز حقيقي |
| keystore | لنسخة المتجر (AAB) — التفاصيل في `MOBILE_BUILD_HANDOFF.md` |
| staging | لا يوجد نشر بعد |
