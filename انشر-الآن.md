# انشر الآن — النسخة المحدَّثة (٢٢ أغسطس ٢٠٢٦)

> يحلّ محلّ نسخة سبرنت F1. كل رقم هنا صحيح لهذا الفرع:
> `feat/w2-operations-surface` @ `083acd1`.

---

## قبل أي شيء: لماذا يهمّ هذا النشر تحديدًا

**الحاوية لم تكن تُقلع أصلًا.** ثلاث حزم يستوردها كود الإنتاج كانت في
`devDependencies`، وصورة الإنتاج تُثبّت بـ `npm ci --omit=dev`:

```
Error: Cannot find module 'pg'
  at .../node_modules/@prisma/adapter-pg/dist/index.js:39
  ... /app/dist/main.js
```

على Railway هذه **حلقة إعادة تشغيل**، لا فشل اختبار — ولا يستطيع أي اختبار
رؤيتها، لأن الاختبارات تعمل على التثبيت الكامل. أُصلحت الثلاث
(`@prisma/adapter-pg`, `prisma`, `dotenv` ⟵ `dependencies`)، وأُضيف حارس
`CI RULE 5` يمنع تكرار هذا الصنف.

**ومسار النشر** كان معطوبًا في أربعة مواضع أخرى بعد ترقية Prisma 7: الـ
Dockerfile لا ينسخ `prisma.config.ts`، ورايات `migrate diff` قديمة، وثلاثة
ملفات تبني العميل بطريقة محذوفة. كلها أُصلحت.

**تحقّقتُ بالتشغيل، لا بالاستنتاج.** Docker Hub محجوب في بيئتي، فأعدتُ إنتاج
مرحلة التشغيل على القرص: نفس ملفات الـ `COPY` بالضبط، ثم `npm ci --omit=dev`،
ثم أمر `CMD` الخاص بالحاوية. النتائج في §4.

---

## ١. الحصول على الكود

النشر يتمّ منك: بيئتي لا تستطيع الـ `push` إلى هذا المستودع — البروكسي يرفضه،
وهو إعداد جلسة لا خلل في الكود:

```
remote: access denied by the git proxy: engmhussin-sys/FamilyOS is not in
this session's authorized repository set
```

استخرج ملف التسليم فوق مجلد المشروع (المسارات نسبية لجذره)، ثم:

```bash
git checkout -b feat/w2-operations-surface
git add -A && git commit -m "sprint F2 — operations surface, safety review, deploy fixes"
git push origin feat/w2-operations-surface
```

---

## ٢. النشر — Railway

`apps/backend/railway.json` يقود كل شيء ولا يحتاج تعديلًا:

| | |
|---|---|
| Builder | `DOCKERFILE` · `apps/backend/Dockerfile` |
| **Root Directory للخدمة** | **`apps/backend`** ← إن كانت خطأ، تفشل كل أوامر `COPY` |
| preDeployCommand | `sh scripts/predeploy.sh` |
| healthcheckPath | `/health/ready` |

### متغيّرات البيئة المطلوبة

| المتغيّر | ملاحظة |
|---|---|
| `DATABASE_URL` | Postgres — **في Prisma 7 يُقرأ عبر `prisma.config.ts`**، لا من `schema.prisma` |
| `REDIS_URL` | مطلوب — جلسات المشغّلين تعيش هنا، وسحب الصلاحية يعتمد عليه |
| `JWT_ACCESS_SECRET` | **٣٢ حرفًا على الأقل** وإلّا يرفض التطبيق الإقلاع |
| `JWT_REFRESH_SECRET` | نفس الشرط |
| `INTERNAL_ADMIN_API_KEY` | البوابة الخارجية لكل مسارات `system/*` |
| `GIT_COMMIT_SHA` | اضبطه كمتغيّر خدمة — `railway up` لا يمرّر build-args، وبدونه يُبلّغ التشخيص `null` |

> **لا يصل أيٌّ من هذه إلى المتصفّح.** لوحة الإدارة لا ترى
> `INTERNAL_ADMIN_API_KEY` إطلاقًا.

### ما سيحدث بالترتيب

1. بناء الصورة (مرحلتان: builder ثم runtime)
2. `preDeployCommand` ⟵ `predeploy.sh` يطبّق الـ migrations، و**يخرج بـ `1`
   عند أي فشل** فلا تُرقّى صورة لم تُطبَّق هجراتها
3. فحص الصحة على `/health/ready` — لا يمرّ إلّا و Postgres و Redis حيّان

### أول مشغّل على النشر الجديد

المسار يُغلق نفسه للأبد بعد أول مشغّل:

```bash
curl -X POST https://<host>/api/v1/system/operators/bootstrap \
  -H "x-internal-admin-key: $INTERNAL_ADMIN_API_KEY" \
  -H 'content-type: application/json' \
  -d '{"email":"you@abny.app","fullName":"…","password":"<١٢ حرفًا فأكثر>"}'
```

ثم `POST /api/v1/system/operators/session` للحصول على الرمز، ويُرسَل بعدها في
ترويسة `x-operator-session`. الرمز صالح **ثماني ساعات** ولا يتجدّد.

---

## ٣. تطبيقا الموبايل — ⚠️ اقرأ قبل النشر

**سلسلة أدوات Android رُقّيت بالكامل ولم تُبنَ.**

| | قبل | بعد |
|---|---|---|
| Flutter | 3.24.5 | **3.47.0** |
| AGP | 8.1.1 | **9.3.0** |
| Gradle | 8.3 | **9.7.1** |
| Kotlin | 1.9.10 | **2.4.10** |
| compileSdk / targetSdk | 34 | **36** |

**السبب موعد نهائي:** Google Play تشترط `targetSdk 36` لكل تحديث من
**٣١ أغسطس ٢٠٢٦**. عند 34 لن تستطيع نشر أي تحديث بعد ذلك التاريخ.

```bash
gh workflow run build-apk.yml
```

**توقّع فشل التشغيل الأول على عمل حقيقي**: AGP 9 أزال `BaseExtension` وخصائص
`android.*`؛ و API 35/36 شدّدا قواعد foreground-service (تطبيق الطفل يعلن
`specialUse`)؛ وأي إضافة في `pubspec` لم تنتقل إلى AGP 9 ستقول ذلك هناك.
**هذه الإخفاقات مقصودة — النجاح غير مفترض.**

الواجهة الخلفية **لا تعتمد على هذا إطلاقًا**: انشرها اليوم، وعالج الموبايل
بالتوازي.

---

## ٤. ما تم التحقّق منه فعليًا — لا استنتاجًا

| ما هو | النتيجة |
|---|---|
| الاختبارات (٢٤٦ حزمة / ٥٦٨٦ اختبارًا) | ✅ مقابل PostgreSQL 16 + Redis 7 حقيقيين، **صفر متخطّى** |
| إقلاع صورة الإنتاج (`npm ci --omit=dev` + أمر CMD) | ✅ صفر أسطر خطأ |
| `/health/live` · `/health/ready` | ✅ 200 · 200 `{database:true, redis:true}` |
| `/health/live` مع إيقاف Postgres | ✅ يبقى **200** بينما `ready` يصير **503** |
| `predeploy.sh` من داخل الصورة | ✅ يحمّل `prisma.config.ts` ويبلغ `migrate deploy`، ويخرج بـ `1` عند الفشل |
| `GET /system/diagnostics` | ✅ يُبلّغ الـ commit الحقيقي |
| **سحب صلاحية مشغّل واحد** | ✅ جلسته الحيّة تصير **401 في الطلب التالي**، والبقية سليمة |
| فصل الصلاحيات | ✅ `SAFETY` يقرأ طابور السلامة و**يُرفَض** دليل الموظفين |
| منع إقفال الكونسول | ✅ `OPERATOR_CANNOT_MODIFY_SELF` |
| طابور السلامة بلا كلمات | ✅ لا `title` ولا `description` · `contentIncluded:false` |
| تدقيق قراءة محتوى بلاغ طفل | ✅ صف تدقيق واحد بالضبط، يسمّي المشغّل ودوره والعائلة |
| منع حذف تاريخ السلامة | ✅ `abny_app` لا يملك `UPDATE` ولا `DELETE` على `ai_alert_notes` |
| سباق مراجعة البلاغات | ✅ فائز واحد من عشرة، والخاسر لا يكتب شيئًا (مع ضابط تحكّم سلبي) |
| لوحة الإدارة | ✅ `tsc` · ٢٣٥ اختبارًا · البناء ينجح |
| الحرّاس الثمانية | ✅ كلها خضراء |
| **بناء صورة Docker** | ⚠️ **لم يُنفَّذ** — Docker Hub محجوب هنا؛ CI يبنيها |
| **بناء تطبيقي Flutter** | ⚠️ **لم يُنفَّذ** — لا SDK ولا شبكة |

---

## ٥. إن فشل شيء

| العَرَض | الجواب |
|---|---|
| كل أوامر `COPY` تفشل بينما الـ Dockerfile يُقرأ | Root Directory للخدمة ليس `apps/backend` — راجع رأس الـ Dockerfile |
| `Cannot find module '<x>'` عند الإقلاع | حزمة في `devDependencies` مستوردة من `src/` — شغّل `npm run ci:runtime-deps` |
| `P3005` من `migrate deploy` | `predeploy.sh` يتولّاها تلقائيًا؛ التفاصيل في `P3005.md` |
| `403` من `binaries.prisma.sh` | schema engine ما زال binary أصليًا — طبّق `prisma/migrations/*/migration.sql` بـ psql |
| فحص الصحة يفشل والتطبيق يعمل | `/health/ready` يفحص Postgres **و** Redis — راجع `REDIS_URL` |
| `"commit": null` في التشخيص | `GIT_COMMIT_SHA` غير مضبوط كمتغيّر خدمة |

قبل أي إصدار، شغّل فحص العشر دقائق في `CONVENTIONS.md` §C-7 — إعادة إنتاج مرحلة
التشغيل وإقلاعها. **هو ما كشف عطب الإقلاع أعلاه.**

---

## ٦. للتجربة المحلية (اختياري)

```bash
docker compose up -d
cd apps/backend
npx prisma migrate deploy      # يحتاج وصولًا إلى binaries.prisma.sh
npm run seed:demo              # بيانات تجريبية واقعية
npm run start:dev

# نافذة أخرى
cd apps/admin-dashboard && npm run dev     # http://localhost:5173
```

`seed:demo` ينشئ ٣٠ أسرة (١٥ مصر · ١٢ السعودية · ٣ بلا بلد)، ٤٥ طفلًا، ٢١
اشتراكًا في كل حالات دورة الحياة بعملة كل بلد، ٣٣٠ محاولة إنجاز، ٢٧٢ سطر
مكافأة، و٩٨ يومًا من التاريخ. الأرقام تمرّ عبر **الخدمات الحقيقية والـ outbox**
— ليست صفوفًا مزروعة، والسكربت يرفض العمل على قاعدة بيانات غير محلية.

---

## ٧. ما يحتاجك بعد النشر

| البند | لماذا |
|---|---|
| **`gh workflow run build-apk.yml`** | ترقية الموبايل غير مبنيّة، وموعد Play بعد تسعة أيام |
| **Paymob / Fawry** | تعاقد ٤–٨ أسابيع. **البند الوحيد الذي يزداد سوءًا بالانتظار** |
| Firebase | للإشعارات على جهاز حقيقي |
| keystore | لنسخة المتجر (AAB) — التفاصيل في `MOBILE_BUILD_HANDOFF.md` |
| قرار: تفعيل RLS | العزل اليوم يقوم على Prisma Extension وحده — التفاصيل في `FINAL_REVIEW_REPORT.md` §6 |
