/// THE CHILD APP DESIGN SYSTEM — one import for every new screen.
///
/// Audit PA-M-044 (🔴 High). Same structure as the Parent App's barrel and
/// deliberately NOT the same values: a parent tool optimises for density
/// and trust signals, a child's daily coach optimises for warmth, big
/// targets and glanceability. Two systems, one architecture.
library;

export 'kid_components.dart';
export 'kid_states.dart';
export 'kid_tokens.dart';
