# الدخول إلى لوحة التحكم

**٢٠٢٦-٠٨-٢١**

---

## الجواب المباشر، قبل أي خطوة

**لا يوجد «حساب أدمن» جاهز في هذا المشروع، ولن أختلق لك واحدًا.** وهذا ليس نقصًا، بل تصميم:

- **الداشبورد لا تملك دور أدمن منفصلًا للدخول.** تسجّل الدخول بـ`POST /auth/login` **بحساب ولي أمر عادي** — الحساب نفسه الذي يستعمله أي مستخدم. الشاشات العائلية (النظرة العامة، الإعدادات، المؤسسة) تعرض **أسرتك أنت فقط**، ويضمن ذلك حارس عزل المستأجرين.
- **الشاشات المنصّية** (`/growth/*` وسجل قرارات الإشعارات) تقف خلف **مفتاح مشغّل** — وهو **ليس حساب دخول**، بل سرّ مشترك بين خادمك ومتصفحك، **تولّده أنت** ولا يأتي منّي.

فالبيانات التي تطلبها لها مصدران مختلفان تمامًا، وأحدهما تصنعه بنفسك بأمر واحد.

---

## ⚠️ أولًا: على staging لن ينجح أي حساب الآن

لا لأن البيانات خاطئة — بل لأن **الداشبورد لا تعرف عنوان الخادم بعد**.

`apps/admin-dashboard/src/shared/lib/httpClient.ts` يقرأ:

```ts
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL;
```

وVite **يحقن هذه القيمة وقت البناء لا وقت التشغيل**. فإن لم تكن مضبوطة عند بناء الحزمة، تصير قيمتها `undefined`، ويذهب أول نداء إلى `undefined/auth/login` — وهو **مسار نسبي** يُحلّ على نطاق الداشبورد نفسها، فيردّ المستضيف الساكن `404`. تظهر لك رسالة تبدو كعطل في الخادم، والخادم سليم تمامًا.

فاضبط الاثنين معًا — **كلاهما ضروري، ولا يرسم Railway خطًّا لأيٍّ منهما، ولهذا يُنسيان:**

| الخدمة | المتغيّر | القيمة |
|---|---|---|
| **الداشبورد** | `VITE_API_BASE_URL` | `https://<خادم-staging>/api/v1` |
| **الخادم** | `CORS_ALLOWED_ORIGINS` | `https://<داشبورد-staging>` |

ثلاثة أشياء يخطئ فيها الجميع، ولكلٍّ عطل يبدو شيئًا آخر:

1. **`/api/v1` جزء من العنوان لا زينة.** بدونه تُحمّل الداشبورد بشكل ممتاز ويردّ كل نداء `404`.
2. **`VITE_API_BASE_URL` تُقرأ وقت البناء.** تغييرها بعد النشر لا يفعل شيئًا حتى **تعيد نشر الداشبورد** — الحزمة العاملة تحمل القيمة القديمة محروقة بداخلها.
3. **`CORS_ALLOWED_ORIGINS` الفارغ يعني «لا أحد» لا «الجميع».** الكود: `process.env.CORS_ALLOWED_ORIGINS?.split(',') ?? []`. ضع **النطاق فقط** — بروتوكول ومضيف، بلا مسار وبلا شرطة في النهاية.

**والأفضل للأول** أن تستعمل مرجع Railway بدل لصق المضيف، فلا يقدم:

```
VITE_API_BASE_URL = https://${{FamilyOS.RAILWAY_PUBLIC_DOMAIN}}/api/v1
```

هذا يجعل الداشبورد **تشير فعلًا** إلى الخادم — فيرسم Railway الخطّ — ويظلّ صحيحًا لو تغيّر نطاق الخادم.

---

## ١ · حسابك على staging — تصنعه بأمر واحد

**كلمة المرور تختارها أنت ولا تصل إليّ.** الشروط التي يفرضها الخادم: **١٠ محارف على الأقل، وفيها حرف ورقم**.

### PowerShell

```powershell
$backend = "https://<خادم-staging>"
$body = @{
  email         = "you@yourdomain.com"
  password      = "<كلمة-مرور-تختارها>"
  fullName      = "اسمك"
  familyName    = "أسرتك"
  acceptedTerms = $true
} | ConvertTo-Json

Invoke-RestMethod -Uri "$backend/api/v1/auth/register" -Method Post -Body $body -ContentType 'application/json'
```

### bash

```bash
curl -s -X POST "https://<خادم-staging>/api/v1/auth/register" \
  -H 'content-type: application/json' \
  -d '{"email":"you@yourdomain.com","password":"<كلمة-مرور-تختارها>","fullName":"اسمك","familyName":"أسرتك","acceptedTerms":true}'
```

> `acceptedTerms: true` **إلزامي** — التحقّق `@Equals(true)` لا `@IsBoolean()`، فإرسال `false` يُرفض تمامًا كإغفال الحقل.
>
> `POST /auth/register` **يردّ بالملف الشخصي لا بجلسة**. الرموز تأتي من `POST /auth/login`، وهذا ما تفعله الداشبورد نفسها. فلا تقلق إن لم ترَ رمزًا في الرد.
>
> حدّ المعدّل: **٥ تسجيلات في الدقيقة** لكل عنوان IP، و**١٠ محاولات دخول**. فإن جرّبت كثيرًا وجاءك `429`، انتظر دقيقة.

ثم ادخل من صفحة الداشبورد بالبريد وكلمة المرور نفسيهما.

> **هل التسجيل مفتوح على staging؟ نعم.** توجد بوّابة تجربة محدودة تشترط دعوة مسبقة، لكن مفتاحها `pilot.enabled` **افتراضه `false`**، فهي مغلقة على قاعدة جديدة والتسجيل يمرّ. إن رأيت يومًا رفضًا بسبب «دعوة مطلوبة»، فأحدهم شغّلها في الإعدادات.

---

## ٢ · مفتاح المشغّل — للشاشات المنصّية

**ولّده على جهازك:**

```powershell
-join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Max 256) })
```
```bash
openssl rand -hex 32
```

ضع الناتج في متغيّرات **خدمة الخادم** على staging:

```
INTERNAL_ADMIN_API_KEY = <الناتج>
```

ثم اكتب **القيمة نفسها** في شاشة الفتح داخل الداشبورد حين تطلبها.

> **لا تضعه في متغيّرات الداشبورد أبدًا.** أي متغيّر يبدأ بـ`VITE_` يُحقَن في حزمة JavaScript ويُسلَّم لكل زائر. ولهذا يكتبه المشغّل وقت التشغيل.
>
> **ويُحفَظ في ذاكرة الصفحة فقط** — لا `localStorage`، ولا `sessionStorage`، ولا كوكي، ولا في الرابط، ولا حتى كخاصية في شجرة React. يموت مع الصفحة، فتعيد إدخاله بعد كل تحديث. **هذا مقصود:** مفتاح مشغّل ينجو من إغلاق التبويب هو مفتاح مسرَّب.
>
> **وبلا ضبطه، الحارس يفشل مغلقًا** — لا أحد يقرأ تلك الشاشات، ولا أنت. مفتاح غير مضبوط يجب ألّا يعني «مفتوح للجميع» أبدًا.

---

## ٣ · أي شاشة تحتاج ماذا

| الشاشة | تحتاج |
|---|---|
| `/` النظرة العامة · `/settings` · `/organization` | **الحساب فقط** — وتعرض أسرتك أنت لا غير |
| `/growth/*` (القمع، التوقّعات، البقاء، اقتصاد الوحدة، الاكتساب، الإحالة) | الحساب **+ مفتاح المشغّل** |
| `/notifications/decisions` سجل قرارات الإشعارات | الحساب **+ مفتاح المشغّل** |

---

## ٤ · محليًّا على جهازك — يعمل الآن بلا انتظار

سكربت البذرة يُنشئ ٣٠ أسرة بحسابات جاهزة:

```bash
docker compose up -d
cd apps/backend && npx prisma migrate deploy && npm run seed:demo && npm run start:dev
# نافذة أخرى
cd apps/admin-dashboard && npm run dev        # المنفذ 5173
```

| | |
|---|---|
| **البريد** | `demo.eg-01.parent1@demo-seed.invalid` |
| **كلمة المرور** | `DemoSeed!2026` |

الصيغة `demo.<slug>.parent1@demo-seed.invalid`، و`slug` من `eg-01`…`eg-15` و`sa-01`…`sa-12`. كلمة المرور نفسها للجميع.

**وهذه الحسابات لا يمكن أن تصل إلى بيئة حقيقية:** السكربت يقرأ `DATABASE_URL` **قبل إقلاع Nest** ويرفض العمل إن لم يكن المضيف `localhost` أو `127.0.0.1` أو `::1`. والنطاق `.invalid` محجوز بـRFC 2606 ولا يمكن أن يوجد فعلًا.

---

## ٥ · تحقّق بدل أن تخمّن

بعد ضبط المتغيّرين وإعادة نشر الخدمتين:

```powershell
.\scripts\deploy-doctor.ps1 https://<خادم-staging> -Key $env:INTERNAL_ADMIN_API_KEY
```

- `PASS build-identity` → البناء الحالي يردّ من هناك.
- `PASS operator-key` مع `PASS environment` → المفتاح الذي بيدك هو المفتاح الذي على الخدمة. **إن رأيت `BLOCKED operator-key` فلا تجرّب الداشبورد أصلًا** — المشكلة في المفتاح لا في متصفحك.
- `PASS schema-version` → الهجرات التسع والعشرون مطبَّقة، فلن ينكسر التسجيل عند جدول ناقص.

---

## ٦ · ما لا أعطيه، ولن أختلقه

- **حسابًا على الإنتاج.** لا أملك واحدًا، وإنشاء حساب على بيانات حيّة قرارك أنت.
- **`INTERNAL_ADMIN_API_KEY`.** لا أعرفه ولا يجب أن أعرفه. وإن كنت قد ضبطته سابقًا، **لا ترسله إليّ** — لا في رسالة ولا في لقطة شاشة.
- **كلمة مرور من عندي.** كلمة مرور يقترحها طرف آخر ليست كلمة مرورك.

> **وحساب `test1@example.com`** — إن كان ما زال موجودًا على أي بيئة حقيقية من تجارب سابقة، فغيّر كلمة مروره أو احذفه. حساب اختبار بكلمة مرور معروفة على مضيف عام هو باب مفتوح، لا أثر تجربة.
