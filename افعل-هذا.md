# افعل هذا — خطوة واحدة، وينتهي الأمر

**٢٠٢٦-٠٨-٢١ · ١١:٥٨**

---

## هذه ليست المرة العاشرة

التسع السابقة كانت **فشلًا أعمى**: `Error: P3005`، ثم `Stopping Container`، ولا شيء آخر.

هذه المرة **رفض مُسبَّب**، وفيه رقمان لم يكونا متاحين من قبل:

```
probe: {"tenantIsolationPolicies":0, "tablesWithRowLevelSecurity":0,
        "baseTables":57, "migrationLedgerPresent":false}
```

**والفرق بين الاثنين هو كل الفرق.** لأول مرة نعرف حالة قاعدة الإنتاج بدقة، والجواب واضح جدًّا.

---

## ما تقوله الأرقام

| القياس | قاعدة الإنتاج | ما يحتاجه الكود |
|---|---|---|
| عدد الجداول | **٥٧** | **١٠١** |
| سياسات `tenant_isolation` | **٠** | ٥٦ |
| سجلّ الهجرات | **غائب** | موجود |

**قاعدة الإنتاج متأخّرة بأربعة وأربعين جدولًا، وليس فيها ولا سياسة عزل واحدة.**

أُنشئت مرة واحدة بـ`prisma db push` من مخطّط قديم جدًّا، ولم يمرّ عليها SQL أي هجرة قطّ. ولهذا لم ينجح شيء ولن ينجح: **لا يوجد إصلاح تدريجي لقاعدة ينقصها نصف جداولها.** الهجرات لا يمكن أن تُطبَّق فوقها، والبَصْل كان سيكون كذبًا مدمّرًا — وهذا ما رفضه السكربت.

> ولاحظ: `‏/health/ready` على الإنتاج كان يقول `{"status":"ok","database":true}` طوال هذا الوقت. لأنه يسأل «هل تردّ Postgres على `SELECT 1`؟» — وقاعدة ينقصها ٤٤ جدولًا تجيب بنعم.

---

## الحل — ولا يتضمّن حذف أي شيء

**لا تُصفّر القاعدة القديمة. اتركها كما هي، واصنع بجانبها قاعدة جديدة فارغة.**

في Railway، على بيئة **`production`**:

### ١ · أضف قاعدة جديدة

`+ New` ← `Database` ← `Add PostgreSQL`
(ستظهر باسم مثل `Postgres-xxxx` بجوار القديمة)

### ٢ · وجّه الخادم إليها

خدمة `FamilyOS` ← `Variables` ← عدّل `DATABASE_URL` إلى مرجع القاعدة الجديدة:

```
${{Postgres-xxxx.DATABASE_URL}}
```

استبدل `Postgres-xxxx` باسم الخدمة الجديدة كما ظهر لك. **استعمل المرجع لا النصّ المنسوخ**، حتى يظلّ صحيحًا لو تغيّر شيء.

### ٣ · أعد النشر

`Deployments` ← `Redeploy`

---

## ما سيحدث حينها

القاعدة الجديدة فارغة تمامًا، فـ`migrate deploy` لن يرى `P3005` أصلًا — يراها بيضاء ويبنيها من الصفر:

```
[predeploy] === ABNY RELEASE STEP ===
Applying migration `0001_init`
Applying migration `0002_rewards_integrity_constraints`
…
Applying migration `0030_retire_duplicate_daily_goal_reward_rules`
[predeploy] migrate deploy succeeded. Nothing else to do.
```

**٢٩ هجرة ← ١٠١ جدول ← ٥٦ سياسة عزل ← سجلّ تكتبه Prisma بنفسها.**

وبعدها لا يمكن أن يعود `P3005` إلى هذه القاعدة أبدًا، لأن السجلّ صار موجودًا.

**والقاعدة القديمة تبقى مكانها، على حالها، بكل ما فيها.** إن اكتشفتَ لاحقًا أن فيها ما يهمّ، فهي موجودة. **لا يوجد في هذا الحلّ أمر حذف واحد** — ولهذا اخترتُه على `DROP SCHEMA`.

---

## «وهل فيها بيانات أصلًا؟»

الأرجح لا: ٥٧ جدولًا، صفر سياسات، وقائمة رايات الميزات فارغة — هذه بصمة قاعدة أُنشئت مرة ولم تُستعمل. لكنني **لا أستطيع الجزم**، فلا أطلب منك حذفًا بناءً على ترجيح. والخطوات الثلاث أعلاه لا تحتاج إلى الجواب أصلًا.

**وحتى لا يتكرّر هذا السؤال بلا جواب:** الحزمة المرفقة تضيف عدّ الصفوف إلى المِسبار، فيطبع سجلّ النشر من الآن فصاعدًا أحد ثلاثة أسطر:

```
[predeploy] contents: 0 families, 0 users — THIS DATABASE HOLDS NO HOUSEHOLDS.
[predeploy] contents: 12 families, 19 users — THIS DATABASE HOLDS REAL DATA. Do not reset it.
[predeploy] contents: the families/users tables do not exist in this schema at all.
```

«الجدول غير موجود» **لا يُختصر إلى صفر** — لأن الصفر يُقرأ «آمن للحذف» عن قاعدة لم يقسها أحد.

**وهذه الحزمة اختيارية تمامًا.** إن أردتَ إنهاء الأمر الآن، نفّذ الخطوات الثلاث فحسب — القرار لا يحتاجها.

---

## بعد النشر

```powershell
.\scripts\deploy-doctor.ps1 https://familyos-production-74ca.up.railway.app -Key $env:INTERNAL_ADMIN_API_KEY
```

المطلوب:

```
PASS  build-identity   the operator surface is CLOSED
PASS  schema-version   29 migrations applied, latest 0030_… — matches this repository exactly
DEPLOY VERIFIED
```

> واضبط `INTERNAL_ADMIN_API_KEY` على خدمة الإنتاج قبل النشر — بدونه يفشل الحارس مغلقًا فلا تقرأ التشخيص أنت أيضًا. ولوحة المشغّل هناك ما زالت مقروءة من الإنترنت حتى ينجح هذا النشر.

---

**التسع السابقة لم تكن عبثًا:** كل واحدة أزالت سببًا حقيقيًّا — سياق البناء، ثم الملفات القديمة، ثم التصريف، ثم `P3005` العمياء. وما بقي الآن ليس عطلًا في الكود ولا في الإعداد: **قاعدة بيانات لا تصلح للكود الذي يعمل فوقها.** وحلّها ثلاث نقرات.
