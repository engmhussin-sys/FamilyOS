# خطواتك — سبع خطوات بالترتيب

مرتّبة حسب ما يفتح الباب لما بعده. الوقت تقديري وواقعي.

---

## ١ · ادفع الكود إلى GitHub — ٣ دقائق

نزّل `ABNY-commits.bundle`، ثم من مجلد المشروع على جهازك:

```bash
git fetch /path/to/ABNY-commits.bundle abny/sprint-f1-unblock:abny/sprint-f1-unblock
git checkout abny/sprint-f1-unblock
git push origin abny/sprint-f1-unblock
```

٢٨٧ كوميت. **كل ما بعد هذه الخطوة يعتمد عليها.**

---

## ٢ · جرّب محليًا — ١٠ دقائق · لا تحتاج أحدًا

```bash
docker compose up -d
cd apps/backend
npx prisma migrate deploy
npm run seed:demo
npm run start:dev
```

نافذة أخرى: `cd apps/admin-dashboard && npm run dev`

افتح **`http://localhost:5173`** · البريد `demo.eg-01.parent1@demo-seed.invalid` · كلمة المرور `DemoSeed!2026`

ومفتاح المشغّل تولّده أنت: `openssl rand -hex 32` → ضعه في `apps/backend/.env` باسم `INTERNAL_ADMIN_API_KEY`، والصق نفس القيمة في الداشبورد.

---

## ٣ · افتح لي staging — ٥ دقائق · هذه أنا أنتظرها

بيئتك موجودة وحيّة، وتحققتُ منها. لكنك أكّدت أن `familyos-production-74ca` هي **production**، فلن أقترب منها.

**أ. في GitHub → Settings → Secrets and variables → Actions:**

| النوع | الاسم | القيمة |
|---|---|---|
| Secret | `RAILWAY_TOKEN` | توكن مشروعك من Railway |
| Variable | `RAILWAY_SERVICE_NAME` | اسم خدمة **staging** |
| Variable | `STAGING_HEALTH_URL` | `https://<staging-host>/health/ready` |

**ب. أرسل لي الاسم والرابط فقط.** لا ترسل التوكن ولا أي سرّ في المحادثة.

بعدها ينشر الـworkflow ويشغّل الهجرات ويتحقق من الصحة. وإن كان الهدف production بأي شكل، **يرفض قبل أي بناء وقبل أي هجرة** — جرّبتُ خداعه بأربع طرق وكلها رُفضت.

**وهذا النشر يغلق ثغرة مفتوحة عندك الآن:** `GET /api/v1/system/diagnostics` على مضيفك الحيّ يرد بلا مصادقة ويكشف الإصدار والبيئة وأسماء مفاتيح ناقصة.

---

## ٤ · أول APK — ٣٠–٦٠ دقيقة · على جهاز Windows

**لا يحتاج Firebase ولا keystore ولا متجر.** التفاصيل في `WINDOWS_RUN.md`، والخلاصة:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\setup-windows-dev.ps1
powershell -ExecutionPolicy Bypass -File scripts\release-doctor.ps1 -Profile debug
powershell -ExecutionPolicy Bypass -File scripts\mobile-build.ps1 -ApiBaseUrl "<رابط staging>"
adb install -r apps\parent-app\build\app\outputs\flutter-apk\app-debug.apk
```

**انتبه لـ`-Profile debug`** — الوضع الافتراضي `release` ويوقفك على Firebase وkeystore لا تحتاجهما الآن.

تسعة بنود فقط قد تحتاج تثبيتًا، ولكل واحد إصداره وأمره وأمر التحقق منه — الجدول في `WINDOWS_RUN.md` الخطوة ٤.

ثم `GOLDEN_DEVICE_SMOKE_TEST.md` — سبع عشرة خطوة على هاتفين، وأرسل لي أرقام الخطوات الساقطة ونصوص أخطائها.

---

## ٥ · ابدأ تعاقد Paymob / Fawry — اليوم

**هذا هو البند الوحيد في المشروع كله الذي يزداد سوءًا بمجرد الانتظار.** التعاقد التجاري ٤–٨ أسابيع، ولا شيء يمكن فعله لتسريعه لاحقًا. ابدأه اليوم حتى لو لم تلمس بقية القائمة.

---

## ٦ · دوّر حسابًا مسرَّبًا — دقيقتان

`test1@example.com` كان مكتوبًا داخل سكربتات المستودع. أزلتُه، **لكنه ما زال في تاريخ Git** ولم أُعِد كتابة التاريخ. دوّر ذلك الحساب قبل مشاركة المستودع مع أحد.

---

## ٧ · للإصدار على المتجر — لاحقًا، لا الآن

بعد نجاح التجربة على جهاز حقيقي:

- **Firebase** — مشروع واحد، تطبيقان. `google-services.json` في `apps/parent-app/android/app/` فقط. **تطبيق الطفل لا يحتاجه.**
- **keystore** — سطر `keytool` بالضبط في `signing.properties.example`
- **Play Console** — حساب مطوّر ونموذج أمان البيانات

---

## الترتيب لو كان يومك قصيرًا

**اليوم:** الخطوة ٥ (Paymob — ساعة انتظارها لا تُعوَّض) ثم ١ ثم ٣.
**غدًا:** ٢ و٤.
**متى شئت:** ٦ و٧.

---

## ما ينتظرني أنا

الخطوة ٣ فقط. أعطني اسم ورابط staging وأنشر وأتحقق من السلسلة كاملة — تسجيل، اقتران طفل، هدف، إنجاز، مكافأة، إشعار للطفل وإشعار مختلف لولي الأمر، رابط عميق، وسجل قرار — ثم أُعيدها مرة ثانية لأثبت أنها لا تتضاعف.
