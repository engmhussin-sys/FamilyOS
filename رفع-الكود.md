# رفع الكود عبر GitHub Desktop

ملف `FamilyOS-code.zip` — **٥ ميجابايت · ٢٠٧١ ملفًا** — مرتّب بنفس شكل مجلدك تمامًا:

```
.github/    apps/    docs/    packages/    scripts/
.gitignore    docker-compose.yml    README.md
render.yaml    .env.staging.example
+ ملفات التوثيق (SHIP_BOARD, WINDOWS_RUN, دليلك-التفصيلي …)
```

> **تغيّر منذ ٢٠٢٦-٠٨-٢١:** `railway.json` و`.dockerignore` لم يعودا في الجذر — انتقلا إلى `apps/backend/` ليكون كل قابل للنشر مستقلًّا بذاته. السبب في `عطل-البناء.md`.

**ما ليس بداخله عمدًا:** `node_modules` و`prisma-client-generated` و`dist` و`.env` — لأنها لا تُرفع إلى Git أصلًا. **ستبقى في مجلدك كما هي ولن يمسّها الفكّ.**

---

## ⚠️ اقرأ هذا أولًا — ١٣ ملفًا يجب حذفها

فكّ الضغط **يضيف ويستبدل**، لكنه **لا يحذف**. وفي هذا التحديث حُذف ١٣ ملفًا — أهمها ملفات دفع قديمة استُبدلت بأخرى، وملف `age.ts` الذي كان يحسب عمر الطفل بساعة الخادم بدل تقويم الأسرة.

**لو تركتَها، سيفشل البناء** — يوجد اختبار يرفض وجود أكثر من طريقة واحدة لحساب عمر الطفل.

---

## الخطوات

### ١ · افتح PowerShell داخل مجلد المشروع

في مستكشف الملفات داخل مجلد `FamilyOS`: اضغط على شريط المسار في الأعلى، اكتب `powershell` واضغط Enter.

### ٢ · احذف الـ١٣ ملفًا — أمر واحد

انسخ هذا كما هو والصقه:

```powershell
@(
 "apps\backend\prisma\migrations\20260731_life_intelligence_platform_sprint13\migration.sql",
 "apps\backend\src\common\utils\age.ts",
 "apps\backend\src\modules\auth\application\services\pairing.service.ts",
 "apps\backend\src\modules\auth\infrastructure\repositories\prisma-device.repository.ts",
 "apps\backend\src\modules\auth\presentation\controllers\device-pairing.controller.ts",
 "apps\backend\src\modules\auth\presentation\dto\confirm-pairing.dto.ts",
 "apps\backend\src\modules\auth\presentation\dto\initiate-pairing.dto.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\apple-iap.adapter.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\fawry.adapter.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\google-play-billing.adapter.ts",
 "apps\backend\src\modules\billing\infrastructure\adapters\paymob.adapter.ts",
 "apps\backend\test\auth\pairing.service.spec.ts",
 "apps\backend\test\common\age.spec.ts"
) | ForEach-Object { if (Test-Path $_) { Remove-Item $_ -Force; "حُذف: $_" } else { "غير موجود (لا بأس): $_" } }
```

ثم احذف مجلدًا واحدًا فارغًا إن بقي:

```powershell
Remove-Item "apps\backend\prisma\migrations\20260731_life_intelligence_platform_sprint13" -Recurse -Force -ErrorAction SilentlyContinue
```

### ٣ · فُكّ الضغط فوق المجلد

انقر يمينًا على `FamilyOS-code.zip` → **Extract All** → اختر مسار مجلد `FamilyOS` نفسه → **Extract**.

Windows سيسألك عن الملفات المتشابهة: اختر **Replace the files in the destination**.

> **مهم:** بعض برامج فكّ الضغط تنشئ مجلدًا باسم الملف داخل الوجهة. لو حدث ذلك ووجدتَ `FamilyOS\FamilyOS-code\apps`، فانقل محتويات `FamilyOS-code` إلى `FamilyOS` ثم احذف المجلد الفارغ. **الشكل الصحيح: `apps` مباشرةً داخل `FamilyOS`.**

### ٤ · افتح GitHub Desktop

سيعرض قائمة طويلة بالتغييرات — ملفات مضافة ومعدّلة، **و١٣ ملفًا محذوفًا** (بعلامة حمراء). وجودها في القائمة هو الدليل على أن الخطوة ٢ نجحت.

### ٥ · أنشئ فرعًا جديدًا — لا تعمل على `main`

من الأعلى: **Current Branch → New Branch** → سمِّه:

```
abny/sprint-f1-unblock
```

اختر **Bring my changes to the new branch**.

**لماذا فرع منفصل؟** حتى يبقى `main` سليمًا كما هو. لو ظهرت مشكلة في التجربة، ترجع إليه بضغطة واحدة.

### ٦ · اكتب وصفًا وارفع

في الخانة السفلية اليسرى:

**Summary:** `ABNY — code freeze, staging deploy config, duplication fixes`

اضغط **Commit to abny/sprint-f1-unblock**، ثم **Publish branch** في الأعلى.

---

## ٧ · تحقق أن كل شيء وصل

بعد الرفع، في PowerShell داخل المجلد:

```powershell
cd apps\backend
npm install
npx prisma generate
npx tsc --noEmit
```

> **`npx prisma generate` ليست اختيارية.** لا يوجد `postinstall` في المشروع، فـ`npm install` **لا** يولّد عميل Prisma. وبدونه لا يرى TypeScript أنواع الجداول، فيطبع **نحو ٢٩٠ خطأ في ٤٧ ملفًا** — كلها في `prisma-*.repository.ts` — سببها واحد وأمرها واحد، وليست أعطالًا في الكود. صورة Docker تنجح رغم ذلك لأنها تشغّل `prisma generate` قبل التصريف.

`npm install` قد يأخذ دقيقتين. `npx tsc --noEmit` يجب أن ينتهي **بلا أي مخرجات** — وهذا يعني نجاحًا تامًا.

**لو ظهر خطأ يذكر `age.ts` أو `calculateAge`،** فمعناه أن الخطوة ٢ لم تُنفَّذ. أعِدها.

---

## ملاحظة عن الفرق بين هذا الملف والحزمة

| | ZIP | Bundle |
|---|---|---|
| الاستخدام | GitHub Desktop | سطر الأوامر |
| ما يحمله | الملفات بشكلها النهائي | **٢٨٩ كوميت** بتاريخها كاملًا |
| السجل | كوميت واحد يجمع كل شيء | كل تعديل بسبب ورسالة منفصلة |

الاثنان يعطيان **نفس الكود بالضبط**. الفرق أن الحزمة تحفظ التاريخ — من غيّر ماذا ولماذا. لو أردتَ ذلك لاحقًا فالحزمة ما زالت صالحة، وأمرها في `دليلك-التفصيلي.md` الخطوة ١.

للتجربة السريعة، **الـZIP يكفي تمامًا.**
