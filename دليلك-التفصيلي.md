# دليلك التفصيلي — خطوة بخطوة

كل خطوة فيها: **ماذا تفعل · لماذا · الأوامر · ماذا سترى إن نجحت · ماذا تفعل إن فشلت.**

لستَ مضطرًا لفهم ما بداخل الكود. كل ما تحتاجه هنا.

---

# الخطوة ١ — ادفع الكود إلى GitHub

⏱️ ثلاث دقائق · 🔑 تحتاج: حساب GitHub وصلاحية الكتابة على المستودع

### لماذا هذه أولًا؟

كل الكود الذي كُتب موجود في ملف واحد اسمه `ABNY-commits.bundle`. تخيّله **حقيبة مغلقة فيها ٢٨٨ تعديل** مرتّبة بترتيبها الزمني. حتى تُفتح الحقيبة داخل مستودعك على GitHub، لا يستطيع أحد — لا أنت ولا GitHub Actions ولا Railway — أن يرى شيئًا منها.

### الأوامر

نزّل `ABNY-commits.bundle` وضعه في مكان تعرفه، مثلًا سطح المكتب. ثم افتح الطرفية **داخل مجلد مشروعك** (المجلد الذي فيه `.git`):

```bash
git fetch ~/Desktop/ABNY-commits.bundle abny/sprint-f1-unblock:abny/sprint-f1-unblock
```

هذا يفتح الحقيبة ويضع محتواها في فرع جديد اسمه `abny/sprint-f1-unblock`. **لم يتغيّر شيء في فرعك الحالي.**

```bash
git checkout abny/sprint-f1-unblock
```

انتقل إلى الفرع الجديد.

```bash
git push origin abny/sprint-f1-unblock
```

ارفعه إلى GitHub.

### ماذا سترى إن نجح

سطر أخير يشبه:

```
 * [new branch]      abny/sprint-f1-unblock -> abny/sprint-f1-unblock
```

وفي GitHub، عند فتح المستودع، ستجد شريطًا أعلى الصفحة يقول إن فرعًا جديدًا وصل، مع زر «Compare & pull request».

### إن فشل

| الرسالة | المعنى | الحل |
|---|---|---|
| `does not appear to be a git repository` | مسار الحقيبة غلط | تأكد من مكان الملف واكتب المسار كاملًا |
| `Permission denied` أو `403` | ليست لك صلاحية الكتابة | تحقق من صلاحياتك على المستودع في GitHub |
| `not a git repository` | أنت خارج مجلد المشروع | ادخل المجلد الذي فيه `.git` أولًا |

**لا تدمج هذا الفرع في `main` الآن.** اتركه فرعًا مستقلًا حتى تنتهي التجربة.

---

# الخطوة ٢ — جرّب المشروع على جهازك

⏱️ عشر دقائق · 🔑 تحتاج: Docker وNode.js مثبَّتين · **لا تحتاج إنترنت للبيانات ولا حسابًا من أحد**

### لماذا؟

قبل أي نشر، تريد أن ترى المنتج بعينك: الداشبورد، الأسر، الأطفال، الاشتراكات، الأرقام. هذه الخطوة تعطيك ذلك خلال عشر دقائق ولا تعتمد على أي طرف خارجي.

---

## ٢-أ · شغّل قاعدة البيانات

```bash
docker compose up -d
```

**ما الذي حدث؟** شغّل Docker حاويتين على جهازك:

- **PostgreSQL** على المنفذ `5432` — قاعدة البيانات، حيث تُخزَّن الأسر والأطفال والأهداف والمكافآت
- **Redis** على المنفذ `6379` — ذاكرة سريعة مؤقتة يستخدمها الخادم للطوابير والعدّادات

`-d` تعني «اعمل في الخلفية» فتستعيد الطرفية.

**تحقق:**

```bash
docker compose ps
```

يجب أن ترى الاثنين بحالة `running` أو `healthy`.

---

## ٢-ب · اضبط ملف الإعدادات — **لا تتخطَّ هذه**

الخادم **يرفض الإقلاع** بلا إعدادات صحيحة، وهذا مقصود لا خلل: خادم يقلع بأسرار ناقصة هو خادم يبدو سليمًا وهو ليس كذلك.

```bash
cd apps/backend
cp .env.example .env
```

الآن ولّد سرّين مختلفين:

```bash
openssl rand -hex 32
openssl rand -hex 32
```

سيظهر لك سطران طويلان من حروف وأرقام. افتح ملف `.env` بأي محرر، وضع **الأول** في `JWT_ACCESS_SECRET` و**الثاني** في `JWT_REFRESH_SECRET`.

```
JWT_ACCESS_SECRET="<السطر الأول>"
JWT_REFRESH_SECRET="<السطر الثاني>"
```

**لماذا سرّان مختلفان؟** الأول يوقّع تذكرة الدخول القصيرة (دقائق)، والثاني يوقّع تذكرة التجديد الطويلة (أيام). لو كانا واحدًا، لصار بإمكان من يسرق التذكرة القصيرة أن يصنع تذكرة تجديد — والخادم يرفض الإقلاع إن تطابقا.

وولّد مفتاحًا ثالثًا بصيغة مختلفة:

```bash
openssl rand -base64 32
```

ضعه في `LOCATION_ENCRYPTION_KEY`.

وتأكد أن هذا السطر موجود كما هو:

```
CORS_ALLOWED_ORIGINS="http://localhost:5173"
```

**ما CORS؟** قاعدة أمان في المتصفح: الخادم يحدّد أي المواقع يُسمح لها بمخاطبته. `5173` هو منفذ الداشبورد. لو تركتَه فارغًا، **لن يُسمح لأي موقع** — والداشبورد سيبدو معطّلًا وهو سليم.

---

## ٢-ج · ابنِ جداول قاعدة البيانات

```bash
npx prisma migrate deploy
```

**ما الذي حدث؟** قاعدة البيانات كانت فارغة تمامًا. هذا الأمر نفّذ ٢٩ ملف هجرة بالترتيب، كل ملف يضيف جداول وفهارس وقيودًا. النتيجة: **١٠١ جدول جاهز**.

**سترى:** قائمة بأسماء الهجرات ثم `All migrations have been successfully applied.`

---

## ٢-د · املأها ببيانات واقعية

```bash
npm run seed:demo
```

**ما الذي حدث؟** أُنشئت ٣٠ أسرة (١٥ مصرية · ١٢ سعودية · ٣ بلا بلد)، و٤٥ طفلًا في كل الفئات العمرية، و٢١ اشتراكًا في كل حالات دورة الحياة، و**٩٨ يومًا من التاريخ** حتى تظهر الاتجاهات والرسوم البيانية بدل شاشات فارغة.

**المهم:** هذه البيانات مرّت عبر **الخدمات الحقيقية** لا بحقن صفوف في الجدول. أي أنها تحترم نفس القيود التي يحترمها الإنتاج — فما تراه هو سلوك حقيقي.

**وأمان:** السكربت **يرفض العمل** على أي قاعدة بيانات ليست على جهازك. لا يمكن تشغيله بالخطأ على بيئة حقيقية.

---

## ٢-هـ · شغّل الخادم

```bash
npm run start:dev
```

**اترك هذه النافذة مفتوحة.** الخادم يعمل فيها.

**سترى:** أسطر إقلاع تنتهي بشيء مثل `Nest application successfully started` و`listening on port 3000`.

**تحقق سريع** من نافذة أخرى:

```bash
curl http://localhost:3000/health/ready
```

الجواب المطلوب:

```json
{"status":"ok","database":true,"redis":true}
```

`database: true` تعني أن الخادم يرى PostgreSQL، و`redis: true` أنه يرى Redis. **لو رأيت هذا، الخادم سليم تمامًا.**

---

## ٢-و · شغّل الداشبورد

نافذة **ثالثة**:

```bash
cd apps/admin-dashboard
cp .env.example .env.local
```

افتح `.env.local` وتأكد أن السطر:

```
VITE_API_BASE_URL=http://localhost:3000/api/v1
```

**انتبه لـ`/api/v1` في آخره.** بدونها ستفشل كل الطلبات — وكان هذا خطأً حقيقيًا في التوثيق أُصلح.

```bash
npm run dev
```

---

## ٢-ز · ادخل

افتح المتصفح على:

### 🔗 `http://localhost:5173`

| | |
|---|---|
| **البريد** | `demo.eg-01.parent1@demo-seed.invalid` |
| **كلمة المرور** | `DemoSeed!2026` |

`eg-01` تعني الأسرة المصرية الأولى. جرّب `eg-01` حتى `eg-15`، و`sa-01` حتى `sa-12` للسعودية. **كلمة المرور واحدة للجميع** لأنها بلا قيمة خارج جهازك.

---

## ٢-ح · مفتاح المشغّل — لشاشات المنصّة

بعض الشاشات (النمو، الاشتراكات، سجل قرارات الإشعارات) تعرض بيانات **كل الأسر** لا أسرتك. هذه ليست شاشات ولي أمر، بل شاشات مالك المنصّة — فتقف خلف حاجز منفصل.

```bash
openssl rand -hex 32
```

ضع الناتج في `apps/backend/.env`:

```
INTERNAL_ADMIN_API_KEY=<الناتج>
```

**أعد تشغيل الخادم** (`Ctrl+C` ثم `npm run start:dev`)، ثم الصق **القيمة نفسها** في الداشبورد حين يطلبها.

**سيطلبها منك بعد كل تحديث للصفحة — وهذا مقصود.** المفتاح لا يُحفظ في المتصفح إطلاقًا: لا في `localStorage` ولا في كوكي ولا في الرابط. مفتاح مشغّل ينجو من إغلاق التبويب هو مفتاح يمكن سرقته من الجهاز.

---

### إن فشل شيء في الخطوة ٢

| العَرَض | السبب الغالب | الحل |
|---|---|---|
| الخادم لا يقلع ويشكو من `JWT` | السرّان ناقصان أو متطابقان أو أقصر من ٣٢ حرفًا | ولّدهما من جديد وتأكد أنهما مختلفان |
| `ECONNREFUSED` على 5432 | Docker لم يعمل | `docker compose ps` ثم `docker compose up -d` |
| الداشبورد يفتح لكن كل لوحة تعطي خطأ | `VITE_API_BASE_URL` غلط — وأشهر خطأ هو نسيان `/api/v1` | صحّحها ثم **أعد تشغيل** `npm run dev` (Vite يقرأ الملف عند الإقلاع فقط) |
| الداشبورد يفتح والشاشات فارغة | لم تشغّل `seed:demo` | شغّله |
| «غير مُقاس» في بعض اللوحات | **ليس خطأً** — هذه لوحات لا يوجد لها مصدر بيانات بعد، وتقول ذلك بصراحة بدل أن تعرض صفرًا كاذبًا | لا شيء |

---

# الخطوة ٣ — افتح لي بيئة staging

⏱️ خمس دقائق · 🔑 تحتاج: حساب Railway وGitHub

### الوضع الحالي

بحثتُ في تاريخ المستودع فوجدتُ بيئتك، وتحققتُ منها **حيّةً**:

```
https://familyos-production-74ca.up.railway.app
  /health/live   →  {"status":"ok"}
  /health/ready  →  {"status":"ok","database":true,"redis":true}
```

بنيتك موجودة وتعمل. لكنك أكّدت أن هذا المضيف هو **production** — **فلن أقترب منه**، وسأنشر على staging فقط.

---

## ٣-أ · احصل على توكن Railway

في Railway: **Account Settings → Tokens → Create Token**. سمّه مثلًا `github-deploy`.

**انسخه فورًا — لن يظهر مرة أخرى.**

## ٣-ب · احصل على اسم خدمة staging ورابطها

في مشروعك على Railway، افتح خدمة **staging**:

- **الاسم** مكتوب أعلى الصفحة
- **الرابط** في تبويب **Settings → Networking → Public Networking**

الشكل عادةً: `https://<something>.up.railway.app`

## ٣-ج · ضعها في GitHub

في مستودعك: **Settings → Secrets and variables → Actions**. ستجد تبويبين — `Secrets` و`Variables`. **التبويب يهمّ:**

**في تبويب Secrets** → New repository secret:

| الاسم | القيمة |
|---|---|
| `RAILWAY_TOKEN` | التوكن من ٣-أ |

**في تبويب Variables** → New repository variable:

| الاسم | القيمة |
|---|---|
| `RAILWAY_SERVICE_NAME` | اسم خدمة staging |
| `STAGING_HEALTH_URL` | `https://<staging-host>/health/ready` |

**لماذا التوكن Secret والباقي Variables؟** الـSecret مشفَّر ولا يظهر أبدًا في السجلات حتى لو حاول أحد طباعته. الاسم والرابط ليسا سرًّا، ووضعهما كـVariables يجعلهما مقروءين في سجل التشغيل — وهذا مفيد حين تريد التأكد أين نشرتَ.

## ٣-د · أرسل لي الاسم والرابط

**فقط الاسم والرابط.** لا ترسل التوكن ولا أي سرّ في المحادثة — لا أحتاجه، وما يُكتب في محادثة لا يُمحى بسهولة.

---

### القفل الذي وضعتُه من أجلك

الـworkflow **يرفض العمل** إن كان الهدف production — قبل أي بناء وقبل أي هجرة على قاعدة البيانات. يقارن المضيف واسم الخدمة معًا، ويشترط أن يتفقا على هدف واحد ليس production.

جرّبتُ خداعه بأربع طرق:

| المحاولة | النتيجة |
|---|---|
| متغيّر فارغ | **رُفض** — الفراغ لا يُقرأ كـ«ليس production» |
| شرطة مائلة أخيرة أو `:443` أو نقطة | **رُفض** — يُطبّع الرابط قبل المقارنة |
| أحرف كبيرة | **رُفض** |
| إعادة تسمية الخدمة | **رُفض** — الرابط ما زال يشي بالهدف |

وللنشر على production يومًا ما يوجد مخرج **متعمَّد الإزعاج**: عبارة كاملة تُكتب يدويًا. لا يمكن أن يحدث صامتًا.

### وثغرة مفتوحة عندك الآن

`GET /api/v1/system/diagnostics` على مضيفك الحيّ **يرد بلا مصادقة** ويكشف الإصدار والبيئة ومدة التشغيل وأسماء مفاتيح اعتماد ناقصة. أي شخص يعرف الرابط يراها. أُغلقت في الكوميت `06f0a5a` ضمن الـ٢٨٨، **لكن البناء المنشور متأخر ٢٨٨ كوميت فالثغرة مفتوحة.** أول نشر يغلقها.

---

# الخطوة ٤ — أول APK على هاتف حقيقي

⏱️ ٣٠–٦٠ دقيقة · 🔑 تحتاج: جهاز Windows وهاتف أندرويد (والأفضل هاتفان)

### لماذا هذه الخطوة بالذات؟

**لم يُصرَّف كود التطبيقين ولا مرة واحدة.** كل ما لديّ من أدلة عليهما ثابت (تسعة فاحصات تقرأ الكود ولا تشغّله)، لأن بيئتي لا تحتوي على أدوات Flutter. أول تصريف حقيقي سيحدث عندك — فتوقّع ملاحظة أو اثنتين من المحلّل. هذا طبيعي وليس دليل خلل.

**والخبر الجيد: أول APK تجريبي لا يحتاج Firebase ولا keystore ولا حساب متجر.**

---

## ٤-أ · ثبّت الأدوات

```powershell
powershell -ExecutionPolicy Bypass -File scripts\setup-windows-dev.ps1
```

يثبّت Flutter وAndroid SDK وJDK 17 بالإصدارات المثبَّتة في المشروع.

**بعده: أغلق الطرفية وافتحها من جديد** — وإلا لن يرى النظام الأدوات الجديدة.

## ٤-ب · افحص البيئة

```powershell
powershell -ExecutionPolicy Bypass -File scripts\release-doctor.ps1 -Profile debug
```

**`-Profile debug` مهمة جدًا.** الوضع الافتراضي `release` يفحص استعدادك لنسخة المتجر، فيوقفك على Firebase وkeystore **لا تحتاجهما الآن**. الوضع `debug` يفحص ما يلزم لأول APK فقط.

كل فحص يطبع واحدة من ثلاث:

- **PASS** — سليم
- **WARN** — ملاحظة، **لا توقفك**
- **BLOCKED** — يجب إصلاحه

إن وُجد `BLOCKED` واحد، ينتهي التشغيل بسطر `SHIP BLOCKED`.

## ٤-ج · أغلق كل BLOCKED

من ٣١ فحصًا، **تسعة فقط** قد تحتاج تثبيتًا. الجدول الكامل — بالإصدار والمصدر وأمر التثبيت وأمر التحقق — في `WINDOWS_RUN.md` الخطوة ٤.

**لا تثبّت شيئًا خارج ذلك الجدول.** لو أوقفك فحص ليس فيه، فهو من فحوص المستودع العشرة (`gradle-wrapper`، `sdk-levels`، `permissions`…) وهي تمرّ اليوم — أرسل لي السطر بدل أن تعدّل.

أعد ٤-ب حتى يختفي `SHIP BLOCKED`.

## ٤-د · شغّل الخادم وخذ عنوان جهازك

الهاتف **لا يستطيع** الوصول إلى `localhost` — لأن `localhost` عند الهاتف يعني الهاتف نفسه.

```powershell
docker compose up -d
cd apps\backend
npm run start:dev
ipconfig
```

من مخرجات `ipconfig` خذ `IPv4 Address` الخاص بكرت الواي فاي، مثل `192.168.1.20`.

**الهاتفان والحاسوب يجب أن يكونوا على نفس شبكة الواي فاي.**

> لو كانت staging جاهزة (الخطوة ٣)، استخدم رابطها بدل عنوان جهازك — أفضل، لأنه يختبر الشبكة الحقيقية.

## ٤-هـ · ابنِ

```powershell
powershell -ExecutionPolicy Bypass -File scripts\mobile-build.ps1 -ApiBaseUrl "http://192.168.1.20:3000"
```

بلا `-Release`. النتيجة:

```
apps\parent-app\build\app\outputs\flutter-apk\app-debug.apk
apps\child-app\build\app\outputs\flutter-apk\app-debug.apk
```

**إن فشل البناء:** سجلّ البناء يسمّي الملف والسطر. **أرسل لي ذلك السطر ولا تعدّل الكود** — الكود مجمَّد وخطأ التصريف إصلاحه صغير وموضعي.

## ٤-و · ثبّت على الهاتفين

فعّل **خيارات المطوّر** ثم **تصحيح USB** في كل هاتف، ووصّلهما.

```powershell
adb devices
```

يجب أن ترى معرّفين. ثم:

```powershell
adb -s <معرّف-هاتف-الأب> install -r apps\parent-app\build\app\outputs\flutter-apk\app-debug.apk
adb -s <معرّف-هاتف-الطفل> install -r apps\child-app\build\app\outputs\flutter-apk\app-debug.apk
```

`-r` تعني «استبدل إن كان مثبَّتًا». **لا تعتبر التثبيت ناجحًا حتى يقول `adb` كلمة `Success`.**

## ٤-ز · الاختبار الذهبي

`GOLDEN_DEVICE_SMOKE_TEST.md` — سبع عشرة خطوة مرتّبة: تشغيل، عربية وRTL، تسجيل، إضافة طفل، اقتران، هدف، إنجاز، تحقق، مكافأة، دليل، إشعار، نقرة إشعار، رابط عميق من تطبيق مغلق، رفض أمان، إعادة تشغيل.

**اقطع الاختبار فقط لخمسة أشياء:** انهيار، ثغرة صلاحيات (طفل يصل لشاشة ولي أمر أو يوافق على شيء)، فشل أمان طفل، ضياع بيانات، أو APK لا يُبنى. **أي شيء آخر: سجّله وأكمل.**

**نقطة مهمة:** بلا Firebase **لا تصل إشعارات على شكل بانر**. الخطوتان ١٣ و١٤ تختبران صندوق الوارد داخل التطبيق والرابط العميق — وهما ما نملكه نحن. غياب البانر ليس عيبًا في هذه الجولة.

## ٤-ح · أرسل لي النتيجة

الأهم: **أرقام الخطوات الساقطة ونصوص أخطائها.** وصورة لكل فشل تساوي أكثر من وصفه.

وأوامر تسجيل دليل البناء (الإصدارات، بصمة الملف، طراز الهاتف) في `WINDOWS_RUN.md` الخطوة ٨.

---

# الخطوة ٥ — ابدأ تعاقد Paymob / Fawry اليوم

⏱️ نصف ساعة منك · ⏳ ثم ٤–٨ أسابيع انتظار

### لماذا اليوم تحديدًا؟

**هذا البند الوحيد في المشروع كله الذي يزداد سوءًا بمجرد مرور الوقت.** كل شيء آخر ينتظرك أنت — تشتغل عليه فيتقدّم. أما التعاقد التجاري فينتظر جهة أخرى، ولا يوجد ما يسرّعه لاحقًا. كل يوم تأخير هنا هو يوم يُضاف إلى موعد أول ريال حقيقي.

### ماذا تفعل

تقدّم بطلب تاجر لدى Paymob (مصر) و/أو Fawry. تحتاج عادةً سجلًا تجاريًا وبطاقة ضريبية وحسابًا بنكيًا وشرحًا للنشاط.

### متى تعود إليّ

حين تحصل على **بيئة الاختبار (sandbox)**. عندها يجب التحقق من **ترتيب حقول الـHMAC** مقابل توثيقهم الحيّ — وهو توقيع رقمي يمنع تزوير إشعارات الدفع، **ولم أخترعه ولم أحزره**، لأن ترتيب حقول خاطئًا يعني قبول دفعات مزوّرة أو رفض دفعات صحيحة.

**لا يعطّل هذا شيئًا الآن:** حالات الاشتراك (تجربة، فعّال، فترة سماح، ملغى) كلها تعمل ومختبَرة. الناقص هو استلام المال الحقيقي فقط.

---

# الخطوة ٦ — دوّر حسابًا مسرَّبًا

⏱️ دقيقتان · ⚠️ افعلها قبل مشاركة المستودع

### ما القصة؟

كان بريد وكلمة مرور مكتوبين نصًّا داخل سكربتات في المستودع، وكانا يُستخدمان على نشر حيّ. أزلتُهما وصارت السكربتات تطلبهما كوسائط بلا قيم افتراضية.

**لكن Git يحفظ كل شيء.** أي شخص يستطيع استخراج أي نسخة قديمة من أي ملف. لم أُعِد كتابة التاريخ لأن ذلك يكسر نسخ كل من عمل على المستودع.

### الحل

**غيّر كلمة مرور ذلك الحساب، أو احذفه.** دقيقتان تُنهيان الموضوع نهائيًا. الحساب: `test1@example.com`.

---

# الخطوة ٧ — الإصدار على المتجر

⏱️ لاحقًا · ⛔ **لا تبدأها قبل نجاح الخطوة ٤**

## ٧-أ · Firebase — للإشعارات

مشروع واحد، وتطبيقان بداخله:

- `com.aifamilycoach.parent_app`
- `com.aifamilycoach.child_app`

نزّل `google-services.json` وضعه في `apps/parent-app/android/app/`.

**تطبيق الطفل لا يحتاجه** — لا يستخدم Firebase أصلًا. توثيق أقدم كان يطلبه من الاثنين وكان ذلك خطأً صُحِّح.

ثم `flutterfire configure`.

## ٧-ب · keystore — لتوقيع النسخة

سطر `keytool` بالضبط في `signing.properties.example`: ملفان (واحد لكل تطبيق)، ٤٠٩٦ بت، صيغة PKCS12.

**احتفظ بهما وبكلمتي السر في مكان آمن.** ضياع keystore يعني أنك **لا تستطيع تحديث تطبيقك على المتجر أبدًا** — لا Google يستطيع مساعدتك.

الملفات مستثناة من Git، ولم يُرفع منها شيء.

## ٧-ج · Play Console

حساب مطوّر (رسوم لمرة واحدة)، وصفحة التطبيق، ونموذج **أمان البيانات** — وهذا الأخير يستحق وقتًا: تطبيق أطفال يخضع لتدقيق أشدّ.

ملف الرفع هو AAB من `mobile-build.ps1 -Release` بعد ٧-أ و٧-ب.

## ٧-د · iOS — قرار مسجَّل، لا نسيان

الرقابة الأبوية الكاملة تحتاج `AccessibilityService` و`UsageStatsManager` — **ولا مقابل لهما في iOS**. تطبيق الطفل يُطرح بوصف «الإشراف يتطلب جهاز أندرويد»، أو لا يُطرح. تطبيق ولي الأمر على iOS سؤال منفصل لاحقًا.

---

# جدول القرار السريع

| لو عندك | افعل |
|---|---|
| نصف ساعة اليوم | **الخطوة ٥** (Paymob) — ساعة انتظارها لا تُعوَّض |
| ساعة | ٥ ثم ١ ثم ٣ |
| نصف يوم | ٥ · ١ · ٢ · ٣ |
| يوم كامل وجهاز Windows | الكل حتى ٤ |
| جهاز Windows فقط | ١ ثم ٤ (الخادم محليًا يكفي) |

---

# ما ينتظرني أنا

**الخطوة ٣ فقط.** أعطني اسم خدمة staging ورابطها، وأنشر وأتحقق من السلسلة كاملة على البيئة الحقيقية:

تسجيل ولي أمر ← اقتران طفل ← إنشاء هدف ← إنجاز الطفل ← تحقّق ولي الأمر ← منح المكافأة ← إشعار للطفل بنصّه ← إشعار مختلف لولي الأمر بنصّه ← رابط عميق يفتح الشاشة الصحيحة ← سجل قرار.

**ثم أُعيد السيناريو نفسه مرة ثانية** لأثبت أن شيئًا لا يتضاعف: منحة واحدة، سطر تاريخ واحد، إشعار واحد لكل جهة.

وأي شيء يسقط في تلك الجولة، أصلحه — **ذلك وحده** — وأعيد.
