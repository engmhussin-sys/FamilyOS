# حالة النشر — قِسْتُها بنفسي، لا انتظارًا لجواب

**٢٠٢٦-٠٨-٢١** · بعد أول بناء ناجح للصورة

---

## الخلاصة في سطرين

الصورة بُنيت ودُفعت بنجاح — هذا مثبت في سجلك.
**لكن staging ما زالت تُشغّل بناءً قديمًا**، وقياسي أدناه هو الدليل، لا التخمين.

---

## ما قِسْتُه على staging، الآن

| الطلب | ما ردّ به |
|---|---|
| `GET /health/live` | `200` · `{"status":"ok"}` |
| `GET /health/ready` | `200` · `{"status":"ok","database":true,"redis":true}` |
| `GET /api/v1/system/readiness` | **`503`** |
| `GET /api/v1/system/diagnostics` | **`500`** |

### لماذا هذان الرقمان هما الحكم

في الكود الحالي، المسـاران `/system/readiness` و`/system/diagnostics` **خلف `InternalAdminGuard`**. والحارس في NestJS يعمل **قبل** أن يصل الطلب إلى دالة المعالجة. فالمُتوقَّع من نداء بلا مفتاح هو **`401` وحدها**، مهما كانت حالة قاعدة البيانات.

**و`503` و`500` لا يمكن أن يخرجا إلا من داخل الدالة نفسها.** الأول هو ردّ لوحة الجاهزية حين يكون أحد مكوّناتها `NOT_READY`، والثاني عطل داخل المعالجة. أي أن **الطلب وصل إلى الدالة** — فلا حارس هناك.

هذا هو **بصمة البناء**: الاثنان كانا مفتوحَين للجميع قبل الإصلاح، وأُغلقا فيه. فردّ غير `401` يعني بناءً سابقًا للإصلاح. لا يحتاج هذا الفحص إلى مفتاح، ولا إلى دخول لوحة Railway.

### وهذه ليست ملاحظة تقنية فحسب

**لوحة المشغّل على staging مقروءة من الإنترنت في هذه اللحظة.** `‏/system/readiness` يكشف في حقول `detail` نصَّ خطأ قاعدة البيانات نفسه، وأي مزوّدي دفع مُهيّأون، ومسارات داخلية. و`/system/diagnostics` كان يكشف رقم البناء والكوميت و`NODE_ENV` وقائمة الرايات كاملة.

الإصلاح موجود في الكود ومُختبَر (١١ اختبارًا في `test/authz/system-diagnostics-anonymous.e2e.spec.ts`). **ما ينقص هو نشره.**

---

## و`/health/ready` قال `ok` طوال الوقت

وهذا هو الدرس الحقيقي في هذا اليوم. `‏/health/ready` يسأل: «هل تردّ Postgres على `SELECT 1`؟» — **وقاعدة متأخرة بثلاثين هجرة تجيب عن هذا السؤال بنفس البشاشة التي تجيب بها قاعدة اليوم.**

فشارة **«Deployed»** الخضراء تعني أن صورة دُفعت. لا تعني أن الحاوية أقلعت، ولا أن كودك هو الذي يردّ، ولا أن `prisma migrate deploy` عمل.

### ما أضفتُه اليوم ليصير هذا مقيسًا

**١ · حقل `schema` في تشخيص المشغّل.** يقرأ جدول `_prisma_migrations` الذي تديره Prisma نفسها، ويردّ: كم هجرة اكتملت، وأيّها الأخيرة، **وكم واحدة بدأت ولم تكتمل أو رُوجعت**. هذا الأخير هو الفارق بين «القاعدة متأخّرة» و«القاعدة نصف مهاجَرة» — والثاني أخطر بكثير.

الحقل **لا يحمل أي رقم متوقَّع مضمَّنًا في الصورة**. الصورة تقول ما في القاعدة فقط؛ والتوقّع يملكه المستودع. مصدران للحقيقة عن الرقم نفسه هو بالضبط ما كُتب هذا الملف لتفاديه.

**٢ · طبيب النشر — `scripts/deploy-doctor.ps1`.** يسأل مضيفًا حيًّا الأسئلة الخمسة، ويطبع لكل واحد `PASS` أو `WARN` أو `BLOCKED` وسطرًا واحدًا قابلًا للتنفيذ. لا يبني ولا ينشر ولا يغيّر شيئًا.

```powershell
.\scripts\deploy-doctor.ps1 https://familyos-staging.up.railway.app
```

هكذا — بلا مفتاح — يجيب عن السؤال الأهم فورًا: **أي بناء يردّ من هنا؟**

ولقراءة الكوميت والهجرات المطبَّقة:

```powershell
.\scripts\deploy-doctor.ps1 https://familyos-staging.up.railway.app -Key $env:INTERNAL_ADMIN_API_KEY
```

> **المفتاح لا يُطبع ولا يُسجَّل ولا يُكتب في ملف.** ومرّره من متغيّر بيئة كما بالأعلى لا كنصّ صريح، حتى لا يدخل تاريخ أوامر PowerShell عندك.

مثال من تشغيل حقيقي (على تطبيق يعمل فعلًا، لا محاكاة):

```
PASS     liveness            the process is up and answering
PASS     readiness           Postgres and Redis both reachable (database=true redis=true)
PASS     build-identity      the operator surface is CLOSED — this host is running current code
WARN     build-commit        NOT VERIFIED — pass -Key to read the deployed commit
WARN     schema-version      NOT VERIFIED — pass -Key to read the applied migrations
==========================================================================
0 blocked, 2 warned.
DEPLOY VERIFIED
```

على staging **الآن**، السطر الثالث سيكون:

```
BLOCKED  build-identity      OLD BUILD: the route answered (500) instead of refusing…
```

---

## ما أطلبه منك — بالترتيب

### ١ · شغّل الطبيب على البيئتين

```powershell
.\scripts\deploy-doctor.ps1 https://familyos-staging.up.railway.app
.\scripts\deploy-doctor.ps1 https://familyos-production-74ca.up.railway.app
```

**لم أستطع أنا قياس الإنتاج** — أداة الجلب عندي تطلب إذنًا صريحًا بعنوانه ولم يصلها. فسطرا الأمر أعلاه هما ما يجيب عن السؤال الذي طرحتُه بالأمس ولم يُجَب: **أين نُشر بناء الـ00:44؟**

- إن ردّ الإنتاج `PASS build-identity` → البناء ذهب إلى **الإنتاج**، ووصلت إليه ٢٩ هجرة على بيانات حيّة.
- إن ردّ `BLOCKED … OLD BUILD` على الاثنين → البناء نجح ولم يُرقَّ إلى أي بيئة بعد.

### ٢ · تأكّد من إعدادات خدمة الخادم على **staging**

مُحدِّد البيئة في أعلى صفحة Railway — **بدّله إلى `staging` قبل أي تعديل.**

```
Settings ← Source ← Root Directory   =  apps/backend
Settings ← Source ← Branch           =  abny/sprint-f1-unblock
Settings ← Config-as-code            =  railway.json
```

**الفرع هو أكثر ما أشكّ فيه.** لو كانت الخدمة تتابع `main`، فهي لم تر شيئًا مما رفعتَه، مهما نجح البناء — وهذا يفسّر تمامًا ما قِسْتُه.

### ٣ · المتغيّرات على staging

| المتغيّر | القيمة |
|---|---|
| `INTERNAL_ADMIN_API_KEY` | قيمة عشوائية طويلة تولّدها أنت — **لا تُرسلها إليّ ولا تضعها في Git** |
| `CORS_ALLOWED_ORIGINS` | عنوان لوحة التحكم على staging |
| `GIT_COMMIT_SHA` | يُمرَّر كـ build arg، ليصبح كل نشر منسوبًا إلى كوميت |

> بلا `INTERNAL_ADMIN_API_KEY`، الحارس **يفشل مغلقًا** — لا أحد يقرأ التشخيص، ولا حتى أنت. وهذا سلوك مقصود: مفتاح غير مضبوط يجب ألّا يعني «مفتوح للجميع» أبدًا.

### ٤ · أعد النشر، ثم شغّل الطبيب مرة أخرى

**لا تعتبر النشر ناجحًا لأن الشارة خضراء.** اعتبره ناجحًا حين يطبع الطبيب:

```
PASS  build-identity   the operator surface is CLOSED
PASS  schema-version   29 migrations applied, latest 0030_retire_duplicate_daily_goal_reward_rules — matches this repository exactly
DEPLOY VERIFIED
```

---

## بأي درجة إثبات وصل كل شيء

| العنصر | الدرجة |
|---|---|
| حالة staging أعلاه | **RUNTIME VERIFIED** — أربعة طلبات حقيقية إلى المضيف الحيّ |
| `MigrationStatusService` | **RUNTIME VERIFIED** — ٧ اختبارات، منها فرع الجدول المفقود على Postgres حقيقي |
| `deploy-doctor.sh` | **RUNTIME VERIFIED** — ٤ اختبارات تشغّل السكربت نفسه على تطبيق مُقلِع يستمع على منفذ حقيقي |
| `deploy-doctor.ps1` | **STATIC VERIFIED** — لا يوجد PowerShell في بيئتي، فلم أشغّله. يحرسه فحص تكافؤ في CI يسقط إن اختلف النصفان في فحص أو في درجته |
| حالة production | **NOT TESTED** — الخطوة ١ أعلاه |

`tsc` نظيف · حارس النطاق ٠ · حارس الأحداث ٠ · ٩٠٢ اختبارًا في النطاقات المتأثرة، كلها خضراء.
