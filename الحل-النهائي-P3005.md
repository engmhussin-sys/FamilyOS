# P3005 — أُغلق داخل النشر نفسه

**٢٠٢٦-٠٨-٢١** · بعد تسع محاولات

---

## ما تقوله صورك بالضبط

| البيئة | آخر نشر |
|---|---|
| **staging** | `#22` · **ACTIVE** · Deployment successful · منذ دقيقتين |
| **production** | `#22` · **FAILED** · «Deployment failed during deploy process» بعد `00:35` — والنشط ما زال نشر «feat(notifications): add Notification Decision Log» منذ **٣ أيام** |

فالمشكلة الآن **على الإنتاج وحده**. وسجلّ النشر يسمّيها بلا لبس: `P3005`، ثم `Stopping Container`.

**staging نجحت لأن قاعدتها صارت نظيفة. الإنتاج لم ينجح لأن قاعدته ليست كذلك.**

---

## لماذا تكرّرت تسع مرات

لأن `preDeployCommand` كان أمرًا واحدًا:

```
npx prisma migrate deploy
```

وهذا الأمر أمام قاعدة فيها جداول بلا سجلّ هجرات **لا يملك إلا جوابًا واحدًا: لا.** لا يفحص، ولا يشرح، ولا يقترح. يخرج بخطأ، فتُلغي Railway الترقية ويبقى النشر القديم يخدم — والشارة خضراء.

**تسع محاولات، وتسع مرات نفس السطرين.** المحاولة العاشرة كانت ستفعل الشيء نفسه.

فبدل أن أطلب منك خطوة يدوية عاشرة، **نقلتُ القرار إلى داخل النشر** — حيث القاعدة قابلة للوصول أصلًا (`postgres.railway.internal` لا يُحلّ من جهازك)، وحيث الصورة تحمل الهجرات والمخطّط والعميل معًا.

---

## ما يفعله النشر الآن

`preDeployCommand` صار `sh scripts/predeploy.sh` — على Railway وRender معًا:

1. **`migrate deploy`** كما كان. نجح؟ انتهى الأمر، ولا يُنفَّذ شيء ممّا تحته. **المسار السليم لم يتغيّر.**
2. **خطأ غير `P3005`** → يفشل تمامًا كما كان، بمخرجاته ورمز خروجه. لا يُعيد تفسير أعطال ليست من شأنه.
3. **`P3005`** → **يقيس القاعدة الحيّة على محورين مستقلَّين** قبل أن يقرّر شيئًا:

   | المحور | الأداة | ما يراه |
   |---|---|---|
   | الجداول والأعمدة | `prisma migrate diff` | الفروق البنيوية عن `schema.prisma` |
   | **SQL الهجرات نفسه** | `predeploy-schema-probe.js` | عدد سياسات `tenant_isolation` |

4. **الاثنان نظيفان** → البَصْل صِدق، فيُكتب (`migrate resolve --applied` لكل هجرة)، **ثم يُعاد `migrate deploy`** ليُثبت أن السجلّ اكتمل فعلًا لا ظنًّا.
5. **أي شكّ** → يرفض، ويطبع **ما قاسه** في سجلّ النشر.

**ولا يحذف ولا يعدّل شيئًا أبدًا.** الكتابة الوحيدة الممكنة هي صفوف في `_prisma_migrations`، وفي الحالة الرابعة فقط.

---

## ⚠️ والمحور الثاني هو الأهم — وهو أمني

`migrate diff` يقارن **الجداول والأعمدة**. وهو **أعمى تمامًا** عن كل ما تفعله هجراتك بـSQL خام. وأهمّ ذلك عندك: **Row Level Security**.

الهجرة `0004_tenant_rls_defence_in_depth` وخمس بعدها تُفعّل RLS وتنشئ سياسة `tenant_isolation` على كل جدول مرتبط بأسرة.

**وقاعدة أُنشئت بـ`prisma db push` فيها كل جدول وكل عمود — وليس فيها سياسة واحدة.** لأن `db push` يطبّق نموذج البيانات ولا يشغّل SQL الهجرات أبدًا.

فماذا كان سيحدث لو اكتفيتُ بـ`migrate diff`؟ كان سيقول **«لا فارق»**، ويُبَصَّل `0004` على أنه مطبَّق **إلى الأبد**، **فلا تُنشأ سياسات RLS على مضيف حيّ ولا مرة واحدة**. لا شيء كان سيبلّغ عن ذلك. التطبيق كان سيعمل. وطبقة الدفاع تحت عزل المستأجرين في منتجك كانت ستغيب بصمت.

**لذلك: صفر سياسات ⇒ رفض، مهما قال الـdiff.** والاختبار يؤكّد أن الـdiff **لا يُستشار أصلًا** في هذه الحالة.

---

## ماذا ستقرأ في سجلّ النشر القادم

**إن كانت قاعدة الإنتاج مُهاجَرة وضاع سجلّها فقط:**

```
[predeploy] P3005: this database has tables but no Prisma migration ledger.
[predeploy] probe: {"tenantIsolationPolicies":56,...,"baseTables":101,...}
[predeploy] the migration SQL HAS run here at some point
[predeploy] migrate diff: exit 0, 0 non-comment SQL lines
[predeploy]   marked applied : 0001_init
…
[predeploy] 29 migrations recorded as applied.
[predeploy] RELEASE STEP COMPLETE
```

**وينتهي الأمر — بلا خطوة يدوية واحدة منك.**

**وإن كانت قاعدة الإنتاج من `db push`:**

```
[predeploy] BLOCKED — THIS SCHEMA WAS NEVER BUILT BY THE MIGRATIONS.
[predeploy] There is not one `tenant_isolation` row-level-security policy…
```

عندها يفشل النشر **كما ينبغي** — لكنك تعرف أخيرًا **لماذا**، بدل `P3005` عارية. أرسل لي السجلّ وأنا أتولّى الباقي.

**وإن كان هناك انحراف بنيوي:** يطبع SQL الناقص كاملًا في السجلّ. وهذه هي المعلومة التي عجزتُ عن الوصول إليها منذ أمس.

---

## ما ترفعه، وما تفعله بعده

1. فُكّ الأرشيف فوق مجلد المشروع وادفع. **لا شيء يُحذف هذه المرة.**
2. انتظر بناء الإنتاج، ثم افتح **Deploy Logs** واقرأ أسطر `[predeploy]`.
3. تحقّق بعدها بالطبيب:

```powershell
.\scripts\deploy-doctor.ps1 https://familyos-production-74ca.up.railway.app -Key $env:INTERNAL_ADMIN_API_KEY
```

> **ولا تنسَ `INTERNAL_ADMIN_API_KEY` على خدمة الإنتاج قبل النشر.** بدونه يفشل الحارس مغلقًا فلا تقرأ التشخيص أنت أيضًا. ولوحة المشغّل على الإنتاج **ما زالت مقروءة من الإنترنت** حتى ينجح هذا النشر.

---

## درجات الإثبات

| | |
|---|---|
| `predeploy.sh` | **RUNTIME VERIFIED** — ١٠ اختبارات تشغّل السكربت نفسه، **تحت `dash`** لا bash: الصورة Alpine وصدفتها busybox، واختبار ناجح تحت bash لا يثبت شيئًا عن الآلة الوحيدة التي تشغّله |
| `predeploy-schema-probe.js` | **RUNTIME VERIFIED** — نُفِّذ على Postgres حقيقي وردّ `{"tenantIsolationPolicies":56,"baseTables":101,"migrationLedgerPresent":false}` |
| وصول الملفين إلى الصورة | **STATIC VERIFIED** — اختبار يؤكّد `COPY` في مرحلة runtime، وأن `.dockerignore` لم يعد يستبعد `scripts` كاملًا (كان يستبعدها، وكان ذلك سيُفشل البناء) |
| اتفاق المضيفين | **مُنفَّذ على الشجرة** — حارس الإعدادات في workflow النشر يسقط الآن إن عاد أيّ من Railway أو Render إلى `migrate deploy` عارية |
| حالة قاعدة الإنتاج | **NOT TESTED** — النشر القادم هو ما يقيسها |

`tsc` نظيف · ١٨ اختبارًا في `test/deploy` · حارس النطاق ٠ · حارس الأحداث ٠ · تكافؤ النصفين ٠.
